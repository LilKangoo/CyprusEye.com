!function(root){const TABLES=Object.freeze({offers:"car_offers",cities:"car_rental_cities",profiles:"car_pricing_profiles",profileCities:"car_pricing_profile_cities",availability:"car_offer_city_availability",dailyRateTiers:"car_offer_daily_rate_tiers",vehicleKinds:"car_vehicle_kinds",partners:"partners",partnerResources:"partner_resources",siteSettings:"site_settings",depositRules:"service_deposit_rules",depositOverrides:"service_deposit_overrides"});function normalizeId(value){return String(value||"").trim()}function staleConflict(message,details=null){const error=new Error(message||"Data changed since Review. Refresh and review again.");return error.code="car_multicity_stale_conflict",error.details=details,error}function internalError(message,details=null){const error=new Error(message||"Unexpected multi-city repository result.");return error.code="car_multicity_internal_error",error.details=details,error}function rows(result){return Array.isArray(result?.data)?result.data:result?.data&&"object"==typeof result.data?[result.data]:[]}function first(result){return rows(result)[0]||null}function assertResult(result){if(result?.error)throw result.error;return result}function assertAllowedPayload(payload,allowedFields,operation){const allowed=new Set(allowedFields||[]),forbidden=Object.keys(payload||{}).filter(key=>!allowed.has(key));if(forbidden.length)throw internalError(`${operation} contains forbidden fields: ${forbidden.join(", ")}`,{forbidden:forbidden})}const api=Object.freeze({TABLES:TABLES,create:function(options={}){const getClient="function"==typeof options.getClient?options.getClient:()=>options.client||null,core=options.core||root.CarRentalMulticityCore;if(!core)throw new Error("CarRentalMulticityCore dependency is required");function client(){const value=getClient();if(!value||"function"!=typeof value.from)throw new Error("Database connection not available");return value}async function listRows(table,select="*",configure=null){let query=client().from(table).select(select);return"function"==typeof configure&&(query=configure(query)),rows(assertResult(await query))}async function getOfferById(offerId){const id=normalizeId(offerId);return id?first(assertResult(await client().from(TABLES.offers).select("*").eq("id",id).limit(1))):null}async function listCities(){return listRows(TABLES.cities,"*",query=>query.order("sort_order",{ascending:!0}).order("code",{ascending:!0}))}async function listProfiles(){return listRows(TABLES.profiles,"*",query=>query.order("code",{ascending:!0}))}async function listProfileCities(){return listRows(TABLES.profileCities,"*",query=>query.order("pricing_profile_id",{ascending:!0}).order("city_id",{ascending:!0}))}async function listVehicleKinds(){return listRows(TABLES.vehicleKinds,"*",query=>query.order("sort_order",{ascending:!0}).order("code",{ascending:!0}))}async function listPartners(){return listRows(TABLES.partners,"id,name,status,can_manage_cars,cars_locations,updated_at",query=>query.order("name",{ascending:!0}))}async function getSiteSetting(){return first(assertResult(await client().from(TABLES.siteSettings).select("id,car_multi_city_mapped_enabled,car_threshold_daily_rates_enabled,updated_at").order("id",{ascending:!0}).limit(1)))}async function listAvailabilityByOfferId(offerId){const id=normalizeId(offerId);return id?listRows(TABLES.availability,"*",query=>query.eq("offer_id",id).order("city_id",{ascending:!0})):[]}async function listDailyRateTiersByOfferId(offerId){const id=normalizeId(offerId);return id?listRows(TABLES.dailyRateTiers,"*",query=>query.eq("offer_id",id).order("threshold_days",{ascending:!0})):[]}async function listPartnerResourcesByOfferId(offerId){const id=normalizeId(offerId);return id?listRows(TABLES.partnerResources,"id,partner_id,resource_type,resource_id,created_at",query=>query.eq("resource_type","cars").eq("resource_id",id).order("created_at",{ascending:!0})):[]}async function getCarsDepositDefault(){const matches=await listRows(TABLES.depositRules,"id,resource_type,mode,amount,currency,include_children,enabled,created_at,updated_at",query=>query.eq("resource_type","cars").limit(2));if(matches.length>1)throw internalError("Multiple Cars deposit default rules detected.");return matches[0]||null}async function getCarsDepositOverride(offerId){const id=normalizeId(offerId);if(!id)return null;const matches=await listRows(TABLES.depositOverrides,"id,resource_type,resource_id,mode,amount,currency,include_children,enabled,created_at,updated_at",query=>query.eq("resource_type","cars").eq("resource_id",id).limit(2));if(matches.length>1)throw internalError("Multiple exact Cars deposit overrides detected.",{offerId:id});return matches[0]||null}function getStorageBucket(){const value=getClient(),bucket=value?.storage?.from?.(core.VEHICLE_IMAGE_BUCKET);if(!bucket||"function"!=typeof bucket.upload)throw new Error("Car image storage is not available");return bucket}function safeImagePathPart(value){return String(value||"").toLowerCase().replace(/[^a-z0-9-]+/g,"-").replace(/^-+|-+$/g,"")||"pending"}function buildVehicleImagePath(file,options={}){const validation=core.validateVehicleImageFile(file);if(!validation.valid){const error=new Error(validation.errors[0]?.message||"Invalid vehicle image");throw error.code="car_multicity_image_validation_failed",error.details=validation,error}const exactId=normalizeId(options.offerId),temporaryId=normalizeId(options.temporaryId);return`car-${safeImagePathPart(exactId||temporaryId||`pending-${Date.now()}`)}-${safeImagePathPart(options.nonce||`${Date.now()}-${Math.random().toString(36).slice(2,10)}`)}.${validation.metadata.extension}`}async function getCatalog(){const[cities,profiles,profileCities,vehicleKinds,partners,siteSetting]=await Promise.all([listCities(),listProfiles(),listProfileCities(),listVehicleKinds(),listPartners(),getSiteSetting()]);return{cities:cities,profiles:profiles,profileCities:profileCities,vehicleKinds:vehicleKinds,partners:partners,siteSetting:siteSetting}}function assertCatalogContract(catalog){const setting=catalog?.siteSetting;if(!setting||"boolean"!=typeof setting.car_multi_city_mapped_enabled||"boolean"!=typeof setting.car_threshold_daily_rates_enabled)throw staleConflict("Cars capability settings could not be verified from the fresh catalog.")}async function exactOfferUpdate(offerId,expectedUpdatedAt,payload,allowedFields,operation){const id=normalizeId(offerId);if(!id||!expectedUpdatedAt)throw staleConflict("Exact offer ID and expectedUpdatedAt are required.",{offerId:id});assertAllowedPayload(payload,allowedFields,operation);let query=client().from(TABLES.offers).update(payload).eq("id",id).eq("updated_at",expectedUpdatedAt).select("*");const matches=rows(assertResult(await query));if(0===matches.length)throw staleConflict("Car offer changed since Review.",{offerId:id,expectedUpdatedAt:expectedUpdatedAt});if(1!==matches.length||normalizeId(matches[0].id)!==id)throw internalError("Exact offer update returned an unexpected number of rows.",{offerId:id,count:matches.length});return matches[0]}async function updateVehicleDetails(request){return exactOfferUpdate(request.offerId,request.expectedUpdatedAt,request.payload,core.VEHICLE_COLUMNS,"Vehicle update")}async function updatePricingProfile(request){if(assertAllowedPayload(request.payload,core.PRICING_EDIT_COLUMNS,"Pricing and profile update"),"legacy_compat"===core.normalizeCode(request.payload.pricing_strategy||"legacy_compat")){const profile=(await listProfiles()).find(row=>normalizeId(row.id)===normalizeId(request.payload.pricing_profile_id));if(!profile||core.profileLocation(profile)!==core.normalizeCode(request.payload.location))throw staleConflict("Pricing profile no longer matches the legacy location.",{profileId:request.payload.pricing_profile_id,location:request.payload.location})}return exactOfferUpdate(request.offerId,request.expectedUpdatedAt,request.payload,core.PRICING_EDIT_COLUMNS,"Pricing and profile update")}async function updatePartnerAssignment(request){assertAllowedPayload(request.payload,core.PARTNER_COLUMNS,"Partner update");const partnerId=normalizeId(request.payload.owner_partner_id);if(partnerId){const partner=(await listPartners()).find(row=>normalizeId(row.id)===partnerId);if(!partner||"active"!==partner.status||!0!==partner.can_manage_cars)throw staleConflict("Selected partner is no longer active for Cars.",{partnerId:partnerId})}return exactOfferUpdate(request.offerId,request.expectedUpdatedAt,request.payload,core.PARTNER_COLUMNS,"Partner update")}async function updateActivationState(request){const offerId=normalizeId(request.offerId);if(!offerId||!request.expectedUpdatedAt)throw staleConflict("Exact offer ID and expectedUpdatedAt are required for activation.",{offerId:offerId});assertAllowedPayload(request.payload,core.ACTIVATION_COLUMNS,"Exact offer activation update");const keys=Object.keys(request.payload||{}).sort(),activate="mapped"===request.payload?.availability_mode&&!0===request.payload?.is_available&&!0===request.payload?.is_published&&"approved"===request.payload?.submission_status&&keys.join(",")===["availability_mode","is_available","is_published","submission_status"].sort().join(","),unpublish=!1===request.payload?.is_published&&1===keys.length&&"is_published"===keys[0];if(!activate&&!unpublish)throw internalError("Activation request does not match the reviewed exact-offer contract.");if("function"!=typeof client().rpc)throw internalError("Transactional activation RPC is unavailable.");const result=await client().rpc("admin_set_car_threshold_offer_activation_state",{p_offer_id:offerId,p_expected_updated_at:request.expectedUpdatedAt,p_activate:activate});if("40001"===result?.error?.code)throw staleConflict("Car offer changed since Review.",{offerId:offerId,expectedUpdatedAt:request.expectedUpdatedAt});const matches=rows(assertResult(result));if(1!==matches.length||normalizeId(matches[0]?.id)!==offerId)throw internalError("Transactional activation returned an unexpected exact offer.",{offerId:offerId,count:matches.length});if(activate&&("mapped"!==matches[0].availability_mode||!0!==matches[0].is_available||!0!==matches[0].is_published||"approved"!==matches[0].submission_status))throw internalError("Transactional activation postcondition failed.",{offerId:offerId});if(unpublish&&!1!==matches[0].is_published)throw internalError("Transactional unpublish postcondition failed.",{offerId:offerId});return matches[0]}async function insertAvailability(request){if(assertAllowedPayload(request.payload,core.AVAILABILITY_COLUMNS,"Availability insert"),await async function(offerId,cityId){const matches=rows(assertResult(await client().from(TABLES.availability).select("*").eq("offer_id",normalizeId(offerId)).eq("city_id",normalizeId(cityId)).limit(2)));if(matches.length>1)throw internalError("Duplicate offer-city availability rows detected.",{offerId:offerId,cityId:cityId});return matches[0]||null}(request.payload.offer_id,request.payload.city_id))throw staleConflict("Availability row appeared since Review.",{offerId:request.payload.offer_id,cityId:request.payload.city_id});const result=assertResult(await client().from(TABLES.availability).insert(request.payload).select("*").single());if(!result.data)throw internalError("Availability insert did not return exactly one row.");return result.data}async function updateAvailability(request){assertAllowedPayload(request.payload,core.AVAILABILITY_COLUMNS,"Availability update");const offerId=normalizeId(request.offerId||request.payload.offer_id),cityId=normalizeId(request.cityId||request.payload.city_id);if(!offerId||!cityId||!request.expectedUpdatedAt)throw staleConflict("Availability exact key or timestamp is missing.");const updatePayload={...request.payload};delete updatePayload.offer_id,delete updatePayload.city_id;let query=client().from(TABLES.availability).update(updatePayload).eq("offer_id",offerId).eq("city_id",cityId).eq("updated_at",request.expectedUpdatedAt).select("*");const updated=rows(assertResult(await query));if(0===updated.length)throw staleConflict("Availability row changed since Review.",{offerId:offerId,cityId:cityId});if(1!==updated.length)throw internalError("Availability update returned multiple rows.",{offerId:offerId,cityId:cityId,count:updated.length});return updated[0]}async function saveAvailabilityBatch(plan){const offerId=normalizeId(plan?.exactOfferId);if(!offerId)throw staleConflict("Exact offer ID is required for the availability batch.");const result=assertResult(await client().rpc("admin_save_car_offer_city_availability_batch",{p_offer_id:offerId,p_expected_rows:core.clone(plan.expectedAvailabilityRows||[]),p_desired_rows:core.clone(plan.desiredAvailabilityRows||[])}));if(!result||null===result.data||void 0===result.data)throw internalError("Availability batch did not return a receipt.",{offerId:offerId});return result.data}async function insertDailyRateTier(request){assertAllowedPayload(request.payload,core.DAILY_RATE_TIER_COLUMNS.slice(1),"Daily-rate tier insert");const offerId=normalizeId(request.payload.offer_id);if(!offerId)throw staleConflict("Exact offer ID is required for a daily-rate tier.");if((await listRows(TABLES.dailyRateTiers,"id,offer_id,threshold_days,updated_at",query=>query.eq("offer_id",offerId).eq("threshold_days",request.payload.threshold_days).limit(2))).length)throw staleConflict("A tier with this exact threshold appeared since Review.",{offerId:offerId,thresholdDays:request.payload.threshold_days});const result=assertResult(await client().from(TABLES.dailyRateTiers).insert(request.payload).select("*").single());if(!result.data||normalizeId(result.data.offer_id)!==offerId)throw internalError("Daily-rate tier insert returned an unexpected row.");return result.data}async function updateDailyRateTier(request){const id=normalizeId(request.tierId),offerId=normalizeId(request.offerId);if(!id||!offerId||!request.expectedUpdatedAt)throw staleConflict("Exact tier ID, offer ID and timestamp are required.");assertAllowedPayload(request.payload,core.DAILY_RATE_TIER_COLUMNS.slice(1),"Daily-rate tier update");const payload={...request.payload};delete payload.offer_id;const updated=rows(assertResult(await client().from(TABLES.dailyRateTiers).update(payload).eq("id",id).eq("offer_id",offerId).eq("updated_at",request.expectedUpdatedAt).select("*")));if(0===updated.length)throw staleConflict("Daily-rate tier changed since Review.",{tierId:id,offerId:offerId});if(1!==updated.length)throw internalError("Daily-rate tier update returned multiple rows.",{tierId:id,count:updated.length});return updated[0]}async function deleteDailyRateTier(request){const id=normalizeId(request.tierId),offerId=normalizeId(request.offerId);if(!id||!offerId||!request.expectedUpdatedAt)throw staleConflict("Exact tier ID, offer ID and timestamp are required.");const removed=rows(assertResult(await client().from(TABLES.dailyRateTiers).delete().eq("id",id).eq("offer_id",offerId).eq("updated_at",request.expectedUpdatedAt).select("*")));if(0===removed.length)throw staleConflict("Daily-rate tier changed or disappeared since Review.",{tierId:id,offerId:offerId});if(1!==removed.length)throw internalError("Daily-rate tier delete returned multiple rows.",{tierId:id,count:removed.length});return removed[0]}async function insertOffer(payload){if(assertAllowedPayload(payload,core.CREATE_COLUMNS,"Vehicle create"),"legacy"!==payload.availability_mode)throw internalError("New offers must remain in legacy mode.");const result=assertResult(await client().from(TABLES.offers).insert(payload).select("*").single());if(!result.data||!normalizeId(result.data.id))throw internalError("Vehicle create did not return exactly one exact ID.");return result.data}async function executeStep(step,execution={}){if(!step||"success"===step.status)return step?.result||null;if("car_offer"===step.type&&"insert"===step.action)return insertOffer(step.payload);if("car_offer"===step.type&&"update"===step.action){if("create"===execution.planKind&&"$created_offer_id"===step.entityId){const createdOfferId=normalizeId(execution.createdOfferId),currentOffer=await getOfferById(createdOfferId);if(!currentOffer)throw staleConflict("Created exact offer disappeared before threshold finalization.",{offerId:createdOfferId});return exactOfferUpdate(createdOfferId,currentOffer.updated_at,step.payload,["pricing_strategy","min_rental_days","max_rental_days","is_available"],"Created threshold offer finalization")}let expectedUpdatedAt=step.expectedUpdatedAt;if("pricing_profile"===execution.planKind&&execution.tierMutated){const currentOffer=await getOfferById(step.entityId),expected=execution.plan?.preflightSnapshot?.offerContract||{};if(!currentOffer)throw staleConflict("Exact offer disappeared after tier save.",{offerId:step.entityId});const mutableByTierTrigger=new Set(["min_rental_days","updated_at"]);if(Object.keys(expected).some(field=>!mutableByTierTrigger.has(field)&&JSON.stringify(currentOffer[field]??null)!==JSON.stringify(expected[field]??null)))throw staleConflict("Car offer changed while daily-rate tiers were being saved.",{offerId:step.entityId});if(Number(currentOffer.min_rental_days)!==Number(execution.plan?.effectiveMinRentalDays))throw staleConflict("Server minimum does not match the lowest active tier.",{offerId:step.entityId});expectedUpdatedAt=currentOffer.updated_at}const request={offerId:step.entityId,expectedUpdatedAt:expectedUpdatedAt,payload:step.payload};if("vehicle"===execution.planKind)return updateVehicleDetails(request);if("pricing_profile"===execution.planKind)return updatePricingProfile(request);if("partner"===execution.planKind)return updatePartnerAssignment(request);if("activation"===execution.planKind)return updateActivationState(request);throw internalError(`Unsupported car offer plan kind: ${execution.planKind}`)}if("car_offer_city_availability"===step.type){const createdOfferId=normalizeId(execution.createdOfferId),payload={...step.payload};if("$created_offer_id"===payload.offer_id&&(payload.offer_id=createdOfferId),!payload.offer_id)throw internalError("Created offer ID is unavailable for availability insert.");if("insert"===step.action)return insertAvailability({payload:payload});const cityId=normalizeId(payload.city_id||step.entityId?.split(":").pop()),offerId=normalizeId(payload.offer_id||step.entityId?.split(":")[0]);if("update"===step.action)return updateAvailability({offerId:offerId,cityId:cityId,expectedUpdatedAt:step.expectedUpdatedAt,payload:payload});if("delete"===step.action)return async function(request){const offerId=normalizeId(request.offerId),cityId=normalizeId(request.cityId);if(!offerId||!cityId||!request.expectedUpdatedAt)throw staleConflict("Availability exact key or timestamp is missing.");const removed=rows(assertResult(await client().from(TABLES.availability).delete().eq("offer_id",offerId).eq("city_id",cityId).eq("updated_at",request.expectedUpdatedAt).select("*")));if(0===removed.length)throw staleConflict("Availability row changed or disappeared since Review.",{offerId:offerId,cityId:cityId});if(1!==removed.length)throw internalError("Availability delete returned multiple rows.",{offerId:offerId,cityId:cityId,count:removed.length});return removed[0]}({offerId:offerId,cityId:cityId,expectedUpdatedAt:step.expectedUpdatedAt})}if("car_offer_city_availability_batch"===step.type){if("availability"!==execution.planKind)throw internalError("Availability batch belongs to an unexpected plan.");return saveAvailabilityBatch(execution.plan)}if("car_offer_daily_rate_tier"===step.type){const payload={...step.payload||{}};"$created_offer_id"===payload.offer_id&&(payload.offer_id=normalizeId(execution.createdOfferId));const offerId=normalizeId(payload.offer_id||execution.plan?.exactOfferId);if("insert"===step.action)return insertDailyRateTier({payload:payload});if("update"===step.action)return updateDailyRateTier({tierId:step.entityId,offerId:offerId,expectedUpdatedAt:step.expectedUpdatedAt,payload:payload});if("delete"===step.action)return deleteDailyRateTier({tierId:step.entityId,offerId:offerId,expectedUpdatedAt:step.expectedUpdatedAt})}throw internalError(`Unsupported save step: ${step.type}/${step.action}`)}return Object.freeze({TABLES:TABLES,createCity:async function(draft){const catalog=await getCatalog();assertCatalogContract(catalog);const validation=core.validateCityDraft(draft,catalog.cities);if(!validation.valid){const error=new Error(validation.errors[0]?.message||"Invalid city draft.");throw error.code="car_multicity_validation_failed",error.details=validation,error}const payload={code:core.normalizeCode(draft.code),name_i18n:core.clone(draft.name_i18n),place_types:core.clone(draft.place_types),sort_order:Number(draft.sort_order),is_active:!1},result=assertResult(await client().from(TABLES.cities).insert(payload).select("*").single());if(!result.data||!1!==result.data.is_active)throw internalError("City create must return one inactive city.");return result.data},executePlan:async function(plan,options={}){if(!plan||0!==plan.globalMappedFlagChanges||0!==plan.globalThresholdFlagChanges||0!==plan.bookingChanges||0!==plan.priceCalculationChanges||0!==plan.depositRuleChanges)throw internalError("Unsafe or missing Stage 2C save plan.");const working=core.clone(plan),pendingImageSteps=working.steps.filter(step=>step?.payload?.image_url===core.PENDING_IMAGE_URL);if(pendingImageSteps.length){const uploadedImageUrl=normalizeId(options.uploadedImageUrl);if(!uploadedImageUrl)throw internalError("Reviewed image upload is missing before the exact-ID write.");pendingImageSteps.forEach(step=>{step.payload.image_url=uploadedImageUrl,(step.changes||[]).forEach(change=>{"image_url"===change.field&&change.after===core.PENDING_IMAGE_URL&&(change.after=uploadedImageUrl)})}),working.media&&(working.media.uploadedUrl=uploadedImageUrl)}working.status="running";let createdOfferId=null;const executionState={planKind:working.kind,createdOfferId:null,tierMutated:!1,plan:working};for(const step of working.steps)if((step.dependsOn||[]).some(key=>{const dependency=working.steps.find(candidate=>candidate.key===key);return"success"!==dependency?.status}))step.status="skipped",step.error={message:"Dependency failed."},options.onProgress?.(core.clone(working),core.clone(step));else{step.status="running",step.attempts+=1,options.onProgress?.(core.clone(working),core.clone(step));try{executionState.createdOfferId=createdOfferId;const result=await executeStep(step,executionState);step.status="success",step.result=core.clone(result),step.error=null,"create_offer"===step.key&&(createdOfferId=normalizeId(result?.id),working.exactOfferId=createdOfferId),"car_offer_daily_rate_tier"===step.type&&(executionState.tierMutated=!0)}catch(error){step.status="error",step.error={code:error?.code||null,message:String(error?.message||error)}}options.onProgress?.(core.clone(working),core.clone(step))}const failures=working.steps.filter(step=>"error"===step.status||"skipped"===step.status);return working.status=failures.length?working.steps.some(step=>"success"===step.status)?"partial":"error":"success",working.completedAt=(new Date).toISOString(),working},getCarsDepositDefault:getCarsDepositDefault,getCarsDepositOverride:getCarsDepositOverride,getCatalog:getCatalog,getCreateContext:async function(){const[catalog,depositRule]=await Promise.all([getCatalog(),getCarsDepositDefault()]);return{offer:null,availability:[],dailyRateTiers:[],partnerResources:[],depositRule:depositRule,depositOverride:null,...catalog,loadedAt:(new Date).toISOString()}},getFleetPresentationContext:async function(offerIds=[]){const exactOfferIds=Array.from(new Set((offerIds||[]).map(normalizeId).filter(Boolean))).sort(),catalog=await getCatalog();if(!exactOfferIds.length)return{...catalog,availability:[],dailyRateTiers:[],partnerResources:[],loadedAt:(new Date).toISOString()};const[availability,dailyRateTiers,partnerResources]=await Promise.all([listRows(TABLES.availability,"*",query=>query.in("offer_id",exactOfferIds).order("offer_id",{ascending:!0}).order("city_id",{ascending:!0})),listRows(TABLES.dailyRateTiers,"*",query=>query.in("offer_id",exactOfferIds).order("offer_id",{ascending:!0}).order("threshold_days",{ascending:!0})),listRows(TABLES.partnerResources,"id,partner_id,resource_type,resource_id,created_at",query=>query.eq("resource_type","cars").in("resource_id",exactOfferIds).order("resource_id",{ascending:!0}))]);return{...catalog,availability:availability,dailyRateTiers:dailyRateTiers,partnerResources:partnerResources,loadedAt:(new Date).toISOString()}},getOfferById:getOfferById,getOfferContext:async function(offerId){const id=normalizeId(offerId);if(!id)throw new Error("Exact car offer ID is required");const[offer,catalog,availability,dailyRateTiers,partnerResources,depositRule,depositOverride]=await Promise.all([getOfferById(id),getCatalog(),listAvailabilityByOfferId(id),listDailyRateTiersByOfferId(id),listPartnerResourcesByOfferId(id),getCarsDepositDefault(),getCarsDepositOverride(id)]);if(!offer)throw staleConflict("Exact car offer no longer exists.",{offerId:id});return{offer:offer,...catalog,availability:availability,dailyRateTiers:dailyRateTiers,partnerResources:partnerResources,depositRule:depositRule,depositOverride:depositOverride,loadedAt:(new Date).toISOString()}},getSiteSetting:getSiteSetting,insertAvailability:insertAvailability,insertDailyRateTier:insertDailyRateTier,insertOffer:insertOffer,buildVehicleImagePath:buildVehicleImagePath,listAvailabilityByOfferId:listAvailabilityByOfferId,listDailyRateTiersByOfferId:listDailyRateTiersByOfferId,listCities:listCities,listMappingImpact:async function(pricingProfileId,cityId,proposedMapping=null){const profileId=normalizeId(pricingProfileId),exactCityId=normalizeId(cityId),availability=await listRows(TABLES.availability,"offer_id,city_id,pickup_enabled,return_enabled,is_active,fee_mode,fee_per_direction,fee_note,updated_at",query=>query.eq("city_id",exactCityId)),offerIds=Array.from(new Set(availability.map(row=>normalizeId(row.offer_id)).filter(Boolean)));if(!offerIds.length)return{count:0,offerIds:[],readyOfferIds:[],readyAfterOfferIds:[],readinessInvalidatedOfferIds:[]};const[offers,allAvailability,catalog]=await Promise.all([listRows(TABLES.offers,"id,pricing_profile_id,location,availability_mode,is_available,is_published",query=>query.in("id",offerIds)),listRows(TABLES.availability,"offer_id,city_id,pickup_enabled,return_enabled,is_active,fee_mode,fee_per_direction,fee_note,updated_at",query=>query.in("offer_id",offerIds)),getCatalog()]),matching=offers.filter(offer=>normalizeId(offer.pricing_profile_id)===profileId),currentMappings=catalog.profileCities||[],nextMappings=proposedMapping?[...currentMappings.filter(row=>!(normalizeId(row.pricing_profile_id)===profileId&&normalizeId(row.city_id)===exactCityId)),{pricing_profile_id:profileId,city_id:exactCityId,pickup_supported:!0===proposedMapping.pickup_supported,return_supported:!0===proposedMapping.return_supported,legacy_pricing_city_key:core.normalizeCode(proposedMapping.legacy_pricing_city_key),is_active:!0===proposedMapping.is_active}]:currentMappings,readyIds=mappings=>matching.filter(offer=>core.getMappedReadiness({pricing:{profileId:profileId,location:core.normalizeCode(offer.location)},availability:allAvailability.filter(row=>normalizeId(row.offer_id)===normalizeId(offer.id))},{...catalog,profileCities:mappings}).ready).map(offer=>normalizeId(offer.id)).sort(),readyOfferIds=readyIds(currentMappings),readyAfterOfferIds=readyIds(nextMappings),readyAfterSet=new Set(readyAfterOfferIds);return{count:matching.length,offerIds:matching.map(offer=>normalizeId(offer.id)).sort(),readyOfferIds:readyOfferIds,readyAfterOfferIds:readyAfterOfferIds,readinessInvalidatedOfferIds:readyOfferIds.filter(offerId=>!readyAfterSet.has(offerId))}},listPartnerResourcesByOfferId:listPartnerResourcesByOfferId,listPartners:listPartners,listProfileCities:listProfileCities,listProfiles:listProfiles,listVehicleKinds:listVehicleKinds,removeVehicleImage:async function(path){const exactPath=normalizeId(path);if(!exactPath||exactPath.includes("..")||exactPath.startsWith("/"))throw internalError("Exact uploaded image path is invalid.",{path:exactPath});const bucket=getStorageBucket();if("function"!=typeof bucket.remove)throw new Error("Car image cleanup is not available");const result=assertResult(await bucket.remove([exactPath]));return{bucket:core.VEHICLE_IMAGE_BUCKET,path:exactPath,removed:!0,data:result?.data||null}},saveAvailabilityBatch:saveAvailabilityBatch,saveProfileCityMapping:async function(draft){const validationContext=await getCatalog();assertCatalogContract(validationContext);const validation=core.validateProfileCityDraft(draft,validationContext);if(!validation.valid){const error=new Error(validation.errors[0]?.message||"Invalid profile-city mapping.");throw error.code="car_multicity_validation_failed",error.details=validation,error}const payload={pricing_profile_id:normalizeId(draft.pricing_profile_id),city_id:normalizeId(draft.city_id),pickup_supported:!0===draft.pickup_supported,return_supported:!0===draft.return_supported,legacy_pricing_city_key:core.normalizeCode(draft.legacy_pricing_city_key),is_active:!0===draft.is_active},existing=(validationContext.profileCities||[]).find(row=>normalizeId(row.pricing_profile_id)===payload.pricing_profile_id&&normalizeId(row.city_id)===payload.city_id);if(!existing){const result=assertResult(await client().from(TABLES.profileCities).insert(payload).select("*").single());if(!result.data)throw internalError("Profile-city insert returned an unexpected result.");return result.data}if(!draft.expectedUpdatedAt||String(existing.updated_at||"")!==String(draft.expectedUpdatedAt))throw staleConflict("Profile-city mapping changed since Review.",{profileId:payload.pricing_profile_id,cityId:payload.city_id});const updatePayload={...payload};delete updatePayload.pricing_profile_id,delete updatePayload.city_id;const updated=rows(assertResult(await client().from(TABLES.profileCities).update(updatePayload).eq("pricing_profile_id",payload.pricing_profile_id).eq("city_id",payload.city_id).eq("updated_at",draft.expectedUpdatedAt).select("*")));if(0===updated.length)throw staleConflict("Profile-city mapping changed since Review.");if(1!==updated.length)throw internalError("Profile-city update returned multiple rows.");return updated[0]},updateAvailability:updateAvailability,updateActivationState:updateActivationState,updateDailyRateTier:updateDailyRateTier,deleteDailyRateTier:deleteDailyRateTier,updateCity:async function(draft){const id=normalizeId(draft.id);if(!id||!draft.expectedUpdatedAt)throw staleConflict("Exact city ID and timestamp are required.");const catalog=await getCatalog();assertCatalogContract(catalog);const current=(catalog.cities||[]).find(city=>normalizeId(city.id)===id);if(!current||String(current.updated_at||"")!==String(draft.expectedUpdatedAt))throw staleConflict("City changed since Review.",{cityId:id});const validation=core.validateCityDraft(draft,catalog.cities);if(!validation.valid){const error=new Error(validation.errors[0]?.message||"Invalid city draft.");throw error.code="car_multicity_validation_failed",error.details=validation,error}const payload={code:core.normalizeCode(draft.code),name_i18n:core.clone(draft.name_i18n),place_types:core.clone(draft.place_types),sort_order:Number(draft.sort_order),is_active:!0===draft.is_active},updated=rows(assertResult(await client().from(TABLES.cities).update(payload).eq("id",id).eq("updated_at",draft.expectedUpdatedAt).select("*")));if(0===updated.length)throw staleConflict("City changed since Review.",{cityId:id});if(1!==updated.length)throw internalError("City update returned multiple rows.",{cityId:id,count:updated.length});return updated[0]},updatePartnerAssignment:updatePartnerAssignment,updatePricingProfile:updatePricingProfile,updateVehicleDetails:updateVehicleDetails,uploadVehicleImage:async function(request={}){const validation=core.validateVehicleImageFile(request.file);if(!validation.valid){const error=new Error(validation.errors[0]?.message||"Invalid vehicle image");throw error.code="car_multicity_image_validation_failed",error.details=validation,error}const path=buildVehicleImagePath(request.file,request);request.onProgress?.({percent:10,status:"validated",path:path});const bucket=getStorageBucket(),upload=assertResult(await bucket.upload(path,request.file,{cacheControl:"31536000",upsert:!1,contentType:validation.metadata.type}));request.onProgress?.({percent:85,status:"uploaded",path:path});const publicResult=bucket.getPublicUrl(path),publicUrl=normalizeId(publicResult?.data?.publicUrl);if(!publicUrl){try{await(bucket.remove?.([path]))}catch(_error){}throw internalError("Uploaded vehicle image has no public URL.",{path:path})}return request.onProgress?.({percent:100,status:"complete",path:path,publicUrl:publicUrl}),{bucket:core.VEHICLE_IMAGE_BUCKET,path:normalizeId(upload?.data?.path||path),publicUrl:publicUrl,metadata:validation.metadata}}})},internalError:internalError,staleConflict:staleConflict});Object.defineProperty(root,"CarRentalMulticityRepository",{value:api,configurable:!1,enumerable:!0,writable:!1})}("undefined"!=typeof window?window:globalThis);