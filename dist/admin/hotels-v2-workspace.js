!function(root){const api=function(Core,Repository){if(!Core||!Repository)throw new Error("Hotels V2 Workspace dependencies are missing.");const state={initialized:!1,loading:!1,properties:[],workspace:null,activeTab:"overview",modal:null,pendingReview:null,lastFocused:null,calendar:{loading:!1,error:null,anchor_date:null,view:"month",data:null,selected_product_ids:[],mobile_product_id:null,selection_start:null,selection_end:null,selection_anchor:null,drag_active:!1}},WORKSPACE_TABS=Object.freeze([["overview","Overview"],["rooms","Rooms & Rates"],["calendar","Calendar"],["bookings","Bookings"],["payments","Payments"],["content","Content & Media"],["partner","Partner & Access"],["distribution","Distribution & Sync"],["activity","Activity"]]);function byId(id){return document.getElementById(id)}function escapeHtml(value){return String(null==value?"":value).replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;").replace(/'/g,"&#039;")}function escapeAttr(value){return escapeHtml(value).replace(/`/g,"&#096;")}function toast(message,type="info"){"function"==typeof window.showToast?window.showToast(String(message||""),type):"error"===type&&console.error(message)}function formatMoney(value,currency="EUR"){const amount=Number(value);if(!Number.isFinite(amount))return"Not configured";try{return new Intl.NumberFormat("en-IE",{style:"currency",currency:String(currency||"EUR")}).format(amount)}catch(_error){return`${String(currency||"EUR")} ${amount.toFixed(2)}`}}function isoDateFromUtc(date){return new Date(date).toISOString().slice(0,10)}function parseIsoDate(value){const match=/^(\d{4})-(\d{2})-(\d{2})$/.exec(String(value||""));if(!match)return null;const date=new Date(Date.UTC(Number(match[1]),Number(match[2])-1,Number(match[3])));return Number.isNaN(date.getTime())?null:date}function todayIsoDate(){const parts=new Intl.DateTimeFormat("en-CA",{timeZone:state.workspace?.property?.timezone||"Europe/Nicosia",year:"numeric",month:"2-digit",day:"2-digit"}).formatToParts(new Date),values=Object.fromEntries(parts.map(part=>[part.type,part.value]));return`${values.year}-${values.month}-${values.day}`}function addCalendarDays(value,days){const date=parseIsoDate(value);return date?(date.setUTCDate(date.getUTCDate()+Number(days||0)),isoDateFromUtc(date)):""}function activeCalendarRange(){const anchor=state.calendar.anchor_date||todayIsoDate();return"week"===state.calendar.view?function(anchor){const date=parseIsoDate(anchor)||parseIsoDate(todayIsoDate()),isoWeekday=date.getUTCDay()||7;date.setUTCDate(date.getUTCDate()-(isoWeekday-1));const start=isoDateFromUtc(date);return{start:start,end:addCalendarDays(start,6)}}(anchor):"two_months"===state.calendar.view?function(anchor){const date=parseIsoDate(anchor)||parseIsoDate(todayIsoDate()),first=new Date(Date.UTC(date.getUTCFullYear(),date.getUTCMonth(),1)),last=new Date(Date.UTC(date.getUTCFullYear(),date.getUTCMonth()+2,0));return{start:isoDateFromUtc(first),end:isoDateFromUtc(last)}}(anchor):function(anchor){const date=parseIsoDate(anchor)||parseIsoDate(todayIsoDate()),first=new Date(Date.UTC(date.getUTCFullYear(),date.getUTCMonth(),1)),last=new Date(Date.UTC(date.getUTCFullYear(),date.getUTCMonth()+1,0));return{start:isoDateFromUtc(first),end:isoDateFromUtc(last)}}(anchor)}function enumerateCalendarDates(start,end){const dates=[];for(let value=start;value&&value<=end;value=addCalendarDays(value,1))dates.push(value);return dates}function calendarDateLabel(value,options={}){const date=parseIsoDate(value);return date?new Intl.DateTimeFormat("en-GB",options.long?{weekday:"short",day:"numeric",month:"short",timeZone:"UTC"}:{weekday:"short",day:"numeric",timeZone:"UTC"}).format(date):value}function propertyTitle(property){return Core.i18nText(property?.name_i18n||property?.title_i18n||property?.title,"en",property?.slug||"Untitled property")}function statusTone(status){const normalized=String(status||"").toUpperCase();return normalized.includes("READY")?"success":"BLOCKED"===normalized?"danger":"LEGACY"===normalized?"neutral":"warning"}function architectureLabel(value){return"rooms_v2"===value?"Rooms V2":"Legacy"}function bookingModeLabel(value){return{request_confirmation:"Request confirmation",instant_booking:"Instant booking",external_redirect:"External redirect"}[value]||"Request confirmation"}function legacyPricingModelLabel(value){return{per_person_per_night:"Per-person legacy pricing",category_per_night:"Category-based legacy pricing",tiered_by_nights:"Tiered legacy pricing",flat_per_night:"Flat legacy nightly pricing"}[String(value||"").trim()]||"Legacy pricing"}function legacyPricingSummary(value){const source=function(value){const source=Core.asObject(value),embedded=Core.asObject(source.legacy_configuration);return Object.keys(embedded).length?embedded:source}(value),pricingEngine=window.CE_HOTEL_PRICING,rawRooms=Core.asArray(source.room_types),normalizedRooms="function"==typeof pricingEngine?.normalizeHotelRoomTypes?pricingEngine.normalizeHotelRoomTypes(rawRooms,source):rawRooms,propertyRuleCount=Core.asArray(Core.asObject(source.pricing_tiers).rules).length,roomRuleCount=normalizedRooms.reduce((count,room)=>count+Core.asArray(Core.asObject(room?.pricing_tiers).rules).length,0),propertyPhotos=Core.normalizeGallery(source.photos),propertyLevelProduct=0===normalizedRooms.length&&propertyRuleCount>0;let minNightlyRate=null;if("function"==typeof pricingEngine?.getHotelMinPricePerNight)try{const calculated=pricingEngine.getHotelMinPricePerNight(source,{preferredPersons:2});null!=calculated&&Number.isFinite(Number(calculated))&&(minNightlyRate=Number(calculated))}catch(_error){minNightlyRate=null}return{source:source,pricing_model:String(source.pricing_model||"").trim(),pricing_model_label:legacyPricingModelLabel(source.pricing_model),property_rule_count:propertyRuleCount,room_rule_count:roomRuleCount,pricing_rule_count:propertyRuleCount+roomRuleCount,room_count:normalizedRooms.length,rooms:normalizedRooms,configured_product_count:normalizedRooms.length||(propertyLevelProduct?1:0),product_kind:propertyLevelProduct?"property_level_accommodation":"structured_legacy_rooms",property_gallery_count:propertyPhotos.length,max_persons:Number(source.max_persons)>0?Number(source.max_persons):null,min_nightly_rate:minNightlyRate,currency:String(source.currency||Core.asObject(source.pricing_tiers).currency||"EUR").toUpperCase()}}function legacyPublicPriceMarkup(summary,options={}){return null!=summary.min_nightly_rate?`${options.compact?"":"From "}${escapeHtml(formatMoney(summary.min_nightly_rate,summary.currency))}${options.compact?"/night":" / night"}`:escapeHtml(summary.pricing_model_label)}function renderMigrationFieldClassifications(preview){const fields=Core.asArray(preview?.legacy_product?.field_classifications);return fields.length?`<div class="hotel-legacy-migration-fields">\n      <span class="hotel-workspace-eyebrow">Source → proposed Room Type</span>\n      <ul>${fields.map(item=>{const classification=String(item.classification||"UNKNOWN");return`<li>\n          <div><strong>${escapeHtml(item.field)}</strong><small>${escapeHtml(item.source_summary||"Not specified")}</small></div>\n          <span class="hotel-migration-classification hotel-migration-classification--${escapeAttr(classification.toLowerCase().replaceAll("_","-"))}">${escapeHtml((value=classification,String(value||"").replaceAll("_"," ")))}</span>\n          <p>${escapeHtml(item.note||"")}</p>\n        </li>`;var value}).join("")}</ul>\n    </div>`:""}function getPropertyReadiness(row){const source=Core.asObject(row?.readiness);return source.state?source:{state:"legacy"===row?.architecture_version?"LEGACY":row?.readiness_state||"DRAFT",preparation_state:row?.preparation_state||row?.readiness_state||"DRAFT",blockers:Core.asArray(row?.readiness_blockers),warnings:Core.asArray(row?.readiness_warnings)}}function init(){state.initialized||(state.initialized=!0,byId("hotelPropertySearch")?.addEventListener("input",renderPropertyList),byId("hotelPropertyArchitectureFilter")?.addEventListener("change",renderPropertyList),byId("hotelPropertyReadinessFilter")?.addEventListener("change",renderPropertyList),byId("btnAddHotel")?.addEventListener("click",openNewPropertyDialog),document.addEventListener("keydown",handleGlobalKeydown))}function handleGlobalKeydown(event){"Escape"===event.key&&(state.modal?closeModal():state.workspace&&closeWorkspace())}async function loadPropertyList(){if(init(),state.loading)return;state.loading=!0;const list=byId("hotelPropertyList");list&&(list.innerHTML='<div class="hotel-property-empty"><span class="hotel-workspace-spinner" aria-hidden="true"></span> Loading properties…</div>');try{state.properties=await Repository.listProperties(),renderPropertyList(),function(){const total=state.properties.length,published=state.properties.filter(property=>!0===property.is_published).length;if(byId("hotelsStatTotal")&&(byId("hotelsStatTotal").textContent=String(total)),byId("hotelsStatPublished")&&(byId("hotelsStatPublished").textContent=String(published)),byId("hotelsStatSubtitle")){const v2=state.properties.filter(property=>"rooms_v2"===property.architecture_version).length;byId("hotelsStatSubtitle").textContent=`${total-v2} legacy · ${v2} Rooms V2`}}()}catch(error){const message=error?.isFoundationMissing?"Hotels H2A Admin foundation is not deployed. Run the approved H2A SQL before deploying this UI.":error?.message||"Failed to load properties.";throw list&&(list.innerHTML=`<div class="hotel-property-empty hotel-property-empty--error">${escapeHtml(message)}</div>`),toast(message,"error"),error}finally{state.loading=!1}}function renderPropertyList(){const container=byId("hotelPropertyList");if(!container)return;const rows=function(){const search=String(byId("hotelPropertySearch")?.value||"").trim().toLowerCase(),architecture=String(byId("hotelPropertyArchitectureFilter")?.value||"all"),readiness=String(byId("hotelPropertyReadinessFilter")?.value||"all");return state.properties.filter(property=>{const ready=getPropertyReadiness(property),haystack=[propertyTitle(property),property.city,property.owner_partner_name||property.owner_partner?.name,property.slug].map(value=>String(value||"").toLowerCase()).join(" ");if(search&&!haystack.includes(search))return!1;if("all"!==architecture&&property.architecture_version!==architecture)return!1;const effectiveReady="legacy"===property.architecture_version&&"LEGACY"===readiness?"LEGACY":ready.preparation_state||ready.state;return"all"===readiness||effectiveReady===readiness||ready.state===readiness})}();rows.length?(container.innerHTML=rows.map(renderPropertyCard).join(""),container.querySelectorAll("[data-hotel-open-workspace]").forEach(button=>{button.addEventListener("click",()=>{openWorkspace(button.dataset.hotelOpenWorkspace)})}),container.querySelectorAll("[data-hotel-edit-basic]").forEach(button=>{button.addEventListener("click",()=>{openWorkspace(button.dataset.hotelEditBasic,{tab:"overview"})})}),container.querySelectorAll("[data-hotel-advanced]").forEach(button=>{button.addEventListener("click",()=>window.editHotel?.(button.dataset.hotelAdvanced))}),container.querySelectorAll("[data-hotel-toggle-legacy-publication]").forEach(button=>{button.addEventListener("click",()=>{const row=state.properties.find(property=>property.id===button.dataset.hotelToggleLegacyPublication);row&&"legacy"===row.architecture_version&&window.toggleHotelPublish?.(row.id,!0!==row.is_published)})})):container.innerHTML='<div class="hotel-property-empty">No properties match the current filters.</div>'}function renderPropertyCard(property){const readiness=getPropertyReadiness(property),preparation=readiness.preparation_state||readiness.state||"DRAFT",roomCount=Number(property.room_type_count||0),inventory=Number(property.total_inventory??property.configured_inventory??0),ratePlans=Number(property.rate_plan_count||0),hasPreparation=roomCount>0||ratePlans>0||Number(property.room_rate_count||readiness.room_rate_count||0)>0,upcoming=Number(property.upcoming_booking_count||0),price=null==property.price_from?null:Number(property.price_from),title=propertyTitle(property),image=String(property.cover_image_url||Core.asArray(property.photos)[0]||"").trim(),architecture=property.architecture_version||"legacy",legacySummary="legacy"===architecture?legacyPricingSummary(property):null,owner=property.owner_partner_name||property.owner_partner?.name||"Not assigned",publicLabel=property.is_published?"Published legacy page":"Not published",readinessLabel="legacy"!==architecture||hasPreparation?preparation.replaceAll("_"," "):"LEGACY";return`\n      <article class="hotel-property-card" data-property-id="${escapeAttr(property.id)}">\n        <div class="hotel-property-card__media">\n          ${image?`<img src="${escapeAttr(image)}" alt="" loading="lazy" />`:'<div class="hotel-property-card__placeholder" aria-hidden="true">⌂</div>'}\n        </div>\n        <div class="hotel-property-card__body">\n          <div class="hotel-property-card__heading">\n            <div>\n              <span class="hotel-workspace-eyebrow">${escapeHtml(architectureLabel(architecture))}</span>\n              <h3>${escapeHtml(title)}</h3>\n              <p>${escapeHtml(property.city||"Location not specified")} · ${escapeHtml(owner)}</p>\n            </div>\n            <span class="hotel-workspace-status hotel-workspace-status--${statusTone(readinessLabel)}">${escapeHtml(readinessLabel)}</span>\n          </div>\n          ${"legacy"===architecture?`\n            <div class="hotel-property-card__configuration-split">\n              <section class="hotel-property-card__legacy-pricing">\n                <span class="hotel-workspace-eyebrow">Current public pricing</span>\n                <strong>${legacyPublicPriceMarkup(legacySummary)}</strong>\n                <small>${escapeHtml(legacySummary.pricing_model_label)} · ${legacySummary.pricing_rule_count} legacy pricing rule${1===legacySummary.pricing_rule_count?"":"s"}</small>\n                <small>${legacySummary.configured_product_count} configured accommodation product${1===legacySummary.configured_product_count?"":"s"} · ${"property_level_accommodation"===legacySummary.product_kind?"property-level legacy product":"structured legacy rooms"}</small>\n              </section>\n              <section class="hotel-property-card__v2-preparation">\n                <span class="hotel-workspace-eyebrow">Rooms V2 preparation</span>\n                <div class="hotel-property-card__metrics hotel-property-card__metrics--preparation">\n                  <span><strong>${roomCount}</strong> normalized room types</span>\n                  <span><strong>${inventory}</strong> inventory</span>\n                  <span><strong>${ratePlans}</strong> rate plans</span>\n                  <span><strong>${null==price?"Not configured":escapeHtml(formatMoney(price,property.currency))}</strong> ${null==price?"shadow setup":"configured from"}</span>\n                </div>\n              </section>\n            </div>`:`\n            <div class="hotel-property-card__metrics">\n              <span><strong>${roomCount}</strong> room types</span>\n              <span><strong>${inventory}</strong> inventory</span>\n              <span><strong>${ratePlans}</strong> rate plans</span>\n              <span><strong>${null==price?"Not configured":escapeHtml(formatMoney(price,property.currency))}</strong> configured from</span>\n              <span><strong>${upcoming}</strong> upcoming</span>\n            </div>`}\n          <div class="hotel-property-card__meta">\n            <span>${escapeHtml(publicLabel)}</span>\n            <span>${escapeHtml(bookingModeLabel(property.booking_mode))}</span>\n            ${"legacy"===architecture?`<span>${upcoming} upcoming</span>`:""}\n            ${Core.asArray(readiness.blockers).length?`<span class="hotel-property-card__blocker">${escapeHtml(Core.asArray(readiness.blockers)[0])}</span>`:""}\n          </div>\n          <div class="hotel-property-card__actions">\n            <button class="btn-primary" type="button" data-hotel-open-workspace="${escapeAttr(property.id)}">Open workspace</button>\n            <button class="btn-secondary" type="button" data-hotel-edit-basic="${escapeAttr(property.id)}">Edit basic property</button>\n            <button class="btn-secondary" type="button" disabled title="Property duplication is deferred until rooms, rates and media copy semantics are reviewed">Duplicate</button>\n            ${"legacy"===architecture?`<button class="btn-secondary" type="button" data-hotel-toggle-legacy-publication="${escapeAttr(property.id)}">${property.is_published?"Unpublish":"Publish"}</button>`:'<button class="btn-secondary" type="button" disabled title="Rooms V2 publication is not available in H2A">Publish unavailable</button>'}\n            ${"legacy"===architecture?`<button class="btn-secondary" type="button" data-hotel-advanced="${escapeAttr(property.id)}">Advanced / legacy</button>`:'<button class="btn-secondary" type="button" disabled title="Legacy JSON pricing and publication controls do not apply to Rooms V2 drafts">Legacy editor unavailable</button>'}\n          </div>\n        </div>\n      </article>`}async function openWorkspace(hotelId,options={}){init();const id=Core.normalizeUuid(hotelId);if(!id)return;const directory=byId("hotelPropertyDirectory"),workspaceElement=byId("hotelPropertyWorkspace");if(workspaceElement){state.lastFocused=document.activeElement,directory&&(directory.hidden=!0),workspaceElement.hidden=!1,workspaceElement.innerHTML='<div class="hotel-property-empty"><span class="hotel-workspace-spinner" aria-hidden="true"></span> Loading Property Workspace…</div>';try{state.workspace=await Repository.getWorkspace(id),state.calendar={loading:!1,error:null,anchor_date:todayIsoDate(),view:"month",data:null,selected_product_ids:[],mobile_product_id:null,selection_start:null,selection_end:null,selection_anchor:null,drag_active:!1},state.activeTab=options.tab||"overview",renderWorkspace(),workspaceElement.scrollIntoView({block:"start"})}catch(error){workspaceElement.innerHTML=`\n        <div class="hotel-property-empty hotel-property-empty--error">\n          <p>${escapeHtml(error?.message||"Failed to load Property Workspace.")}</p>\n          <button class="btn-secondary" type="button" data-hotel-workspace-back>Back to properties</button>\n        </div>`,workspaceElement.querySelector("[data-hotel-workspace-back]")?.addEventListener("click",closeWorkspace),toast(error?.message||"Failed to load Property Workspace.","error")}}}function closeWorkspace(){const directory=byId("hotelPropertyDirectory"),workspaceElement=byId("hotelPropertyWorkspace");state.workspace=null,state.calendar.data=null,workspaceElement&&(workspaceElement.hidden=!0,workspaceElement.innerHTML=""),directory&&(directory.hidden=!1),state.lastFocused?.focus?.()}function renderWorkspace(){const container=byId("hotelPropertyWorkspace");if(!container||!state.workspace)return;const property=state.workspace.property,readiness=Core.deriveWorkspaceReadiness(state.workspace),preparationLabel=readiness.preparation_label||readiness.preparation_state;container.innerHTML=`\n      <header class="hotel-workspace-header">\n        <button class="hotel-workspace-back" type="button" data-hotel-workspace-back aria-label="Back to property list">←</button>\n        <div class="hotel-workspace-header__title">\n          <span class="hotel-workspace-eyebrow">Property Workspace · ${escapeHtml(architectureLabel(property.architecture_version))}</span>\n          <h2>${escapeHtml(propertyTitle(property))}</h2>\n          <p>${escapeHtml(property.city||"Location not specified")} · ${escapeHtml(bookingModeLabel(property.booking_mode))}</p>\n        </div>\n        <div class="hotel-workspace-header__status">\n          <span class="hotel-workspace-status hotel-workspace-status--${statusTone("legacy"===property.architecture_version?"LEGACY":preparationLabel)}">\n            ${escapeHtml("legacy"===property.architecture_version?"LEGACY PROPERTY":preparationLabel.replaceAll("_"," "))}\n          </span>\n          ${"legacy"===property.architecture_version&&readiness.has_configuration?`<span class="hotel-workspace-status hotel-workspace-status--${statusTone(preparationLabel)}">V2 ${escapeHtml(preparationLabel.replaceAll("_"," "))}</span>`:""}\n        </div>\n      </header>\n      <nav class="hotel-workspace-tabs" role="tablist" aria-label="Property Workspace sections">\n        ${WORKSPACE_TABS.map(([key,label])=>`\n          <button type="button" role="tab" data-hotel-workspace-tab="${key}" aria-selected="${state.activeTab===key}" class="${state.activeTab===key?"is-active":""}">${escapeHtml(label)}</button>\n        `).join("")}\n      </nav>\n      <div class="hotel-workspace-panel" id="hotelWorkspaceActivePanel" role="tabpanel"></div>`,container.querySelector("[data-hotel-workspace-back]")?.addEventListener("click",closeWorkspace),container.querySelectorAll("[data-hotel-workspace-tab]").forEach(button=>{button.addEventListener("click",()=>{state.activeTab=button.dataset.hotelWorkspaceTab,renderWorkspace()})}),renderActivePanel()}function renderActivePanel(){const panel=byId("hotelWorkspaceActivePanel");panel&&state.workspace&&({overview:renderOverviewPanel,rooms:renderRoomsPanel,calendar:renderCalendarPanel,bookings:renderBookingsPanel,payments:renderPaymentsPanel,content:renderContentPanel,partner:renderPartnerPanel,distribution:renderDistributionPanel,activity:renderActivityPanel}[state.activeTab]||renderOverviewPanel)(panel)}function workspacePanelHeader(title,description,actions=""){return`<header class="hotel-workspace-panel-header"><div><h3>${escapeHtml(title)}</h3><p>${escapeHtml(description)}</p></div>${actions}</header>`}function i18nFields(prefix,label,values,type="input"){const normalized=Core.normalizeI18n(values);return`<fieldset class="hotel-workspace-i18n"><legend>${escapeHtml(label)}</legend><div class="hotel-workspace-i18n-grid">\n      ${Core.LANGUAGES.map(language=>`<label class="admin-form-field"><span>${escapeHtml(language.toUpperCase())}</span>${"textarea"===type?`<textarea name="${escapeAttr(prefix)}_${language}" rows="3" dir="${"he"===language?"rtl":"ltr"}">${escapeHtml(normalized[language]||"")}</textarea>`:`<input name="${escapeAttr(prefix)}_${language}" type="text" value="${escapeAttr(normalized[language]||"")}" dir="${"he"===language?"rtl":"ltr"}" />`}</label>`).join("")}\n    </div></fieldset>`}function readI18n(formData,prefix){return Core.normalizeI18n(Object.fromEntries(Core.LANGUAGES.map(language=>[language,formData.get(`${prefix}_${language}`)])))}function childPolicyText(policy,minimumAge,options={}){if(!policy)return options.inherit?"Use property policy":"Not reviewed";try{return Core.childrenPolicyLabel(policy,minimumAge)}catch(_error){return"Invalid policy"}}function childPolicyOptions(policy,options={}){const current=String(policy||"");return`${options.allowInherit?`<option value="" ${current?"":"selected"}>Use property policy</option>`:`<option value="" ${current?"":"selected"} disabled>Choose policy</option>`}\n      <option value="allowed" ${"allowed"===current?"selected":""}>Children allowed</option>\n      <option value="not_allowed" ${"not_allowed"===current?"selected":""}>Adults only / No children</option>\n      <option value="minimum_age" ${"minimum_age"===current?"selected":""}>Children allowed from minimum age</option>`}function bindChildPolicyAge(form,policyName,ageName){const policy=form.elements[policyName],age=form.elements[ageName],sync=()=>{const enabled="minimum_age"===policy.value;age.closest("label").hidden=!enabled,age.disabled=!enabled,age.required=enabled,enabled||(age.value="")};policy.addEventListener("change",sync),sync()}function openPropertyChildrenPolicyEditor(){const property=state.workspace.property;openModal({title:"Children policy",body:`<form id="hotelPropertyChildPolicyForm" class="hotel-workspace-form">\n        <p class="hotel-workspace-intro">Choose how this property accepts guests classified as children. This does not define a legal age category.</p>\n        <div class="hotel-child-policy-options" role="group" aria-label="Children policy">\n          <label class="admin-form-field"><span>Children</span><select name="children_policy" required>${childPolicyOptions(property.children_policy)}</select></label>\n          <label class="admin-form-field"><span>Minimum child age</span><input name="minimum_child_age" type="number" min="${Core.CHILD_AGE_MIN}" max="${Core.CHILD_AGE_MAX}" step="1" value="${escapeAttr(property.minimum_child_age??"")}" /></label>\n        </div>\n        <p class="hotel-workspace-safety-note">For a legacy property this remains Admin/shadow metadata. Public guest selection is unchanged in H2B.1.</p>\n      </form>`,footer:'<button class="btn-secondary" type="button" data-hotel-modal-close>Cancel</button><button class="btn-primary" type="submit" form="hotelPropertyChildPolicyForm">Review policy</button>',onReady(overlay){const form=overlay.querySelector("#hotelPropertyChildPolicyForm");bindChildPolicyAge(form,"children_policy","minimum_child_age"),form.addEventListener("submit",async event=>{event.preventDefault();const fd=new FormData(form);let policy;try{policy=Core.normalizeChildrenPolicy(fd.get("children_policy"),fd.get("minimum_child_age"))}catch(error){return void toast(error.message,"error")}const policyPayload={children_policy:policy.policy,minimum_child_age:policy.minimum_age},before={id:property.id,children_policy:property.children_policy,minimum_child_age:property.minimum_child_age},after={id:property.id,...policyPayload},plan={hotel_id:property.id,expected_property_updated_at:property.updated_at||null,reviewed_at:(new Date).toISOString(),property_policy:policyPayload,room_policies:[]};closeModal({restoreFocus:!1}),await openReview({title:"Review property children policy",entity:"children_policy",before:before,after:after,onConfirm:()=>Repository.applyGuestPolicyPlan(plan),contextMessage:"No public booking form, legacy price, property architecture, feature flag or historical row is changed.",successMessage:"Property children policy saved as Rooms V2 Admin metadata."})})}})}function renderOverviewPanel(panel){const property=state.workspace.property,readiness=Core.deriveWorkspaceReadiness(state.workspace),preview=Core.migrationPreview(state.workspace),legacySummary="legacy"===property.architecture_version?legacyPricingSummary(property):null,partnerOptions=state.workspace.partners.map(partner=>`\n      <option value="${escapeAttr(partner.id)}" ${partner.id===property.owner_partner_id?"selected":""}>${escapeHtml(partner.name||partner.company_name||partner.id)}</option>\n    `).join("");var summary;panel.innerHTML=`\n      ${workspacePanelHeader("Overview","Property identity, location and operating configuration. Rooms and prices remain independent.",'<button class="btn-primary" type="submit" form="hotelWorkspaceOverviewForm">Review changes</button>')}\n      <div class="hotel-workspace-overview-grid">\n        <form id="hotelWorkspaceOverviewForm" class="hotel-workspace-card hotel-workspace-form">\n          <input type="hidden" name="property_id" value="${escapeAttr(property.id)}" />\n          ${i18nFields("title","Property name",property.title_i18n||property.title)}\n          ${i18nFields("description","Property description",property.description_i18n||property.description,"textarea")}\n          <div class="hotel-workspace-form-grid">\n            <label class="admin-form-field"><span>Address</span><input name="address_line" value="${escapeAttr(property.address_line||"")}" /></label>\n            <label class="admin-form-field"><span>City</span><input name="city" value="${escapeAttr(property.city||"")}" required /></label>\n            <label class="admin-form-field"><span>District / area</span><input name="district" value="${escapeAttr(property.district||"")}" /></label>\n            <label class="admin-form-field"><span>Postcode</span><input name="postal_code" value="${escapeAttr(property.postal_code||"")}" /></label>\n            <label class="admin-form-field"><span>Country</span><input name="country" value="${escapeAttr(property.country||"Cyprus")}" /></label>\n            <label class="admin-form-field"><span>Latitude</span><input name="latitude" type="number" step="any" value="${escapeAttr(property.latitude??"")}" /></label>\n            <label class="admin-form-field"><span>Longitude</span><input name="longitude" type="number" step="any" value="${escapeAttr(property.longitude??"")}" /></label>\n            <label class="admin-form-field"><span>Google Maps URL</span><input name="google_maps_url" type="url" value="${escapeAttr(property.google_maps_url||"")}" /></label>\n            <label class="admin-form-field"><span>Check-in from</span><input name="check_in_from" type="time" value="${escapeAttr(String(property.check_in_from||"").slice(0,5))}" /></label>\n            <label class="admin-form-field"><span>Check-out until</span><input name="check_out_until" type="time" value="${escapeAttr(String(property.check_out_until||"").slice(0,5))}" /></label>\n            <label class="admin-form-field"><span>Timezone</span><input name="timezone" value="${escapeAttr(property.timezone||"Europe/Nicosia")}" required /></label>\n            <label class="admin-form-field"><span>Currency</span><input name="currency" maxlength="3" value="${escapeAttr(property.currency||"EUR")}" required /></label>\n            <label class="admin-form-field"><span>Booking mode</span><select name="booking_mode">\n              ${Core.BOOKING_MODES.map(mode=>`<option value="${mode}" ${property.booking_mode===mode?"selected":""}>${escapeHtml(bookingModeLabel(mode))}</option>`).join("")}\n            </select></label>\n            <label class="admin-form-field"><span>Commercial owner</span><select name="owner_partner_id"><option value="">Not assigned</option>${partnerOptions}</select></label>\n          </div>\n          <fieldset><legend>Property amenities</legend>${amenitiesMarkup(property.amenities,"property_amenity")}</fieldset>\n          <div class="hotel-workspace-locked-fields">\n            <div><span>Architecture</span><strong>${escapeHtml(architectureLabel(property.architecture_version))}</strong><small>Changes only through a future reviewed activation.</small></div>\n            <div><span>Public state</span><strong>${property.is_published?"Published legacy page":"Not published"}</strong><small>Rooms V2 publishing is disabled in H2A.</small></div>\n            <div><span>Exact property ID</span><code>${escapeHtml(property.id)}</code></div>\n          </div>\n        </form>\n        <aside class="hotel-workspace-side-stack">\n          ${function(property){const reviewed=Boolean(property.children_policy);return`<section class="hotel-workspace-card hotel-guest-policy-card${reviewed?"":" is-unreviewed"}">\n      <span class="hotel-workspace-eyebrow">Guest policy · Rooms V2 metadata</span>\n      <h4>${escapeHtml(childPolicyText(property.children_policy,property.minimum_child_age))}</h4>\n      <p>${"legacy"===property.architecture_version?"Shadow metadata only. Current legacy public booking remains unchanged until an exact future migration.":"This policy will become authoritative only when the future Rooms V2 public booking contract is activated."}</p>\n      <button class="btn-secondary" type="button" data-edit-property-child-policy>${reviewed?"Edit children policy":"Review children policy"}</button>\n    </section>`}(property)}\n          ${legacySummary?`<section class="hotel-workspace-card hotel-legacy-live">\n            <span class="hotel-workspace-eyebrow">Current live legacy product</span>\n            <h4>${legacyPublicPriceMarkup(legacySummary)}</h4>\n            <dl>\n              <div><dt>Pricing model</dt><dd>${escapeHtml(legacySummary.pricing_model_label)}</dd></div>\n              <div><dt>Existing public price</dt><dd>${legacyPublicPriceMarkup(legacySummary)}</dd></div>\n              <div><dt>Legacy pricing rules</dt><dd>${legacySummary.pricing_rule_count}</dd></div>\n              <div><dt>Configured accommodation products</dt><dd>${legacySummary.configured_product_count}</dd></div>\n              <div><dt>Normalized room types</dt><dd>${state.workspace.room_types.length}</dd></div>\n              <div><dt>Legacy room rows</dt><dd>${legacySummary.room_count}</dd></div>\n              <div><dt>Existing property gallery</dt><dd>${legacySummary.property_gallery_count} photo${1===legacySummary.property_gallery_count?"":"s"}</dd></div>\n              <div><dt>Publication</dt><dd>${property.is_published?"Published":"Not published"}</dd></div>\n              <div><dt>Booking mode</dt><dd>${escapeHtml(bookingModeLabel(property.booking_mode))}</dd></div>\n            </dl>\n            ${summary=legacySummary,summary.rooms.length?`<div class="hotel-legacy-room-summary"><span class="hotel-workspace-eyebrow">Current legacy rooms / pricing</span><ul>${summary.rooms.map(room=>{const roomId=String(room.id||"").trim();let roomPrice=null;if(roomId&&"function"==typeof window.CE_HOTEL_PRICING?.getHotelMinPricePerNight)try{roomPrice=window.CE_HOTEL_PRICING.getHotelMinPricePerNight(summary.source,{preferredPersons:2,selectedRoomTypeId:roomId})}catch(_error){roomPrice=null}const label=Core.i18nText(room.name,"en",room.id||"Legacy room"),capacity=Number(room.max_persons)>0?`${Number(room.max_persons)} guests`:"Capacity not specified",inventory=Number(room.inventory_units)>0?`${Number(room.inventory_units)} units`:"Inventory not specified",plans=Core.asArray(room.rate_plans).length;return`<li><span><strong>${escapeHtml(label)}</strong><small>${escapeHtml(capacity)} · ${escapeHtml(inventory)} · ${plans} rate plan${1===plans?"":"s"}</small></span><b>${null==roomPrice?"Pricing inherited":legacyPublicPriceMarkup({...summary,min_nightly_rate:Number(roomPrice)},{compact:!0})}</b></li>`}).join("")}</ul></div>`:'<p class="hotel-legacy-live__empty">Property-level legacy pricing is active. No structured legacy room rows are configured.</p>'}\n            <p class="hotel-workspace-safety-note">This is the current legacy configuration. Rooms V2 preparation below is separate and cannot overwrite it.</p>\n          </section>`:""}\n          ${function(readiness){const preparation=readiness.preparation_label||readiness.preparation_state;return`<section class="hotel-workspace-card hotel-readiness-card">\n      <div class="hotel-readiness-card__header"><div><span class="hotel-workspace-eyebrow">Rooms V2 preparation</span><h4>${escapeHtml(preparation.replaceAll("_"," "))}</h4></div><span class="hotel-workspace-status hotel-workspace-status--${statusTone(preparation)}">${escapeHtml(preparation.replaceAll("_"," "))}</span></div>\n      <div class="hotel-readiness-progress"><span style="width:${readiness.ready_for_calendar?"100":readiness.has_configuration?"55":"12"}%"></span></div>\n      ${readiness.blockers.length?`<ul class="hotel-readiness-list hotel-readiness-list--blockers">${readiness.blockers.map(item=>`<li>${escapeHtml(item)}</li>`).join("")}</ul>`:'<p class="hotel-readiness-success">Configuration is structurally ready for the future Calendar stage.</p>'}\n      ${readiness.warnings.length?`<ul class="hotel-readiness-list">${readiness.warnings.map(item=>`<li>${escapeHtml(item)}</li>`).join("")}</ul>`:""}\n      <small>This is Admin readiness only. It never means the property is publicly live.</small>\n    </section>`}(readiness)}\n          <section class="hotel-workspace-card hotel-migration-preview">\n            <span class="hotel-workspace-eyebrow">Migration preview · read only</span>\n            <h4>${escapeHtml(preview.property_name)}</h4>\n            <dl><div><dt>Live accommodation products</dt><dd>${preview.legacy_live_product_count}</dd></div><div><dt>Legacy room rows</dt><dd>${preview.legacy_room_count}</dd></div><div><dt>Legacy pricing rules</dt><dd>${preview.legacy_pricing_rule_count}</dd></div><div><dt>Status</dt><dd>Not migrated</dd></div></dl>\n            <p>${escapeHtml(preview.messages[0])}</p>\n            ${preview.suggestions.length?`<ul>${preview.suggestions.map(item=>`<li>${escapeHtml(Core.i18nText(item.proposed_name,"en",item.proposed_code))} → draft Room Type</li>`).join("")}</ul>`:""}\n            ${legacySummary?function(preview,legacySummary){const product=Core.asObject(preview?.legacy_product),pricingPreview=Core.asObject(preview?.pricing_preview);if(!Object.keys(product).length)return"";const sevenArches=preview.property_id===Core.SEVEN_ARCHES_PROPERTY_ID?Core.sevenArchesShadowPreparation(state.workspace):null;return`<div class="hotel-legacy-product-preview">\n      <div class="hotel-legacy-product-preview__columns">\n        <section>\n          <span class="hotel-workspace-eyebrow">Current live product</span>\n          <h5>${escapeHtml(product.label||"Legacy property-level accommodation")}</h5>\n          <p><strong>${preview.legacy_live_product_count}</strong> configured accommodation product${1===preview.legacy_live_product_count?"":"s"}</p>\n          <ul>\n            <li>${legacyPublicPriceMarkup(legacySummary)}</li>\n            <li>${preview.legacy_pricing_rule_count} legacy pricing rule${1===preview.legacy_pricing_rule_count?"":"s"}</li>\n            <li>${preview.property_gallery_count} property gallery photo${1===preview.property_gallery_count?"":"s"}</li>\n          </ul>\n        </section>\n        <section>\n          <span class="hotel-workspace-eyebrow">Proposed V2 shadow</span>\n          ${sevenArches?`<h5>2 confirmed apartments</h5><p><strong>${sevenArches.eligible?"Ready for photo selection and Review":"Preparation blocked"}</strong></p><small>Upper Floor Apartment · Ground Floor Apartment</small>`:"<h5>Room Type #1</h5><p><strong>Awaiting Admin confirmation</strong></p><small>No Room Type, Rate Plan, Room Rate or Calendar row has been created.</small>"}\n        </section>\n      </div>\n      ${renderMigrationFieldClassifications(preview)}\n      ${pricingPreview.requires_occupancy_los_model?`<div class="hotel-legacy-pricing-blocker">\n        <strong>Pricing activation remains separate</strong>\n        <p>${pricingPreview.rule_count} rules combine ${Core.asArray(pricingPreview.guest_counts).length} guest counts with ${Core.asArray(pricingPreview.stay_thresholds).length} stay thresholds. H2B can preserve the matrix in shadow, but the shared Standard plan remains inactive until cancellation/payment conditions and the complete oracle are reviewed.</p>\n        <code>${escapeHtml(pricingPreview.oracle)}</code>\n      </div>`:""}\n      ${sevenArches?.eligible?'<button class="btn-primary" type="button" data-prepare-seven-arches-apartments>Prepare 2 existing apartments</button>':preview.can_prepare_existing_accommodation&&!sevenArches?'<button class="btn-primary" type="button" data-prepare-legacy-accommodation>Prepare existing accommodation as Room Type</button>':""}\n      ${sevenArches&&!sevenArches.eligible?`<p class="hotel-property-card__blocker">${escapeHtml(sevenArches.blocker)}</p>`:""}\n      <p class="hotel-workspace-safety-note">Pricing migration is separate and remains blocked until the full legacy price oracle can be reproduced.</p>\n    </div>`}(preview,legacySummary):""}\n          </section>\n        </aside>\n      </div>`;const overviewForm=byId("hotelWorkspaceOverviewForm");overviewForm?.addEventListener("submit",handleOverviewReview),overviewForm?.querySelector("[data-amenity-search]")?.addEventListener("input",event=>{const term=event.currentTarget.value.trim().toLowerCase();overviewForm.querySelectorAll(".hotel-amenity-group").forEach(group=>{group.hidden=term&&!group.textContent.toLowerCase().includes(term)})}),panel.querySelector("[data-prepare-legacy-accommodation]")?.addEventListener("click",()=>openLegacyAccommodationPreparation(preview)),panel.querySelector("[data-prepare-seven-arches-apartments]")?.addEventListener("click",openSevenArchesPreparation),panel.querySelector("[data-edit-property-child-policy]")?.addEventListener("click",openPropertyChildrenPolicyEditor)}async function handleOverviewReview(event){event.preventDefault();const form=event.currentTarget,fd=new FormData(form),before=Core.clone(state.workspace.property),after={...before,title_i18n:readI18n(fd,"title"),description_i18n:readI18n(fd,"description"),address_line:Core.asNullableText?Core.asNullableText(fd.get("address_line")):String(fd.get("address_line")||"").trim()||null,city:String(fd.get("city")||"").trim(),district:String(fd.get("district")||"").trim()||null,postal_code:String(fd.get("postal_code")||"").trim()||null,country:String(fd.get("country")||"").trim()||"Cyprus",latitude:Core.asNumber(fd.get("latitude"),null),longitude:Core.asNumber(fd.get("longitude"),null),google_maps_url:String(fd.get("google_maps_url")||"").trim()||null,check_in_from:String(fd.get("check_in_from")||"").trim()||null,check_out_until:String(fd.get("check_out_until")||"").trim()||null,timezone:String(fd.get("timezone")||"").trim(),currency:String(fd.get("currency")||"").trim().toUpperCase(),booking_mode:String(fd.get("booking_mode")||"").trim(),owner_partner_id:Core.normalizeUuid(fd.get("owner_partner_id"))||null,amenities:fd.getAll("property_amenity")};return Core.i18nText(after.title_i18n,"en")?after.city?/^[A-Z]{3}$/.test(after.currency)?Core.BOOKING_MODES.includes(after.booking_mode)?void await openReview({title:"Review property changes",entity:"property",before:before,after:after,operation:Core.operationForEntity("property",after,before)}):toast("Booking mode is invalid.","error"):toast("Currency must be a three-letter code.","error"):toast("City is required.","error"):toast("English property name is required.","error")}function renderRoomsPanel(panel){const workspace=state.workspace,migration=Core.migrationPreview(workspace),sevenArches=workspace.property.id===Core.SEVEN_ARCHES_PROPERTY_ID?Core.sevenArchesShadowPreparation(workspace):null,rooms=workspace.room_types.slice().sort((a,b)=>a.sort_order-b.sort_order||a.code.localeCompare(b.code)),plans=workspace.rate_plans.slice().sort((a,b)=>a.sort_order-b.sort_order||a.code.localeCompare(b.code));panel.innerHTML=`\n      ${workspacePanelHeader("Rooms & Rates","Manage Room Types, optional physical units, reusable Rate Plans and their sellable products.",`\n        <div class="hotel-workspace-panel-actions"><button class="btn-secondary" type="button" data-add-rate-plan>+ Rate Plan</button><button class="btn-primary" type="button" data-add-room>${rooms.length?"+ Another Room Type":"+ Room Type"}</button></div>`)}\n      ${migration.legacy_product?`<section class="hotel-workspace-card hotel-legacy-product-banner">\n        <div><span class="hotel-workspace-eyebrow">Current live legacy product</span><h4>${migration.legacy_live_product_count} configured accommodation product${1===migration.legacy_live_product_count?"":"s"}</h4><p>${migration.legacy_pricing_rule_count} legacy pricing rules remain live. Below are ${rooms.length} normalized Room Types in inert V2 preparation.</p></div>\n        ${sevenArches?.eligible?'<button class="btn-primary" type="button" data-prepare-seven-arches-apartments>Prepare 2 existing apartments</button>':migration.can_prepare_existing_accommodation&&!sevenArches?'<button class="btn-primary" type="button" data-prepare-legacy-accommodation>Prepare existing accommodation as Room Type</button>':'<span class="hotel-workspace-status hotel-workspace-status--warning">Not migrated</span>'}\n      </section>`:""}\n      <div class="hotel-rooms-layout">\n        <section>\n          <div class="hotel-workspace-section-title"><div><h4>Normalized Room Types</h4><p>One Room Type may use several Rate Plans.</p></div><span>${rooms.length}</span></div>\n          <div class="hotel-room-grid">${rooms.length?rooms.map(renderRoomCard).join(""):renderEmptyState("No Room Types yet","Create the first draft room without changing the current public Hotel.")}</div>\n        </section>\n        <section>\n          <div class="hotel-workspace-section-title"><div><h4>Property Rate Plans</h4><p>Create once and connect to one or many Room Types.</p></div><span>${plans.length}</span></div>\n          <div class="hotel-rate-plan-list">${plans.length?plans.map(renderRatePlanCard).join(""):renderEmptyState("No Rate Plans yet","Add Flexible, Non-refundable or another property-level plan.")}</div>\n        </section>\n      </div>`,panel.querySelector("[data-add-room]")?.addEventListener("click",()=>openRoomEditor()),panel.querySelector("[data-add-rate-plan]")?.addEventListener("click",()=>openRatePlanEditor()),panel.querySelector("[data-prepare-legacy-accommodation]")?.addEventListener("click",()=>openLegacyAccommodationPreparation(migration)),panel.querySelector("[data-prepare-seven-arches-apartments]")?.addEventListener("click",openSevenArchesPreparation),function(panel){panel.querySelectorAll("[data-edit-room]").forEach(button=>button.addEventListener("click",()=>openRoomEditor(button.dataset.editRoom))),panel.querySelectorAll("[data-duplicate-room]").forEach(button=>button.addEventListener("click",()=>async function(roomId){const source=state.workspace.room_types.find(room=>room.id===roomId);if(!source)return;const duplicate=Core.buildDuplicateRoom(source,state.workspace),operation=Core.operationForEntity("room_type",{...duplicate,source_id:source.id},source,"duplicate");operation.id=duplicate.id,operation.expected_version=source.version,closeModal({restoreFocus:!1}),await openReview({title:"Review Room Type duplicate",entity:"room_type",before:source,after:duplicate,operation:operation,successMessage:"Room Type duplicated as a draft. Units and Rate products were not copied."})}(button.dataset.duplicateRoom))),panel.querySelectorAll("[data-disable-room]").forEach(button=>button.addEventListener("click",()=>async function(roomId){const room=state.workspace.room_types.find(candidate=>candidate.id===roomId);if(!room)return;const after={...room,status:"disabled"};await openReview({title:"Review Room Type disable",entity:"room_type",before:room,after:after,operation:Core.operationForEntity("room_type",after,room,"disable"),successMessage:"Room Type disabled. No rows were deleted."})}(button.dataset.disableRoom))),panel.querySelectorAll("[data-add-unit]").forEach(button=>button.addEventListener("click",()=>openUnitEditor(null,button.dataset.addUnit))),panel.querySelectorAll("[data-edit-unit]").forEach(button=>button.addEventListener("click",()=>openUnitEditor(button.dataset.editUnit))),panel.querySelectorAll("[data-edit-rate-plan]").forEach(button=>button.addEventListener("click",()=>openRatePlanEditor(button.dataset.editRatePlan))),panel.querySelectorAll("[data-connect-room-rate]").forEach(button=>button.addEventListener("click",()=>openRoomRateEditor(null,button.dataset.connectRoomRate))),panel.querySelectorAll("[data-edit-room-rate]").forEach(button=>button.addEventListener("click",()=>openRoomRateEditor(button.dataset.editRoomRate))),panel.querySelectorAll("[data-edit-room-child-policy]").forEach(button=>button.addEventListener("click",()=>function(roomId){const room=state.workspace.room_types.find(candidate=>candidate.id===roomId);room&&openModal({title:`Children policy · ${Core.i18nText(room.name_i18n,"en",room.code)}`,body:`<form id="hotelRoomChildPolicyForm" class="hotel-workspace-form">\n        <p class="hotel-workspace-intro">Use the property policy by default, or configure an exact Room Type override.</p>\n        <div class="hotel-child-policy-options">\n          <label class="admin-form-field"><span>Room policy</span><select name="children_policy_override">${childPolicyOptions(room.children_policy_override,{allowInherit:!0})}</select></label>\n          <label class="admin-form-field"><span>Minimum child age</span><input name="minimum_child_age_override" type="number" min="${Core.CHILD_AGE_MIN}" max="${Core.CHILD_AGE_MAX}" step="1" value="${escapeAttr(room.minimum_child_age_override??"")}" /></label>\n        </div>\n        <div class="hotel-workspace-locked-fields"><div><span>Property default</span><strong>${escapeHtml(childPolicyText(state.workspace.property.children_policy,state.workspace.property.minimum_child_age))}</strong></div><div><span>Exact Room Type</span><code>${escapeHtml(room.id)}</code></div></div>\n      </form>`,footer:'<button class="btn-secondary" type="button" data-hotel-modal-close>Cancel</button><button class="btn-primary" type="submit" form="hotelRoomChildPolicyForm">Review override</button>',onReady(overlay){const form=overlay.querySelector("#hotelRoomChildPolicyForm");bindChildPolicyAge(form,"children_policy_override","minimum_child_age_override"),form.addEventListener("submit",async event=>{event.preventDefault();const fd=new FormData(form);let policy;try{policy=Core.normalizeChildrenPolicy(fd.get("children_policy_override"),fd.get("minimum_child_age_override"),{allowInherit:!0})}catch(error){return void toast(error.message,"error")}const before={id:room.id,children_policy_override:room.children_policy_override,minimum_child_age_override:room.minimum_child_age_override},after={id:room.id,children_policy_override:policy.policy,minimum_child_age_override:policy.minimum_age},plan={hotel_id:state.workspace.property.id,expected_property_updated_at:state.workspace.property.updated_at||null,reviewed_at:(new Date).toISOString(),room_policies:[{room_type_id:room.id,expected_version:room.version,children_policy_override:policy.policy,minimum_child_age_override:policy.minimum_age}]};closeModal({restoreFocus:!1}),await openReview({title:"Review Room Type children policy",entity:"room_children_policy",before:before,after:after,onConfirm:()=>Repository.applyGuestPolicyPlan(plan),contextMessage:`Exact Room Type ${room.id}. This override does not alter pricing, inventory, publication or the property default.`,successMessage:"Room Type children-policy override saved."})})}})}(button.dataset.editRoomChildPolicy)))}(panel)}function renderEmptyState(title,message){return`<div class="hotel-workspace-empty"><strong>${escapeHtml(title)}</strong><span>${escapeHtml(message)}</span></div>`}function renderRoomCard(room){const units=state.workspace.units.filter(unit=>unit.room_type_id===room.id),activeUnits=units.filter(unit=>"active"===unit.status).length,roomRates=state.workspace.room_rates.filter(rate=>rate.room_type_id===room.id),inventoryLabel="pooled"===room.inventory_mode?`${room.base_inventory_count} pooled units`:`${activeUnits} active physical units`,capacityLabel=null!=room.max_occupancy?`Max ${room.max_occupancy} guests · adult/child split not confirmed`:`${room.capacity_adults} adults · ${room.capacity_children} children`;let childPolicy;try{const resolved=Core.resolveChildrenPolicy(state.workspace.property,room);childPolicy=`${childPolicyText(resolved.policy,resolved.minimum_age)} · ${"property"===resolved.source?"property default":"room override"}`}catch(_error){childPolicy="Children policy not reviewed"}return`<article class="hotel-room-card" data-room-id="${escapeAttr(room.id)}">\n      <header><div><span class="hotel-workspace-eyebrow">${escapeHtml(room.code)}</span><h4>${escapeHtml(Core.i18nText(room.name_i18n,"en",room.code))}</h4><p>${escapeHtml(capacityLabel)} · ${escapeHtml(inventoryLabel)}</p></div><span class="hotel-workspace-status hotel-workspace-status--${statusTone("active"===room.status?"READY":"disabled"===room.status?"BLOCKED":"DRAFT")}">${escapeHtml(room.status.toUpperCase())}</span></header>\n      <div class="hotel-room-card__details"><span>${escapeHtml(Core.formatBedConfiguration(room.bed_configuration))}</span><span>${null==room.bathrooms?"Bathrooms not specified":`${room.bathrooms} bathroom(s)`}</span><span>${null==room.size_sqm?"Size not specified":`${room.size_sqm} m²`}</span></div>\n      <div class="hotel-room-card__guest-policy"><span>Children</span><strong>${escapeHtml(childPolicy)}</strong><button class="btn-secondary" type="button" data-edit-room-child-policy="${escapeAttr(room.id)}">Edit</button></div>\n      <div class="hotel-room-card__rates">${roomRates.length?roomRates.map(renderRoomRateLine).join(""):"<p>No Rate Plans connected.</p>"}</div>\n      ${"unitized"===room.inventory_mode?`<details class="hotel-room-card__units"><summary>${units.length} physical units</summary>${units.length?units.map(renderUnitLine).join(""):"<p>No units configured.</p>"}<button class="btn-secondary" type="button" data-add-unit="${escapeAttr(room.id)}">Add unit</button></details>`:""}\n      <footer><button class="btn-secondary" type="button" data-edit-room="${escapeAttr(room.id)}">Edit</button><button class="btn-secondary" type="button" data-connect-room-rate="${escapeAttr(room.id)}">Rates</button><button class="btn-secondary" type="button" data-duplicate-room="${escapeAttr(room.id)}">Duplicate</button>${"disabled"!==room.status?`<button class="btn-secondary hotel-danger-action" type="button" data-disable-room="${escapeAttr(room.id)}">Disable</button>`:""}</footer>\n    </article>`}function renderRoomRateLine(rate){const plan=state.workspace.rate_plans.find(candidate=>candidate.id===rate.rate_plan_id),schedule=rate.pricing_schedule_id?state.workspace.pricing_schedules.find(candidate=>candidate.id===rate.pricing_schedule_id):null,tierCount=schedule?state.workspace.pricing_schedule_tiers.filter(tier=>tier.schedule_id===schedule.id&&!1!==tier.is_active).length:0,detail=schedule?`Inactive shared schedule · ${tierCount} tiers · H3 allocation review required`:rate.is_active?"Active product":"Inactive product",price=schedule?"Shared schedule":formatMoney(rate.base_nightly_rate,rate.currency);return`<button type="button" class="hotel-room-rate-line" data-edit-room-rate="${escapeAttr(rate.id)}"><span><strong>${escapeHtml(Core.i18nText(plan?.name_i18n,"en","Rate Plan"))}</strong><small>${escapeHtml(detail)}</small></span><b>${escapeHtml(price)}</b></button>`}function renderUnitLine(unit){return`<button type="button" class="hotel-unit-line" data-edit-unit="${escapeAttr(unit.id)}"><span>${escapeHtml(Core.i18nText(unit.name_i18n,"en",unit.code))}</span><small>${escapeHtml(unit.status)}</small></button>`}function renderRatePlanCard(plan){const productCount=state.workspace.room_rates.filter(rate=>rate.rate_plan_id===plan.id).length;return`<article class="hotel-rate-plan-card"><div><span class="hotel-workspace-eyebrow">${escapeHtml(plan.code)}</span><h5>${escapeHtml(Core.i18nText(plan.name_i18n,"en",plan.code))}</h5><p>${escapeHtml(Core.cancellationPolicyLabel(plan.cancellation_policy))} · ${productCount} room products</p></div><div><span class="hotel-workspace-status hotel-workspace-status--${statusTone(plan.is_active?"READY":"DRAFT")}">${plan.is_active?"ACTIVE":"INACTIVE"}</span><button class="btn-secondary" type="button" data-edit-rate-plan="${escapeAttr(plan.id)}">Edit</button></div></article>`}function openModal({title:title,body:body,footer:footer="",onReady:onReady=null,onClose:onClose=null,className:className=""}){closeModal({restoreFocus:!1}),state.lastFocused=document.activeElement;const overlay=document.createElement("div");return overlay.className=`hotel-workspace-modal ${className}`.trim(),overlay.innerHTML=function(title,body,footer=""){return`<div class="hotel-workspace-modal__backdrop" data-hotel-modal-close></div>\n      <section class="hotel-workspace-modal__dialog" role="dialog" aria-modal="true" aria-labelledby="hotelWorkspaceModalTitle">\n        <header><div><span class="hotel-workspace-eyebrow">Hotels 2.0 Admin</span><h3 id="hotelWorkspaceModalTitle">${escapeHtml(title)}</h3></div><button type="button" class="hotel-workspace-modal__close" data-hotel-modal-close aria-label="Close">×</button></header>\n        <div class="hotel-workspace-modal__body">${body}</div>\n        <footer>${footer||'<button class="btn-secondary" type="button" data-hotel-modal-close>Close</button>'}</footer>\n      </section>`}(title,body,footer),document.body.appendChild(overlay),overlay.hotelWorkspaceOnClose="function"==typeof onClose?onClose:null,document.body.classList.add("hotel-workspace-modal-open"),state.modal=overlay,overlay.querySelectorAll("[data-hotel-modal-close]").forEach(button=>button.addEventListener("click",event=>{event.currentTarget.classList.contains("hotel-workspace-modal__backdrop")&&event.target!==event.currentTarget||closeModal()})),onReady?.(overlay),requestAnimationFrame(()=>{const focusTarget=overlay.querySelector('[autofocus], input:not([type="hidden"]), select, textarea, button');focusTarget?.focus?.()}),overlay}function setModalSaving(overlay,saving){overlay&&(overlay.dataset.saving=saving?"true":"false",overlay.querySelectorAll("[data-hotel-modal-close]").forEach(control=>{control.setAttribute("aria-disabled",saving?"true":"false"),"disabled"in control&&(control.disabled=Boolean(saving))}))}function closeModal(options={}){const overlay=state.modal;if(!overlay)return;if("true"===overlay.dataset.saving&&!0!==options.force)return;const onClose=overlay.hotelWorkspaceOnClose;overlay.remove(),state.modal=null,state.pendingReview=null,document.body.classList.remove("hotel-workspace-modal-open"),options.skipCleanup||"function"!=typeof onClose||Promise.resolve(onClose()).catch(error=>console.error("Failed to clean pending Hotel workspace media:",error)),!1!==options.restoreFocus&&state.lastFocused?.focus?.()}function displayReviewValue(value){return null==value||""===value?"Not specified":"boolean"==typeof value?value?"Yes":"No":"object"==typeof value?JSON.stringify(value,null,2):String(value)}async function openReview({title:title,entity:entity,before:before,after:after,operation:operation,operations:operations,onConfirm:onConfirm,onCancel:onCancel,onApplyError:onApplyError,onStaleReview:onStaleReview,closeOnApplyError:closeOnApplyError=!1,successMessage:successMessage,contextMessage:contextMessage=""}){const reviewedOperations=Array.isArray(operations)?operations:[operation],rows=Core.buildReviewRows(entity,before,after);rows.length?(state.pendingReview={reviewedOperations:reviewedOperations},openModal({title:title,className:"hotel-workspace-modal--review",body:`<div class="hotel-review-summary"><p>One atomic exact-property operation will be applied only after all version and relationship checks pass.</p><dl><div><dt>Property ID</dt><dd><code>${escapeHtml(state.workspace.property.id)}</code></dd></div><div><dt>Entity</dt><dd>${escapeHtml(entity.replaceAll("_"," "))}</dd></div><div><dt>Changes</dt><dd>${rows.length}</dd></div></dl></div>\n        ${contextMessage?`<p class="hotel-workspace-safety-note">${escapeHtml(contextMessage)}</p>`:""}\n        <div class="hotel-review-table-wrap"><table class="hotel-review-table"><thead><tr><th>Field</th><th>Before</th><th>After</th></tr></thead><tbody>${rows.map(row=>`<tr><th>${escapeHtml(row.field)}</th><td><pre>${escapeHtml(displayReviewValue(row.before))}</pre></td><td><pre>${escapeHtml(displayReviewValue(row.after))}</pre></td></tr>`).join("")||'<tr><td colspan="3">The exact reviewed operation has no scalar field diff.</td></tr>'}</tbody></table></div>\n        <p class="hotel-workspace-safety-note">Public Hotels V2 remains disabled. This save does not publish, convert, book or alter historical rows.</p>`,footer:'<button class="btn-secondary" type="button" data-hotel-modal-close>Back</button><button class="btn-primary" type="button" data-hotel-review-confirm>Save reviewed changes</button>',onClose:onCancel,onReady(overlay){overlay.querySelector("[data-hotel-review-confirm]")?.addEventListener("click",async event=>{const button=event.currentTarget;button.disabled=!0,button.textContent="Saving…",setModalSaving(overlay,!0);try{if("function"==typeof onConfirm){const result=await onConfirm();result?.workspace&&(state.workspace=result.workspace),closeModal({restoreFocus:!1,skipCleanup:!0,force:!0}),renderWorkspace(),loadPropertyList().catch(()=>{}),toast(successMessage||"Reviewed Property Workspace changes saved.","success")}else await async function(operations,options={}){const reviewedOperations=Core.asArray(operations),isExactRoomTypeSave=1===reviewedOperations.length&&"room_type"===reviewedOperations[0]?.entity,plan=isExactRoomTypeSave?Core.buildRoomTypePlan(state.workspace,reviewedOperations[0]):Core.buildWorkspacePlan(state.workspace,reviewedOperations),result=isExactRoomTypeSave?await Repository.applyRoomTypePlan(plan):await Repository.applyWorkspacePlan(plan);return state.workspace=result.workspace,closeModal({restoreFocus:!1,skipCleanup:!0,force:!0}),renderWorkspace(),loadPropertyList().catch(()=>{}),toast(options.successMessage||"Reviewed Property Workspace changes saved.","success"),result}(reviewedOperations,{successMessage:successMessage})}catch(error){let failure=error;if(error?.isStale&&"function"==typeof onStaleReview)try{const freshReview=await onStaleReview(error);if(freshReview)return setModalSaving(overlay,!1),closeModal({restoreFocus:!1,skipCleanup:!0,force:!0}),await openReview(freshReview),void toast(freshReview.reReviewMessage||"Save stopped because this workspace changed. Fresh values are ready for review; nothing was retried automatically.","warning")}catch(refreshError){failure=refreshError,failure.userMessage||(failure.userMessage="The stale save was stopped safely, but fresh values could not be prepared for review. Refresh the workspace and try again.")}try{await(onApplyError?.(failure))}catch(cleanupError){console.error("Failed to clean up reviewed Hotel media upload:",cleanupError)}if(failure?.diagnosticContext&&console.error("Reviewed Hotel save rejected.",{code:failure.code||null,reason:failure.diagnosticReason||null,diagnosticContext:failure.diagnosticContext}),!0===failure?.closeReviewAfterStale)return setModalSaving(overlay,!1),closeModal({restoreFocus:!1,skipCleanup:!0,force:!0}),renderWorkspace(),failure.openSevenArchesConflictReview?openSevenArchesConflictReview(failure.openSevenArchesConflictReview):failure.reopenSevenArchesPreparation&&openSevenArchesPreparation(failure.reopenSevenArchesPreparation),void toast(failure.userMessage||failure.message,failure.openSevenArchesConflictReview?"warning":"error");setModalSaving(overlay,!1),closeOnApplyError&&closeModal({restoreFocus:!1,skipCleanup:!0,force:!0}),button.disabled=!1,button.textContent="Save reviewed changes",toast(failure?.userMessage||(failure?.isStale?"Save stopped: this configuration changed after Review. Refresh and review the fresh values.":failure?.isAmbiguousOutcome?"The save result could not be confirmed because the connection was interrupted. Uploaded media was preserved. Refresh Property Workspace before retrying; do not upload the same files again.":failure?.isDefinitiveFailure?"The reviewed save was rejected safely. Refresh the workspace and review the current configuration; no partial save was kept.":failure?.message||"Reviewed save failed. No database changes were kept."),failure?.isAmbiguousOutcome?"warning":"error")}})}})):toast("There are no changes to review.","info")}function slugify(value){return String(value||"").trim().toLowerCase().normalize("NFKD").replace(/[\u0300-\u036f]/g,"").replace(/[^a-z0-9]+/g,"-").replace(/^-+|-+$/g,"").slice(0,120)}function openNewPropertyDialog(){const exactId=Core.newUuid();openModal({title:"Create Rooms V2 property draft",body:`<p class="hotel-workspace-intro">Create one unpublished top-level property. Rooms, rates and calendar are configured inside its workspace.</p>\n        <form id="hotelNewPropertyForm" class="hotel-workspace-form">\n          <div class="hotel-workspace-form-grid"><label class="admin-form-field"><span>Exact property ID</span><input value="${escapeAttr(exactId)}" readonly /></label><label class="admin-form-field"><span>Internal slug</span><input name="slug" pattern="[a-z0-9][a-z0-9-]*" required /></label></div>\n          ${i18nFields("title","Property name",{})}\n          ${i18nFields("description","Property description",{},"textarea")}\n          <div class="hotel-workspace-form-grid">\n            <label class="admin-form-field"><span>City</span><input name="city" required /></label>\n            <label class="admin-form-field"><span>Address</span><input name="address_line" /></label>\n            <label class="admin-form-field"><span>Timezone</span><input name="timezone" value="Europe/Nicosia" required /></label>\n            <label class="admin-form-field"><span>Currency</span><input name="currency" value="EUR" maxlength="3" required /></label>\n          </div>\n          <div class="hotel-workspace-locked-fields"><div><span>Architecture</span><strong>Rooms V2 draft</strong></div><div><span>Booking mode</span><strong>Request confirmation</strong></div><div><span>Public state</span><strong>Unpublished</strong></div></div>\n        </form>`,footer:'<button class="btn-secondary" type="button" data-hotel-modal-close>Cancel</button><button class="btn-primary" type="submit" form="hotelNewPropertyForm">Review draft</button>',onReady(overlay){const form=overlay.querySelector("#hotelNewPropertyForm"),englishName=form?.elements?.title_en,slugInput=form?.elements?.slug;englishName?.addEventListener("input",()=>{slugInput.dataset.edited||(slugInput.value=slugify(englishName.value))}),slugInput?.addEventListener("input",()=>{slugInput.dataset.edited="true"}),form?.addEventListener("submit",event=>{event.preventDefault();const fd=new FormData(form),payload={slug:slugify(fd.get("slug")),title_i18n:readI18n(fd,"title"),description_i18n:readI18n(fd,"description"),city:String(fd.get("city")||"").trim(),address_line:String(fd.get("address_line")||"").trim()||null,timezone:String(fd.get("timezone")||"").trim()||"Europe/Nicosia",currency:String(fd.get("currency")||"EUR").trim().toUpperCase()};if(!(payload.slug&&Core.i18nText(payload.title_i18n,"en")&&payload.city&&/^[A-Z]{3}$/.test(payload.currency)))return void toast("Slug, English name, city and valid currency are required.","error");closeModal({restoreFocus:!1});const before={id:exactId,architecture_version:null,is_published:null},after={id:exactId,...payload,architecture_version:"rooms_v2",booking_mode:"request_confirmation",is_published:!1,status:"draft"},rows=Core.buildReviewRows("property",before,after);openModal({title:"Review new property draft",body:`<div class="hotel-review-summary"><p>The property will be created as an inert Rooms V2 draft. No feature flag or public page is enabled.</p><code>${escapeHtml(exactId)}</code></div><div class="hotel-review-table-wrap"><table class="hotel-review-table"><tbody>${rows.map(row=>`<tr><th>${escapeHtml(row.field)}</th><td><pre>${escapeHtml(displayReviewValue(row.after))}</pre></td></tr>`).join("")}</tbody></table></div>`,footer:'<button class="btn-secondary" type="button" data-hotel-modal-close>Cancel</button><button class="btn-primary" type="button" data-create-property-confirm>Create draft</button>',onReady(reviewOverlay){reviewOverlay.querySelector("[data-create-property-confirm]")?.addEventListener("click",async confirmEvent=>{const button=confirmEvent.currentTarget;let result;button.disabled=!0,button.textContent="Creating…",setModalSaving(reviewOverlay,!0);try{result=await Repository.createPropertyDraft(exactId,payload)}catch(error){return setModalSaving(reviewOverlay,!1),button.disabled=!1,button.textContent="Create draft",void toast(error?.message||"Property draft creation failed.","error")}state.workspace=result.workspace,closeModal({restoreFocus:!1,skipCleanup:!0,force:!0}),toast("Rooms V2 property draft created. Public behavior remains disabled.","success");try{await loadPropertyList(),await openWorkspace(exactId,{tab:"overview"})}catch(error){toast("Property draft was created, but the workspace refresh failed. Reload Hotels to continue.","error")}})}})})}})}function bedRowMarkup(bed={}){const type=Core.BED_TYPES.includes(bed.type)?bed.type:"double";return`<div class="hotel-bed-row">\n      <label class="admin-form-field"><span>Bed type</span><select data-bed-type>${Core.BED_TYPES.map(candidate=>`<option value="${candidate}" ${candidate===type?"selected":""}>${escapeHtml(Core.BED_LABELS[candidate])}</option>`).join("")}</select></label>\n      <label class="admin-form-field"><span>Quantity</span><input data-bed-quantity type="number" min="1" max="20" step="1" value="${escapeAttr(bed.quantity||1)}" /></label>\n      <label class="admin-form-field hotel-bed-other"><span>Other bed label (EN)</span><input data-bed-label value="${escapeAttr(Core.i18nText(bed.label,"en"))}" /></label>\n      <button class="btn-secondary" type="button" data-remove-bed aria-label="Remove bed row">Remove</button>\n    </div>`}function amenitiesMarkup(selectedValues,inputName="room_amenity"){const selected=new Set(Core.normalizeAmenities(selectedValues)),catalog=state.workspace.amenities_catalog,known=new Set(catalog.map(amenity=>String(amenity.code||"").trim()).filter(Boolean)),groups=new Map;catalog.forEach(amenity=>{const category=String(amenity.category||"Other");groups.has(category)||groups.set(category,[]),groups.get(category).push(amenity)});const groupHtml=Array.from(groups.entries()).map(([category,amenities])=>`<section class="hotel-amenity-group" data-amenity-category="${escapeAttr(category.toLowerCase())}"><h5>${escapeHtml(category)}</h5><div>${amenities.map(amenity=>{const code=String(amenity.code||"").trim(),label=amenity.name_en||amenity.name_pl||code;return`<label><input type="checkbox" name="${escapeAttr(inputName)}" value="${escapeAttr(code)}" ${selected.has(code)?"checked":""} /><span>${escapeHtml(label)}</span></label>`}).join("")}</div></section>`).join(""),custom=Array.from(selected).filter(code=>!known.has(code));return`<div class="hotel-amenity-picker"><label class="admin-form-field"><span>Search amenities</span><input type="search" data-amenity-search placeholder="Search by name or category" /></label><div class="hotel-amenity-groups">${groupHtml||"<p>No active amenity catalogue entries.</p>"}</div>${custom.length?`<section class="hotel-amenity-group"><h5>Preserved custom values</h5><div>${custom.map(code=>`<label><input type="checkbox" name="${escapeAttr(inputName)}" value="${escapeAttr(code)}" checked /><span>${escapeHtml(code)}</span></label>`).join("")}</div></section>`:""}</div>`}function galleryEditorMarkup(room){const gallery=Core.normalizeGallery(room.gallery);return`<fieldset class="hotel-gallery-editor"><legend>Room gallery</legend>\n      ${gallery.length?`<div class="hotel-gallery-editor__grid">${gallery.map(url=>`<label><img src="${escapeAttr(url)}" alt="" loading="lazy" /><span><input type="checkbox" name="remove_gallery_url" value="${escapeAttr(url)}" /> Remove</span></label>`).join("")}</div>`:"<p>No room-specific images yet.</p>"}\n      <label class="admin-form-field"><span>Add room photos</span><input type="file" name="room_gallery_files" accept="image/*" multiple ${room.created_at?"":"disabled"} /></label>\n      <small>${room.created_at?"Images are optimized to WebP and stored under this exact Room Type ID.":"Create the Room Type first, then edit it to upload files to its exact ID."}</small>\n    </fieldset>`}function legacyPropertyPhotoSelectionMarkup(preview){const photos=Core.normalizeGallery(state.workspace?.property?.photos);return photos.length?`<fieldset class="hotel-legacy-photo-picker"><legend>Property photos requiring review</legend>\n      <p>These remain property-level media. Select a photo only after confirming that it belongs to this exact accommodation. Nothing is selected automatically.</p>\n      <div class="hotel-legacy-photo-picker__grid">${photos.map((url,index)=>`<label><img src="${escapeAttr(url)}" alt="Property photo candidate ${index+1}" loading="lazy" /><span><input type="checkbox" name="legacy_property_photo" value="${escapeAttr(url)}" /> Use for this Room Type</span><small>REQUIRES REVIEW</small></label>`).join("")}</div>\n      <small>${preview.property_gallery_count} source photo${1===preview.property_gallery_count?"":"s"} · the property gallery is not moved or changed.</small>\n    </fieldset>`:'<fieldset class="hotel-legacy-photo-picker"><legend>Property photos requiring review</legend><p>No property gallery photos are available. No Room Type photo will be copied.</p></fieldset>'}function sevenArchesConflictValue(value){return null==value?"Not specified":Array.isArray(value)?value.length?value.join(", "):"None":"object"==typeof value?JSON.stringify(value,null,2):String(value)}function openSevenArchesConflictReview({workspace:workspace,roomReviews:roomReviews,reconciliation:reconciliation}){const conflicts=Core.asArray(reconciliation?.conflicts),immutableConflict=conflicts.some(conflict=>!0===conflict?.identity_conflict||"legacy_pricing_fingerprint"===conflict?.field||"room_type"===conflict?.field),conflictRows=conflicts.map(conflict=>`<article class="hotel-seven-arches-conflict">\n      <header><strong>${escapeHtml(conflict.scope||"7 Arches")}</strong><span>${escapeHtml(conflict.label||conflict.field||"Reviewed field")}</span></header>\n      <div><section><small>Originally reviewed</small><pre>${escapeHtml(sevenArchesConflictValue(conflict.original))}</pre></section><section><small>Current</small><pre>${escapeHtml(sevenArchesConflictValue(conflict.current))}</pre></section><section><small>Requested</small><pre>${escapeHtml(sevenArchesConflictValue(conflict.target))}</pre></section></div>\n    </article>`).join(""),returnToPreparation=notice=>{closeModal({restoreFocus:!1,skipCleanup:!0,force:!0}),state.workspace=workspace,openSevenArchesPreparation({draftReviews:roomReviews,notice:notice})};openModal({title:"Review changed 7 Arches values",className:"hotel-workspace-modal--wide hotel-workspace-modal--review",body:`<section>\n        <div class="hotel-review-summary"><p>This configuration changed after the apartment preparation was opened. No database change has been submitted.</p></div>\n        <p class="hotel-workspace-safety-note">Compare the original, current and requested values. Keeping the current value leaves this shadow package unsaved. Using the reviewed value creates a new Review with the fresh exact versions; it never submits automatically.</p>\n        <div class="hotel-seven-arches-conflict-list">${conflictRows}</div>\n        ${immutableConflict?'<p class="hotel-workspace-card hotel-property-empty--error">An identity, existence or legacy-pricing conflict cannot be replaced by this preparation. Keep the current value or cancel and investigate it separately.</p>':""}\n      </section>`,footer:'<button class="btn-secondary" type="button" data-seven-arches-conflict-cancel>Cancel</button><button class="btn-secondary" type="button" data-seven-arches-keep-current>Keep current</button>'+(immutableConflict?"":'<button class="btn-primary" type="button" data-seven-arches-use-reviewed>Use reviewed value</button>'),onReady(overlay){overlay.setAttribute("data-seven-arches-conflict-review",""),overlay.querySelector("[data-seven-arches-conflict-cancel]")?.addEventListener("click",()=>{returnToPreparation("Conflict review cancelled. Your selected room photos were preserved; nothing was saved.")}),overlay.querySelector("[data-seven-arches-keep-current]")?.addEventListener("click",()=>{returnToPreparation("Current structural values were kept. Your selected room photos were preserved, but the canonical two-apartment shadow package remains unsaved.")}),overlay.querySelector("[data-seven-arches-use-reviewed]")?.addEventListener("click",async event=>{const button=event.currentTarget;button.disabled=!0,button.textContent="Preparing fresh Review…";try{const review=sevenArchesReviewConfiguration(workspace,roomReviews,{originalWorkspace:workspace,afterStaleConflict:!0});closeModal({restoreFocus:!1,skipCleanup:!0,force:!0}),await openReview(review)}catch(error){button.disabled=!1,button.textContent="Use reviewed value",toast(error?.userMessage||error?.message||"The fresh conflict Review could not be prepared.","error")}})}})}function sevenArchesReviewConfiguration(workspace,roomReviews,options={}){const preparation=Core.sevenArchesShadowPreparation(workspace),retained=function(roomReviews,workspace){const allowedPhotos=new Set(Core.normalizeGallery(workspace?.property?.photos));let removedPhotoCount=0;return{reviews:Core.asArray(roomReviews).map(review=>{const gallery=Core.normalizeGallery(review?.gallery),retainedGallery=gallery.filter(url=>allowedPhotos.has(url));return removedPhotoCount+=gallery.length-retainedGallery.length,{...Core.clone(review),gallery:retainedGallery}}),removedPhotoCount:removedPhotoCount}}(roomReviews,workspace);if(retained.removedPhotoCount){const error=new Error("The property gallery changed while this preparation was open.");throw error.userMessage=`${retained.removedPhotoCount} selected room photo reference${1===retained.removedPhotoCount?" is":"s are"} no longer present in the property gallery. Remaining selections were preserved; review the current gallery and select replacements before continuing.`,error.closeReviewAfterStale=!0,error.reopenSevenArchesPreparation={draftReviews:retained.reviews,notice:error.userMessage},error}const reconciliation=Core.sevenArchesShadowReconciliation(options.originalWorkspace||workspace,workspace,{roomReviews:retained.reviews});if(!reconciliation.eligible){const error=new Error(reconciliation.blockers.join(" "));throw error.userMessage=preparation.eligible&&reconciliation.conflicts?.length?"This preparation has a real concurrent value conflict. Compare the current and requested values before deciding; nothing was saved.":`Fresh workspace values cannot be safely applied: ${reconciliation.blockers.join(" ")} The old review was closed because this property is no longer eligible for the two-apartment preparation.`,error.closeReviewAfterStale=!0,preparation.eligible&&reconciliation.conflicts?.length?error.openSevenArchesConflictReview={workspace:workspace,roomReviews:retained.reviews,reconciliation:reconciliation}:preparation.eligible&&(error.reopenSevenArchesPreparation={draftReviews:retained.reviews,notice:error.userMessage}),error}const reviewedBusinessValues=Core.clone(retained.reviews),plan=Core.buildSevenArchesShadowPlan(workspace,reviewedBusinessValues),normalizedWorkspace=Core.normalizeWorkspace(workspace),before={id:preparation.hotel_id,architecture_version:workspace.property.architecture_version,property_children_policy:workspace.property.children_policy,property_minimum_child_age:workspace.property.minimum_child_age,room_types:preparation.rooms.map(preparedRoom=>{const room=normalizedWorkspace.room_types.find(candidate=>candidate.id===preparedRoom.id);return room?{id:room.id,version:room.version,code:room.code,name_i18n:room.name_i18n,gallery:room.gallery,status:room.status,max_occupancy:room.max_occupancy,capacity_adults:room.capacity_adults,capacity_children:room.capacity_children,inventory_mode:room.inventory_mode,base_inventory_count:room.base_inventory_count,amenities:room.amenities}:{id:preparedRoom.id,status:"Not created"}}),pricing:"Legacy 63-rule matrix remains authoritative",...reconciliation.changes.length?{business_values_changed_since_preparation:"None in the original preparation snapshot"}:{}},after={id:preparation.hotel_id,architecture_version:"legacy (unchanged)",property_children_policy:"minimum_age",property_minimum_child_age:10,rooms:plan.rooms.map(room=>({id:room.id,source_key:room.source_key,name_i18n:room.name_i18n,max_occupancy:room.max_occupancy,inventory:"pooled · 1",amenities:room.amenities,selected_photo_count:room.gallery.length,status:preparation.rooms.find(candidate=>candidate.id===room.id)?.status||"draft"})),pricing:"Dormant shared Standard schedule preview; cancellation/payment blocked",public_change:!1,...reconciliation.changes.length?{business_values_changed_since_preparation:reconciliation.changes}:{},...reconciliation.safe_rebases?.length?{safe_three_way_rebases:reconciliation.safe_rebases}:{}},isConflictReview=!0===options.afterStaleConflict;return{title:isConflictReview?"Review fresh 7 Arches two-apartment values":"Review 7 Arches two-apartment shadow package",entity:"seven_arches_shadow_package",before:before,after:after,onConfirm:()=>Repository.prepareLegacyShadowRooms(plan),onStaleReview:async()=>{const freshWorkspace=await Repository.getWorkspace(plan.hotel_id);state.workspace=freshWorkspace;try{return sevenArchesReviewConfiguration(freshWorkspace,reviewedBusinessValues,{afterStaleConflict:!0,originalWorkspace:workspace})}catch(error){throw error.closeReviewAfterStale=!0,error}},contextMessage:isConflictReview?"The stale save was stopped. Fresh workspace values are shown with your reviewed names and photo selections preserved. Nothing was retried automatically; review these values and click Save reviewed changes again to submit.":"Fresh workspace values were loaded immediately before this Review. One atomic exact-property RPC creates or updates only the deterministic shadow IDs. Existing legacy pricing, property gallery, public state, architecture, bookings and feature flags remain unchanged.",reReviewMessage:"This room was updated after this review was prepared. Current data has been refreshed and your selected photos were preserved. Please review the changes again before saving; nothing was retried automatically.",successMessage:"Two reviewed 7 Arches apartments prepared in Rooms V2 shadow configuration."}}function openSevenArchesPreparation(options={}){const preparationWorkspace=Core.clone(state.workspace),preparation=Core.sevenArchesShadowPreparation(preparationWorkspace);if(!preparation.eligible)return void toast(preparation.blocker||"The two-apartment preparation is not available.","error");const draftByRoom=new Map(Core.asArray(options.draftReviews).map(review=>[Core.normalizeUuid(review?.id),review])),currentPropertyPhotos=new Set(preparation.property_gallery),preparedRooms=preparation.rooms.map(room=>{const draft=draftByRoom.get(room.id);return draft?{...room,name_i18n:Core.normalizeI18n(draft.name_i18n||room.name_i18n),gallery:Core.normalizeGallery(draft.gallery).filter(url=>currentPropertyPhotos.has(url))}:room}),[upper,ground]=preparedRooms,roomMarkup=(room,index,locationFacts)=>`<section class="hotel-seven-arches-room" data-seven-arches-room="${escapeAttr(room.id)}">\n      <header><div><span class="hotel-workspace-eyebrow">Room ${index+1} · exact shadow Room Type</span><h4>${escapeHtml(Core.i18nText(room.name_i18n,"en",room.code))}</h4></div><code>${escapeHtml(room.id)}</code></header>\n      ${i18nFields(`seven_room_${index}_name`,"Editable room name",room.name_i18n)}\n      <div class="hotel-seven-arches-facts">\n        <div><span>Floor</span><strong>${escapeHtml(locationFacts.floor)}</strong></div>\n        <div><span>Terrace</span><strong>Yes</strong></div>\n        <div><span>Balcony</span><strong>${locationFacts.balcony?"Yes":"No"}</strong></div>\n        <div><span>Capacity</span><strong>Max 4 guests</strong><small>Adult/child split not confirmed</small></div>\n        <div><span>Inventory</span><strong>1 pooled apartment</strong></div>\n        <div><span>Beds · bathrooms · size</span><strong>Not confirmed</strong></div>\n      </div>\n      <div class="hotel-seven-arches-amenities"><span>Confirmed room amenities</span>${room.amenities.map(code=>`<b>${escapeHtml(code.replaceAll("_"," "))}</b>`).join("")}</div>\n      ${function(room,index,propertyPhotos){const selected=new Set(Core.normalizeGallery(room.gallery)),inputName=`seven_arches_room_${index}_photo`;return`<fieldset class="hotel-seven-arches-photos"><legend>Select room photos · required</legend>\n      <p>Every source image remains in the Property gallery. Checking an image adds a safe reference to this exact Room Type.</p>\n      <div class="hotel-legacy-photo-picker__grid">${propertyPhotos.map((url,photoIndex)=>`<label>\n        <img src="${escapeAttr(url)}" alt="7 Arches property photo ${photoIndex+1}" loading="lazy" />\n        <span><input type="checkbox" name="${inputName}" value="${escapeAttr(url)}" ${selected.has(url)?"checked":""} /> Use for ${escapeHtml(Core.i18nText(room.name_i18n,"en",room.code))}</span>\n      </label>`).join("")}</div>\n      <small>No photo is selected automatically. Shared photos may be selected for both apartments only when they genuinely apply to both.</small>\n    </fieldset>`}(room,index,preparation.property_gallery)}\n    </section>`;openModal({title:"Prepare 2 existing apartments",className:"hotel-workspace-modal--wide hotel-workspace-modal--seven-arches",body:`<form id="hotelSevenArchesPreparationForm" class="hotel-workspace-form">\n        ${options.notice?`<section class="hotel-workspace-card hotel-property-empty--error"><strong>Fresh review required</strong><p>${escapeHtml(options.notice)}</p></section>`:""}\n        <section class="hotel-legacy-source-review">\n          <span class="hotel-workspace-eyebrow">7 Arches · reviewed source contract</span>\n          <h4>Two real accommodation units</h4>\n          <div><span>Legacy pricing remains live</span><span>Property gallery: ${preparation.property_gallery.length} photos</span><span>Public: no change</span></div>\n          <p>The two normalized Room Types, dormant shared Standard pricing structure and age policy remain Rooms V2 shadow configuration. No architecture or feature flag is changed.</p>\n        </section>\n        <section class="hotel-workspace-card hotel-guest-policy-card"><span class="hotel-workspace-eyebrow">Children policy · source confirmed</span><h4>Children allowed from age 10</h4><p>Saved as shadow/Admin metadata. The current legacy booking form remains unchanged.</p></section>\n        <div class="hotel-seven-arches-room-grid">\n          ${roomMarkup(upper,0,{floor:"Upper floor",balcony:!0})}\n          ${roomMarkup(ground,1,{floor:"Ground floor",balcony:!1})}\n        </div>\n        <section class="hotel-workspace-card hotel-legacy-pricing-blocker">\n          <strong>Shared pricing preparation · inactive</strong>\n          <p>One reusable Standard Rate Plan structure is prepared for both apartments. The server copies the 27 confirmed 2–4 guest tiers into their dormant shared room schedule and preserves the complete 63-rule matrix as a separate inactive property-party preview for future multi-room allocation. Cancellation and payment terms remain BLOCKED for Review.</p>\n          <small>No public price, legacy pricing JSON, booking, deposit or coupon is changed.</small>\n        </section>\n      </form>`,footer:'<button class="btn-secondary" type="button" data-hotel-modal-close>Cancel</button><button class="btn-primary" type="submit" form="hotelSevenArchesPreparationForm">Review 2 apartments</button>',onReady(overlay){const form=overlay.querySelector("#hotelSevenArchesPreparationForm");form.addEventListener("submit",async event=>{event.preventDefault();const submitButton=overlay.querySelector('button[form="hotelSevenArchesPreparationForm"]'),fd=new FormData(form),reviews=preparedRooms.map((room,index)=>({id:room.id,name_i18n:readI18n(fd,`seven_room_${index}_name`),gallery:fd.getAll(`seven_arches_room_${index}_photo`)}));submitButton&&(submitButton.disabled=!0,submitButton.textContent="Loading fresh values…"),setModalSaving(overlay,!0);try{const freshWorkspace=await Repository.getWorkspace(preparation.hotel_id);state.workspace=freshWorkspace;const review=sevenArchesReviewConfiguration(freshWorkspace,reviews,{originalWorkspace:preparationWorkspace});closeModal({restoreFocus:!1,skipCleanup:!0,force:!0}),await openReview(review)}catch(error){if(setModalSaving(overlay,!1),error?.openSevenArchesConflictReview)return closeModal({restoreFocus:!1,skipCleanup:!0,force:!0}),openSevenArchesConflictReview(error.openSevenArchesConflictReview),void toast(error.userMessage||error.message,"warning");if(error?.reopenSevenArchesPreparation)return closeModal({restoreFocus:!1,skipCleanup:!0,force:!0}),openSevenArchesPreparation(error.reopenSevenArchesPreparation),void toast(error.userMessage||error.message,"error");if(!0===error?.closeReviewAfterStale)return closeModal({restoreFocus:!1,skipCleanup:!0,force:!0}),renderWorkspace(),void toast(error.userMessage||error.message,"error");submitButton&&(submitButton.disabled=!1,submitButton.textContent="Review 2 apartments"),toast(error?.userMessage||error?.message||"Fresh workspace values could not be prepared for review.","error")}})}})}function openLegacyAccommodationPreparation(preview=null){const freshPreview=Core.migrationPreview(state.workspace);freshPreview.can_prepare_existing_accommodation&&freshPreview.legacy_product?preview?.property_id&&preview.property_id!==freshPreview.property_id?toast("The reviewed legacy source no longer matches this property.","error"):openRoomEditor(null,{legacyPreparation:freshPreview}):toast("The current legacy product is no longer eligible for one-room shadow preparation. Refresh and review the latest workspace.","error")}function openRoomEditor(roomId=null,options={}){const existing=roomId?state.workspace.room_types.find(room=>room.id===roomId):null,legacyPreparation=existing?{}:Core.asObject(options.legacyPreparation),isLegacyPreparation="property_level_accommodation"===legacyPreparation.legacy_product?.kind,room=existing||(isLegacyPreparation?Core.buildLegacyShadowRoomSeed(state.workspace,Core.newUuid()):Core.normalizeRoomType({id:Core.newUuid(),hotel_id:state.workspace.property.id,code:"",name_i18n:{},description_i18n:{},gallery:[],capacity_adults:2,capacity_children:0,max_occupancy:null,bed_configuration:[],amenities:[],inventory_mode:"pooled",base_inventory_count:1,status:"draft",sort_order:1e3,version:1})),adultsValue=isLegacyPreparation?"":room.capacity_adults??"",childrenValue=isLegacyPreparation?"":room.capacity_children??"",totalValue=isLegacyPreparation?"":room.max_occupancy??"",capacityContract=null!=room.max_occupancy?"total_only":"split",inventoryValue=isLegacyPreparation?"":room.base_inventory_count;openModal({title:existing?"Edit Room Type":isLegacyPreparation?"Prepare existing accommodation as Room Type":"Add Room Type",className:"hotel-workspace-modal--wide",body:`<form id="hotelRoomEditorForm" class="hotel-workspace-form">\n        ${isLegacyPreparation?`<section class="hotel-legacy-source-review">\n          <span class="hotel-workspace-eyebrow">Legacy source · read only</span>\n          <h4>${escapeHtml(legacyPreparation.legacy_product.label)}</h4>\n          <div><span>${legacyPreparation.legacy_pricing_rule_count} legacy pricing rules</span><span>Legacy booking maximum ${legacyPreparation.legacy_product.max_persons||"not specified"}</span><span>${legacyPreparation.property_gallery_count} property photos</span></div>\n          <p>The source is a property-level accommodation product, not a normalized Room Type. Enter and confirm every room-specific value below. Pricing is not copied in this operation.</p>\n        </section>`:""}\n        <div class="hotel-workspace-form-grid"><label class="admin-form-field"><span>Internal code</span><input name="code" value="${escapeAttr(room.code)}" required pattern="[a-z0-9][a-z0-9_-]*" /></label><label class="admin-form-field"><span>Exact Room Type ID</span><input value="${escapeAttr(room.id)}" readonly /></label></div>\n        ${i18nFields("name","Room name",room.name_i18n)}\n        ${i18nFields("description","Room description",room.description_i18n,"textarea")}\n        <fieldset><legend>Capacity & inventory</legend><div class="hotel-workspace-form-grid">\n          <label class="admin-form-field"><span>Capacity detail</span><select name="capacity_contract"><option value="split" ${"split"===capacityContract?"selected":""}>Adults and children confirmed</option><option value="total_only" ${"total_only"===capacityContract?"selected":""}>Maximum total only · split not confirmed</option></select></label>\n          <label class="admin-form-field" data-capacity-split><span>Adults</span><input name="capacity_adults" type="number" min="1" max="50" step="1" value="${adultsValue}" /></label>\n          <label class="admin-form-field" data-capacity-split><span>Children</span><input name="capacity_children" type="number" min="0" max="50" step="1" value="${childrenValue}" /></label>\n          <label class="admin-form-field" data-capacity-total><span>Maximum total guests</span><input name="max_occupancy" type="number" min="1" max="50" step="1" value="${totalValue}" /><small>Use only when the adult/child split is genuinely not confirmed.</small></label>\n          <label class="admin-form-field"><span>Inventory model</span><select name="inventory_mode" required>${isLegacyPreparation?'<option value="" selected disabled>Select after confirmation</option>':""}<option value="pooled" ${isLegacyPreparation||"pooled"!==room.inventory_mode?"":"selected"}>Pooled inventory</option><option value="unitized" ${isLegacyPreparation||"unitized"!==room.inventory_mode?"":"selected"}>Individual units</option></select></label>\n          <label class="admin-form-field"><span>Base inventory count</span><input name="base_inventory_count" type="number" min="${isLegacyPreparation?"1":"0"}" step="1" value="${inventoryValue}" ${isLegacyPreparation?"required":""} /></label>\n          <label class="admin-form-field"><span>Bathrooms</span><input name="bathrooms" type="number" min="0" step="0.5" value="${escapeAttr(room.bathrooms??"")}" /></label>\n          <label class="admin-form-field"><span>Size m²</span><input name="size_sqm" type="number" min="0.01" step="0.01" value="${escapeAttr(room.size_sqm??"")}" /></label>\n          ${isLegacyPreparation?'<label class="admin-form-field"><span>Status</span><input name="status" value="draft" readonly /><small>Shadow preparation is always inert.</small></label>':`<label class="admin-form-field"><span>Status</span><select name="status">${Core.ROOM_STATUSES.map(status=>`<option value="${status}" ${status===room.status?"selected":""}>${escapeHtml(status)}</option>`).join("")}</select></label>`}\n          <label class="admin-form-field"><span>Admin sort order</span><input name="sort_order" type="number" min="0" step="1" value="${room.sort_order}" /></label>\n        </div><p class="hotel-inventory-mode-note" data-inventory-note></p></fieldset>\n        <fieldset><legend>Bed configuration</legend><div data-bed-rows>${room.bed_configuration.map(bedRowMarkup).join("")}</div><button class="btn-secondary" type="button" data-add-bed>+ Add bed</button></fieldset>\n        <fieldset><legend>Room amenities</legend>${isLegacyPreparation?"<p>Property amenities are not copied. Select only amenities confirmed for this exact accommodation.</p>":""}${amenitiesMarkup(room.amenities)}</fieldset>\n        ${galleryEditorMarkup(room)}\n        ${isLegacyPreparation?legacyPropertyPhotoSelectionMarkup(legacyPreparation):""}\n        ${isLegacyPreparation?'<p class="hotel-workspace-safety-note">This operation creates one draft Room Type only. It does not create a Rate Plan, Room Rate, Calendar row, booking, or pricing conversion.</p>':""}\n      </form>`,footer:`<button class="btn-secondary" type="button" data-hotel-modal-close>Cancel</button><button class="btn-primary" type="submit" form="hotelRoomEditorForm">Review ${existing?"changes":isLegacyPreparation?"shadow Room Type":"new Room Type"}</button>`,onReady(overlay){const form=overlay.querySelector("#hotelRoomEditorForm"),beds=form.querySelector("[data-bed-rows]"),syncBedRows=()=>beds.querySelectorAll(".hotel-bed-row").forEach(row=>{row.classList.toggle("is-other","other"===row.querySelector("[data-bed-type]")?.value)});beds.addEventListener("change",syncBedRows),beds.addEventListener("click",event=>{const remove=event.target.closest("[data-remove-bed]");remove&&remove.closest(".hotel-bed-row")?.remove()}),form.querySelector("[data-add-bed]")?.addEventListener("click",()=>{beds.insertAdjacentHTML("beforeend",bedRowMarkup({type:"double",quantity:1})),syncBedRows()});const inventoryMode=form.elements.inventory_mode,inventoryCount=form.elements.base_inventory_count,inventoryNote=form.querySelector("[data-inventory-note]"),syncInventory=()=>{if(!inventoryMode.value)return inventoryCount.disabled=!1,void(inventoryNote.textContent="Select and confirm the inventory model. No legacy inventory value is assumed.");const unitized="unitized"===inventoryMode.value;inventoryCount.disabled=unitized,inventoryNote.textContent=unitized?"Inventory is derived from active physical units. This switch never creates or deletes units.":"Pooled inventory uses the configured base count; physical units are optional and not required."};inventoryMode.addEventListener("change",syncInventory),syncInventory();const capacityContractInput=form.elements.capacity_contract,syncCapacityContract=()=>{const totalOnly="total_only"===capacityContractInput.value;form.querySelectorAll("[data-capacity-split]").forEach(label=>{label.hidden=totalOnly}),form.querySelector("[data-capacity-total]").hidden=!totalOnly,form.elements.capacity_adults.disabled=totalOnly,form.elements.capacity_children.disabled=totalOnly,form.elements.max_occupancy.disabled=!totalOnly,form.elements.capacity_adults.required=!totalOnly,form.elements.capacity_children.required=!totalOnly,form.elements.max_occupancy.required=totalOnly};capacityContractInput.addEventListener("change",syncCapacityContract),syncCapacityContract();const search=form.querySelector("[data-amenity-search]");search?.addEventListener("input",()=>{const term=search.value.trim().toLowerCase();form.querySelectorAll(".hotel-amenity-group").forEach(group=>{group.hidden=term&&!group.textContent.toLowerCase().includes(term)})}),syncBedRows(),form.addEventListener("submit",async event=>{event.preventDefault();const fd=new FormData(form),bedConfiguration=Array.from(form.querySelectorAll(".hotel-bed-row")).map(row=>{const type=row.querySelector("[data-bed-type]").value,quantity=Number(row.querySelector("[data-bed-quantity]").value),label=row.querySelector("[data-bed-label]").value.trim();return{type:type,quantity:quantity,..."other"===type&&label?{label:{en:label}}:{}}}),candidate={...room,code:String(fd.get("code")||"").trim().toLowerCase(),name_i18n:readI18n(fd,"name"),description_i18n:readI18n(fd,"description"),capacity_adults:"total_only"===fd.get("capacity_contract")?null:Number(fd.get("capacity_adults")),capacity_children:"total_only"===fd.get("capacity_contract")?null:Number(fd.get("capacity_children")),max_occupancy:"total_only"===fd.get("capacity_contract")?Number(fd.get("max_occupancy")):null,inventory_mode:String(fd.get("inventory_mode")),base_inventory_count:"unitized"===fd.get("inventory_mode")?0:Number(fd.get("base_inventory_count")),bathrooms:""===fd.get("bathrooms")?null:Number(fd.get("bathrooms")),size_sqm:""===fd.get("size_sqm")?null:Number(fd.get("size_sqm")),status:isLegacyPreparation?"draft":String(fd.get("status")),sort_order:Number(fd.get("sort_order")),bed_configuration:bedConfiguration,amenities:fd.getAll("room_amenity"),gallery:Core.normalizeGallery([...room.gallery.filter(url=>!fd.getAll("remove_gallery_url").includes(url)),...isLegacyPreparation?fd.getAll("legacy_property_photo"):[]])};let validated;try{validated=Core.validateRoomType(candidate,state.workspace)}catch(error){return void toast(error.message,"error")}const files=Array.from(form.elements.room_gallery_files?.files||[]);closeModal({restoreFocus:!1});let uploadedUrls=[];if(files.length){const uploader=window.HotelsV2AdminMedia?.uploadRoomGallery;if("function"!=typeof uploader)return void toast("Optimized room-image uploader is unavailable.","error");try{uploadedUrls=await uploader(state.workspace.property.slug,validated.id,files),validated={...validated,gallery:Core.normalizeGallery([...validated.gallery,...uploadedUrls])}}catch(error){return void toast(error?.message||"Room gallery upload failed before Review.","error")}}const cleanupUploaded=async()=>{uploadedUrls.length&&(await(window.HotelsV2AdminMedia?.removeRoomGalleryUploads?.(uploadedUrls)),uploadedUrls=[])};await openReview({title:existing?"Review Room Type changes":isLegacyPreparation?"Review legacy → shadow Room Type":"Review new Room Type",entity:"room_type",before:existing,after:validated,operation:Core.operationForEntity("room_type",validated,existing),onCancel:files.length?cleanupUploaded:null,onApplyError:files.length?async error=>{error?.isDefinitiveFailure&&await cleanupUploaded()}:null,closeOnApplyError:files.length>0,contextMessage:isLegacyPreparation?"The exact legacy property remains live and unchanged. This reviewed operation creates one draft Room Type only; pricing migration stays separate.":"",successMessage:existing?"Room Type updated.":isLegacyPreparation?"Existing accommodation prepared as one inert draft Room Type.":"Room Type created as an inert configuration."})})}})}function openUnitEditor(unitId=null,roomTypeId=null){const existing=unitId?state.workspace.units.find(unit=>unit.id===unitId):null,parentId=existing?.room_type_id||Core.normalizeUuid(roomTypeId),room=state.workspace.room_types.find(candidate=>candidate.id===parentId);if(!room||"unitized"!==room.inventory_mode)return void toast("Physical units can be managed only for a unitized Room Type.","error");const unit=existing||Core.normalizeUnit({id:Core.newUuid(),room_type_id:room.id,code:"",name_i18n:{},status:"active",version:1});openModal({title:existing?"Edit physical unit":`Add unit to ${Core.i18nText(room.name_i18n,"en",room.code)}`,body:`<form id="hotelUnitEditorForm" class="hotel-workspace-form">\n        <div class="hotel-workspace-form-grid"><label class="admin-form-field"><span>Unit code</span><input name="code" value="${escapeAttr(unit.code)}" required pattern="[a-z0-9][a-z0-9_-]*" /></label><label class="admin-form-field"><span>Status</span><select name="status">${Core.UNIT_STATUSES.map(status=>`<option value="${status}" ${unit.status===status?"selected":""}>${escapeHtml(status)}</option>`).join("")}</select></label></div>\n        ${i18nFields("name","Optional display name",unit.name_i18n)}\n        <div class="hotel-workspace-locked-fields"><div><span>Room Type</span><strong>${escapeHtml(Core.i18nText(room.name_i18n,"en",room.code))}</strong></div><div><span>Exact unit ID</span><code>${escapeHtml(unit.id)}</code></div></div>\n      </form>`,footer:'<button class="btn-secondary" type="button" data-hotel-modal-close>Cancel</button><button class="btn-primary" type="submit" form="hotelUnitEditorForm">Review unit</button>',onReady(overlay){overlay.querySelector("#hotelUnitEditorForm")?.addEventListener("submit",async event=>{event.preventDefault();const fd=new FormData(event.currentTarget);let validated;try{validated=Core.validateUnit({...unit,code:String(fd.get("code")||"").trim().toLowerCase(),name_i18n:readI18n(fd,"name"),status:String(fd.get("status"))},state.workspace)}catch(error){return void toast(error.message,"error")}closeModal({restoreFocus:!1}),await openReview({title:existing?"Review unit changes":"Review new physical unit",entity:"unit",before:existing,after:validated,operation:Core.operationForEntity("unit",validated,existing),successMessage:existing?"Physical unit updated.":"Physical unit created."})})}})}function cancellationFields(policy){const normalized=Core.normalizeCancellationPolicy(policy);return`<fieldset><legend>Cancellation policy</legend><div class="hotel-workspace-form-grid">\n      <label class="admin-form-field"><span>Policy</span><select name="cancellation_type">${"requires_review"===normalized.type?'<option value="requires_review" selected disabled>Requires confirmation</option>':""}<option value="flexible" ${"flexible"===normalized.type?"selected":""}>Flexible</option><option value="non_refundable" ${"non_refundable"===normalized.type?"selected":""}>Non-refundable</option><option value="custom" ${"custom"===normalized.type?"selected":""}>Custom</option></select></label>\n      <label class="admin-form-field" data-custom-cancellation><span>Deadline before arrival (hours)</span><input name="deadline_hours" type="number" min="0" step="1" value="${normalized.deadline_hours??48}" /></label>\n      <label class="admin-form-field" data-custom-cancellation><span>Penalty</span><select name="penalty_mode"><option value="none" ${"none"===normalized.penalty_mode?"selected":""}>No configured penalty</option><option value="flat" ${"flat"===normalized.penalty_mode?"selected":""}>Fixed amount</option><option value="percent" ${"percent"===normalized.penalty_mode?"selected":""}>Percent of total</option></select></label>\n      <label class="admin-form-field" data-custom-cancellation data-penalty-value><span>Penalty value</span><input name="penalty_value" type="number" min="0" step="0.01" value="${escapeAttr(normalized.penalty_value??"")}" /></label>\n    </div>${"requires_review"===normalized.type?'<p class="hotel-workspace-safety-note">Cancellation terms are unresolved. This Rate Plan must remain inactive until an Admin selects and reviews a confirmed policy.</p>':""}<small>This stores a safe policy description only. H2A does not change booking or refund calculations.</small></fieldset>`}function readCancellationPolicy(form,currentPolicy=null){const type=String(form.elements.cancellation_type.value||"flexible");if("requires_review"===type)return Core.normalizeCancellationPolicy(currentPolicy);if("custom"!==type)return Core.normalizeCancellationPolicy({type:type});const mode=String(form.elements.penalty_mode.value||"none");return Core.normalizeCancellationPolicy({type:type,deadline_hours:Number(form.elements.deadline_hours.value||0),penalty_mode:mode,..."none"===mode?{}:{penalty_value:Number(form.elements.penalty_value.value)}})}function openRatePlanEditor(planId=null){const existing=planId?state.workspace.rate_plans.find(plan=>plan.id===planId):null,plan=existing||Core.normalizeRatePlan({id:Core.newUuid(),hotel_id:state.workspace.property.id,code:"",name_i18n:{},description_i18n:{},cancellation_policy:{type:"flexible"},booking_mode_override:null,meal_plan_code:null,is_active:!1,sort_order:1e3,version:1});openModal({title:existing?"Edit Rate Plan":"Add Rate Plan",className:"hotel-workspace-modal--wide",body:`<form id="hotelRatePlanEditorForm" class="hotel-workspace-form">\n        <div class="hotel-workspace-form-grid"><label class="admin-form-field"><span>Internal code</span><input name="code" value="${escapeAttr(plan.code)}" required pattern="[a-z0-9][a-z0-9_-]*" /></label><label class="admin-form-field"><span>Meal plan code</span><input name="meal_plan_code" value="${escapeAttr(plan.meal_plan_code||"")}" placeholder="room_only, breakfast…" /></label></div>\n        ${i18nFields("name","Rate Plan name",plan.name_i18n)}\n        ${i18nFields("description","Rate Plan description",plan.description_i18n,"textarea")}\n        ${cancellationFields(plan.cancellation_policy)}\n        <div class="hotel-workspace-form-grid"><label class="admin-form-field"><span>Booking-mode override</span><select name="booking_mode_override"><option value="">Use property booking mode</option>${Core.BOOKING_MODES.map(mode=>`<option value="${mode}" ${plan.booking_mode_override===mode?"selected":""}>${escapeHtml(bookingModeLabel(mode))}</option>`).join("")}</select></label><label class="admin-form-field"><span>Admin sort order</span><input name="sort_order" type="number" min="0" step="1" value="${plan.sort_order}" /></label><label class="admin-checkbox-field"><input name="is_active" type="checkbox" ${plan.is_active?"checked":""} /><span>Active configuration</span></label></div>\n        <div class="hotel-workspace-locked-fields"><div><span>Exact Rate Plan ID</span><code>${escapeHtml(plan.id)}</code></div><div><span>Scope</span><strong>This property only</strong></div></div>\n      </form>`,footer:'<button class="btn-secondary" type="button" data-hotel-modal-close>Cancel</button><button class="btn-primary" type="submit" form="hotelRatePlanEditorForm">Review Rate Plan</button>',onReady(overlay){const form=overlay.querySelector("#hotelRatePlanEditorForm"),syncCancellation=()=>{const custom="custom"===form.elements.cancellation_type.value;form.querySelectorAll("[data-custom-cancellation]").forEach(element=>{element.hidden=!custom});const needsValue=custom&&"none"!==form.elements.penalty_mode.value;form.querySelector("[data-penalty-value]").hidden=!needsValue};form.elements.cancellation_type.addEventListener("change",syncCancellation),form.elements.penalty_mode.addEventListener("change",syncCancellation),syncCancellation(),form.addEventListener("submit",async event=>{event.preventDefault();const fd=new FormData(form);let validated;try{validated=Core.validateRatePlan({...plan,code:String(fd.get("code")||"").trim().toLowerCase(),name_i18n:readI18n(fd,"name"),description_i18n:readI18n(fd,"description"),meal_plan_code:String(fd.get("meal_plan_code")||"").trim().toLowerCase()||null,cancellation_policy:readCancellationPolicy(form,plan.cancellation_policy),booking_mode_override:String(fd.get("booking_mode_override")||"")||null,is_active:"on"===fd.get("is_active"),sort_order:Number(fd.get("sort_order"))},state.workspace)}catch(error){return void toast(error.message,"error")}closeModal({restoreFocus:!1}),await openReview({title:existing?"Review Rate Plan changes":"Review new Rate Plan",entity:"rate_plan",before:existing,after:validated,operation:Core.operationForEntity("rate_plan",validated,existing),successMessage:existing?"Rate Plan updated.":"Rate Plan created."})})}})}function openRoomRateEditor(rateId=null,preferredRoomId=null){const existing=rateId?state.workspace.room_rates.find(rate=>rate.id===rateId):null,rate=existing||Core.normalizeRoomRate({id:Core.newUuid(),hotel_id:state.workspace.property.id,room_type_id:Core.normalizeUuid(preferredRoomId)||state.workspace.room_types[0]?.id,rate_plan_id:state.workspace.rate_plans[0]?.id,base_nightly_rate:null,currency:state.workspace.property.currency||"EUR",is_active:!1,sort_order:1e3,version:1});if(state.workspace.room_types.length&&state.workspace.rate_plans.length){if(existing?.pricing_schedule_id){const schedule=state.workspace.pricing_schedules.find(candidate=>candidate.id===existing.pricing_schedule_id),tierCount=state.workspace.pricing_schedule_tiers.filter(tier=>tier.schedule_id===existing.pricing_schedule_id&&!1!==tier.is_active).length;return void openModal({title:"Shared Room Rate schedule",body:`<section class="hotel-workspace-card hotel-legacy-pricing-blocker">\n          <span class="hotel-workspace-eyebrow">Dormant Rooms V2 shadow pricing</span>\n          <h4>${escapeHtml(Core.i18nText(schedule?.name_i18n,"en","Shared apartment pricing"))}</h4>\n          <p>This exact product uses a reusable ${tierCount}-tier occupancy × length-of-stay schedule. Its base rate is not an executable €0 price.</p>\n          <p>Generic Room Rate editing is locked until H3 adds allocation-aware detach/clone and authoritative public resolution.</p>\n          <div class="hotel-workspace-locked-fields"><div><span>Exact product ID</span><code>${escapeHtml(existing.id)}</code></div><div><span>Schedule ID</span><code>${escapeHtml(existing.pricing_schedule_id)}</code></div></div>\n        </section>`})}openModal({title:existing?"Edit Room Rate product":"Connect Room Type + Rate Plan",body:`<form id="hotelRoomRateEditorForm" class="hotel-workspace-form"><div class="hotel-workspace-form-grid">\n        <label class="admin-form-field"><span>Room Type</span><select name="room_type_id" ${existing?"disabled":""}>${state.workspace.room_types.map(room=>`<option value="${room.id}" ${rate.room_type_id===room.id?"selected":""}>${escapeHtml(Core.i18nText(room.name_i18n,"en",room.code))}</option>`).join("")}</select></label>\n        <label class="admin-form-field"><span>Rate Plan</span><select name="rate_plan_id" ${existing?"disabled":""}>${state.workspace.rate_plans.map(plan=>`<option value="${plan.id}" ${rate.rate_plan_id===plan.id?"selected":""}>${escapeHtml(Core.i18nText(plan.name_i18n,"en",plan.code))}</option>`).join("")}</select></label>\n        <label class="admin-form-field"><span>Base nightly rate</span><input name="base_nightly_rate" type="number" min="0" step="0.01" value="${escapeAttr(rate.base_nightly_rate??"")}" required /></label>\n        <label class="admin-form-field"><span>Currency</span><input name="currency" maxlength="3" value="${escapeAttr(rate.currency)}" required /></label>\n        <label class="admin-form-field"><span>Admin sort order</span><input name="sort_order" type="number" min="0" step="1" value="${rate.sort_order}" /></label>\n        <label class="admin-checkbox-field"><input name="is_active" type="checkbox" ${rate.is_active?"checked":""} /><span>Active sellable configuration</span></label>\n      </div><div class="hotel-workspace-locked-fields"><div><span>Exact product ID</span><code>${escapeHtml(rate.id)}</code></div><div><span>Public effect</span><strong>None while V2 flags are off</strong></div></div></form>`,footer:'<button class="btn-secondary" type="button" data-hotel-modal-close>Cancel</button><button class="btn-primary" type="submit" form="hotelRoomRateEditorForm">Review product</button>',onReady(overlay){overlay.querySelector("#hotelRoomRateEditorForm")?.addEventListener("submit",async event=>{event.preventDefault();const form=event.currentTarget,fd=new FormData(form);let validated;try{validated=Core.validateRoomRate({...rate,room_type_id:existing?rate.room_type_id:String(fd.get("room_type_id")),rate_plan_id:existing?rate.rate_plan_id:String(fd.get("rate_plan_id")),base_nightly_rate:Number(fd.get("base_nightly_rate")),currency:String(fd.get("currency")||"").trim().toUpperCase(),is_active:"on"===fd.get("is_active"),sort_order:Number(fd.get("sort_order"))},state.workspace)}catch(error){return void toast(error.message,"error")}closeModal({restoreFocus:!1}),await openReview({title:existing?"Review Room Rate changes":"Review Room + Rate Plan product",entity:"room_rate",before:existing,after:validated,operation:Core.operationForEntity("room_rate",validated,existing),successMessage:existing?"Room Rate product updated.":"Room Rate product created."})})}})}else toast("Create at least one Room Type and one Rate Plan before connecting them.","error")}function calendarProducts(calendar=state.calendar.data){const data=Core.asObject(calendar),roomById=new Map(state.workspace.room_types.map(room=>[room.id,room])),planById=new Map(state.workspace.rate_plans.map(plan=>[plan.id,plan]));return Core.asArray(data.room_rates).map(raw=>{const rate=Core.asObject(raw),room=roomById.get(rate.room_type_id)||Core.asObject(rate.room_type),plan=planById.get(rate.rate_plan_id)||Core.asObject(rate.rate_plan),schedule=rate.pricing_schedule_id?state.workspace.pricing_schedules.find(candidate=>candidate.id===rate.pricing_schedule_id):null,scheduleTierCount=schedule?state.workspace.pricing_schedule_tiers.filter(tier=>tier.schedule_id===schedule.id&&!1!==tier.is_active).length:0;return{...rate,id:Core.normalizeUuid(rate.id),room_type_id:Core.normalizeUuid(rate.room_type_id||room.id),room_name:Core.i18nText(rate.room_name_i18n||room.name_i18n,"en",room.code||"Room Type"),room_code:String(room.code||rate.room_code||"").trim(),room_version:Number(room.version||rate.room_version||0)||null,base_inventory_count:Number(room.base_inventory_count??rate.base_inventory_count??0),rate_plan_name:Core.i18nText(rate.rate_plan_name_i18n||plan.name_i18n,"en",plan.code||"Rate Plan"),rate_plan_code:String(plan.code||rate.rate_plan_code||"").trim(),currency:String(rate.currency||state.workspace.property.currency||"EUR"),pricing_label:schedule?`Shared ${scheduleTierCount}-tier shadow schedule`:`${formatMoney(rate.base_nightly_rate,rate.currency||state.workspace.property.currency||"EUR")} base`}}).filter(rate=>rate.id&&rate.room_type_id)}function calendarBlockingReasonLabel(reason){const source="string"==typeof reason?{code:reason}:Core.asObject(reason),code=String(source.code||source.reason||"not_requestable").trim();return{closed_to_departure:source.stay_date?`Checkout ${source.stay_date}: closed to departure`:"Closed to departure on checkout date",closed_to_arrival:"Closed to arrival",insufficient_or_closed_inventory:"Inventory unavailable or closed",missing_occupancy_los_tier:"No matching occupancy / stay tier",ambiguous_range_rules:"Ambiguous seasonal rules",ambiguous_weekday_rules:"Ambiguous weekday rules",invalid_resolved_stay_restriction:"Invalid stay restriction",shared_room_pricing_schedule_requires_h3_resolution:"H3 occupancy/allocation resolution required"}[code]||code.replaceAll("_"," ")}function calendarCellMarkup(product,date,maps){const cell=function(product,date,maps){const rateRow=maps.dailyRates.get(`${product.id}:${date}`)||null,inventoryRow=maps.inventory.get(`${product.room_type_id}:${date}`)||null,overrideRow=maps.overrides.get(`${product.id}:${date}`)||null,serverCell=maps.effective.get(`${product.id}:${date}`)||null,sharedSchedule=Core.sharedScheduleCalendarDisplayState(product,date,overrideRow,inventoryRow);if(sharedSchedule)return{rate:null,inventory:sharedSchedule.configured_inventory,closed:sharedSchedule.explicitly_closed,minimumStay:"set"===sharedSchedule.minimum_stay.mode?sharedSchedule.minimum_stay.value:null,maximumStay:"set"===sharedSchedule.maximum_stay.mode?sharedSchedule.maximum_stay.value:null,cta:"set"===sharedSchedule.closed_to_arrival.mode&&!0===sharedSchedule.closed_to_arrival.value,ctd:"set"===sharedSchedule.closed_to_departure.mode&&!0===sharedSchedule.closed_to_departure.value,source:"Shadow only · H3 occupancy/allocation pending",requestable:!1,blockingReasons:[sharedSchedule.blocker],rateRow:rateRow,inventoryRow:inventoryRow,overrideRow:overrideRow,serverCell:serverCell,unresolved:!0,sharedSchedule:sharedSchedule};if(!serverCell)return{rate:null,inventory:null,closed:!0,minimumStay:null,maximumStay:null,cta:!1,ctd:!1,source:"Unresolved — reload required",requestable:null,blockingReasons:[],rateRow:rateRow,inventoryRow:inventoryRow,overrideRow:overrideRow,serverCell:null,unresolved:!0};const resolved=Core.asObject(serverCell?.resolved),resolvedNight=Core.asObject(Core.asArray(resolved.nightly_breakdown)[0]),rate=resolvedNight.nightly_rate??serverCell.nightly_rate??serverCell.effective_nightly_rate??null,inventory=resolvedNight.sellable_units??serverCell.sellable_units??serverCell.effective_sellable_units??null,closed=Boolean(resolvedNight.closed??serverCell.closed??serverCell.effective_closed??!0),minimumStay=resolvedNight.minimum_stay??serverCell.minimum_stay??null,maximumStay=resolvedNight.maximum_stay??serverCell.maximum_stay??null,cta=Boolean(resolvedNight.closed_to_arrival??serverCell.closed_to_arrival??!1),ctd=Boolean(resolvedNight.closed_to_departure??serverCell.closed_to_departure??!1),provenance=Core.asObject(resolvedNight.provenance||serverCell.provenance),blockingReasons=Core.asArray(serverCell.blocking_reasons||resolved.blocking_reasons),requestableValue=serverCell.requestable??resolved.requestable??resolved.bookable,requestable="boolean"==typeof requestableValue?requestableValue:!blockingReasons.length&&null;return{rate:rate,inventory:inventory,closed:closed,minimumStay:minimumStay,maximumStay:maximumStay,cta:cta,ctd:ctd,source:String(resolvedNight.source||serverCell.source_label||serverCell.source||provenance.source||blockingReasons[0]?.code||blockingReasons[0]||"Resolved"),requestable:requestable,blockingReasons:blockingReasons,rateRow:rateRow,inventoryRow:inventoryRow,overrideRow:overrideRow,serverCell:serverCell,unresolved:null==rate||null==inventory}}(product,date,maps),restrictions=[cell.minimumStay?`Min ${cell.minimumStay}`:"",cell.maximumStay?`Max ${cell.maximumStay}`:"",cell.cta?"CTA":"",cell.ctd?"CTD":""].filter(Boolean),selected=function(date){return Boolean(state.calendar.selection_start&&state.calendar.selection_end&&date>=state.calendar.selection_start&&date<=state.calendar.selection_end)}(date)&&state.calendar.selected_product_ids.includes(product.id);if(cell.sharedSchedule){const shadow=cell.sharedSchedule,nightlyRate="set"===shadow.nightly_rate.mode?`Exact date draft rate: ${formatMoney(shadow.nightly_rate.value,product.currency)}`:"clear"===shadow.nightly_rate.mode?"Exact date rate cleared · shared schedule inherited":"No exact date price override",inventory="exact_room_date"===shadow.inventory_source?`Exact room inventory: ${shadow.configured_inventory} room${1===Number(shadow.configured_inventory)?"":"s"}`:`${"clear"===shadow.sellable_units.mode?"Daily inventory cleared · ":""}Base room inventory: ${shadow.configured_inventory}`,shadowRestrictions=[shadow.explicitly_closed?"Closed":"",...restrictions].filter(Boolean);return`<button type="button" class="hotel-calendar-cell is-shadow-schedule${selected?" is-selected":""}${shadow.explicitly_closed?" is-closed":""} is-not-requestable" data-calendar-cell data-product-id="${escapeAttr(product.id)}" data-date="${escapeAttr(date)}" aria-pressed="${selected}" title="${escapeAttr(`${product.room_name} · ${product.rate_plan_name} · ${date}`)}">\n        <strong>Shared schedule · H3 pending</strong>\n        <span>${escapeHtml(nightlyRate)}</span>\n        <span>${escapeHtml(inventory)}</span>\n        <small>${shadowRestrictions.length?escapeHtml(shadowRestrictions.join(" · ")):"No exact restrictions"}</small>\n        <em class="hotel-calendar-cell__blocker">Shadow only · not requestable until occupancy/allocation is resolved</em>\n        <i>Exact room configuration</i>\n      </button>`}const blockerLabels=cell.blockingReasons.map(calendarBlockingReasonLabel),requestability=cell.unresolved||!1!==cell.requestable?"":`<em class="hotel-calendar-cell__blocker">Not requestable${blockerLabels.length?` · ${escapeHtml(blockerLabels.join(" · "))}`:""}</em>`;return`<button type="button" class="hotel-calendar-cell${selected?" is-selected":""}${cell.closed?" is-closed":""}${!1===cell.requestable?" is-not-requestable":""}" data-calendar-cell data-product-id="${escapeAttr(product.id)}" data-date="${escapeAttr(date)}" aria-pressed="${selected}" title="${escapeAttr(`${product.room_name} · ${product.rate_plan_name} · ${date}`)}">\n      <strong>${cell.unresolved?"Not resolved":cell.closed?"Closed":escapeHtml(formatMoney(cell.rate,product.currency))}</strong>\n      <span>${null==cell.inventory?"Inventory unavailable":`${cell.inventory} room${1===Number(cell.inventory)?"":"s"}`}</span>\n      ${restrictions.length?`<small>${escapeHtml(restrictions.join(" · "))}</small>`:"<small>No restrictions</small>"}\n      ${requestability}\n      <i>${escapeHtml(cell.source)}</i>\n    </button>`}function calendarMonthTitle(range){const start=parseIsoDate(range.start),end=parseIsoDate(range.end);if(!start||!end)return`${range.start} – ${range.end}`;if("week"===state.calendar.view)return`${calendarDateLabel(range.start,{long:!0})} – ${calendarDateLabel(range.end,{long:!0})}`;if("two_months"===state.calendar.view){const format=new Intl.DateTimeFormat("en-GB",{month:"long",year:"numeric",timeZone:"UTC"});return`${format.format(start)} – ${format.format(end)}`}return new Intl.DateTimeFormat("en-GB",{month:"long",year:"numeric",timeZone:"UTC"}).format(start)}function renderCalendarLoaded(panel,range,data){const products=calendarProducts(data),dates=enumerateCalendarDates(range.start,range.end),maps=function(calendar=state.calendar.data){const data=Core.asObject(calendar),dailyRates=new Map,inventory=new Map,overrides=new Map,effective=new Map;return Core.asArray(data.daily_rates).forEach(row=>dailyRates.set(`${row.room_rate_id}:${row.stay_date}`,row)),Core.asArray(data.daily_inventory).forEach(row=>inventory.set(`${row.room_type_id}:${row.stay_date}`,row)),Core.asArray(data.calendar_overrides).forEach(row=>overrides.set(`${row.room_rate_id}:${row.stay_date}`,row)),Core.asArray(data.effective_cells).forEach(row=>{const productId=row.room_rate_id||row.product_id,date=row.stay_date||row.date;productId&&date&&effective.set(`${productId}:${date}`,row)}),{dailyRates:dailyRates,inventory:inventory,overrides:overrides,effective:effective}}(data),selectedCount=state.calendar.selected_product_ids.length,selectionLabel=state.calendar.selection_start?`${state.calendar.selection_start}${state.calendar.selection_end!==state.calendar.selection_start?` → ${state.calendar.selection_end}`:""}`:"Select one date or a range";if(!products.length)return panel.innerHTML=`${workspacePanelHeader("Calendar & Rates","Manual prices, inventory and restrictions for normalized Room × Rate Plan products.")}\n        <section class="hotel-workspace-card hotel-placeholder-card"><span class="hotel-workspace-eyebrow">Rooms V2 shadow</span><h4>No Room Rate products to calendar yet</h4><p>The current legacy property and its public prices remain unchanged. Prepare a normalized Room Type, Rate Plan and Room Rate product first.</p><button class="btn-primary" type="button" data-calendar-open-rooms>Open Rooms & Rates</button></section>`,void panel.querySelector("[data-calendar-open-rooms]")?.addEventListener("click",()=>{state.activeTab="rooms",renderWorkspace()});state.calendar.mobile_product_id&&products.some(product=>product.id===state.calendar.mobile_product_id)||(state.calendar.mobile_product_id=products[0].id),panel.innerHTML=`${workspacePanelHeader("Calendar & Rates","Select exact products and dates, then Review one transactional manual update.")}\n      <section class="hotel-calendar-toolbar hotel-workspace-card">\n        <div class="hotel-calendar-toolbar__navigation"><button class="btn-secondary" type="button" data-calendar-shift="-1" aria-label="Previous ${state.calendar.view}">←</button><button class="btn-secondary" type="button" data-calendar-today>Today</button><button class="btn-secondary" type="button" data-calendar-shift="1" aria-label="Next ${state.calendar.view}">→</button><strong>${escapeHtml(calendarMonthTitle(range))}</strong></div>\n        <div class="hotel-calendar-toolbar__actions"><label><span>View</span><select data-calendar-view><option value="month" ${"month"===state.calendar.view?"selected":""}>Month</option><option value="two_months" ${"two_months"===state.calendar.view?"selected":""}>2 months</option><option value="week" ${"week"===state.calendar.view?"selected":""}>Week</option></select></label><button class="btn-secondary" type="button" data-calendar-select-all>${selectedCount===products.length?"Clear products":"Select all products"}</button><button class="btn-primary" type="button" data-calendar-edit-range ${selectedCount&&state.calendar.selection_start?"":"disabled"}>Edit selected range</button></div>\n        <div class="hotel-calendar-selection" aria-live="polite"><strong>${selectedCount} product${1===selectedCount?"":"s"} selected</strong><span>${escapeHtml(selectionLabel)}</span><button type="button" data-calendar-clear-selection ${state.calendar.selection_start?"":"disabled"}>Clear dates</button></div>\n      </section>\n      <div class="hotel-calendar-layout"><section class="hotel-calendar-board">${function(products,dates,maps){return`<div class="hotel-calendar-grid-shell" tabindex="0" aria-label="Monthly room rate and inventory calendar">\n      <table class="hotel-calendar-grid">\n        <thead><tr><th class="hotel-calendar-grid__product"><span>Room × Rate Plan</span><small>Choose products to edit</small></th>${dates.map(date=>`<th class="${date===todayIsoDate()?"is-today":""}"><span>${escapeHtml(calendarDateLabel(date))}</span><small>${escapeHtml(date.slice(5))}</small></th>`).join("")}</tr></thead>\n        <tbody>${products.map(product=>`<tr data-calendar-product-row="${escapeAttr(product.id)}"><th class="hotel-calendar-grid__product"><label><input type="checkbox" data-calendar-product-check value="${escapeAttr(product.id)}" ${state.calendar.selected_product_ids.includes(product.id)?"checked":""} /><span><strong>${escapeHtml(product.room_name)}</strong><small>${escapeHtml(product.rate_plan_name)} · ${escapeHtml(product.pricing_label)}</small></span></label></th>${dates.map(date=>`<td>${calendarCellMarkup(product,date,maps)}</td>`).join("")}</tr>`).join("")}</tbody>\n      </table>\n    </div>`}(products,dates,maps)}${function(products,dates,maps){const selectedProduct=products.find(product=>product.id===state.calendar.mobile_product_id)||products[0];return selectedProduct?`<div class="hotel-calendar-mobile">\n      <label class="admin-form-field"><span>Room and Rate Plan</span><select data-calendar-mobile-product>${products.map(product=>`<option value="${escapeAttr(product.id)}" ${product.id===selectedProduct.id?"selected":""}>${escapeHtml(product.room_name)} · ${escapeHtml(product.rate_plan_name)}</option>`).join("")}</select></label>\n      <label class="hotel-calendar-mobile__select"><input type="checkbox" data-calendar-product-check value="${escapeAttr(selectedProduct.id)}" ${state.calendar.selected_product_ids.includes(selectedProduct.id)?"checked":""} /> Include this product in the next edit</label>\n      <div class="hotel-calendar-mobile__days">${dates.map(date=>`<article class="${date===todayIsoDate()?"is-today":""}"><header><strong>${escapeHtml(calendarDateLabel(date,{long:!0}))}</strong><small>${escapeHtml(date)}</small></header>${calendarCellMarkup(selectedProduct,date,maps)}</article>`).join("")}</div>\n    </div>`:""}(products,dates,maps)}</section>${function(products){const selected=new Set(state.calendar.selected_product_ids),rules=Core.asArray(state.calendar.data?.rate_rules).filter(rule=>!selected.size||selected.has(rule.room_rate_id)),tiers=Core.asArray(state.calendar.data?.occupancy_tiers).filter(tier=>!selected.size||selected.has(tier.room_rate_id)),productById=new Map(products.map(product=>[product.id,product]));return`<aside class="hotel-calendar-rules">\n      <section class="hotel-workspace-card">\n        <div class="hotel-calendar-rules__heading"><div><span class="hotel-workspace-eyebrow">Seasonal & weekday rules</span><h4>${rules.length} rule${1===rules.length?"":"s"}</h4></div><button class="btn-secondary" type="button" data-add-calendar-rule ${selected.size?"":"disabled"}>+ Rule</button></div>\n        ${rules.length?`<ul>${rules.slice(0,20).map(rule=>{const product=productById.get(rule.room_rate_id);return`<li><button type="button" data-edit-calendar-rule="${escapeAttr(rule.id)}"><strong>${escapeHtml(product?.room_name||"Rate product")} · ${escapeHtml(formatMoney(rule.nightly_rate,product?.currency))}</strong><small>${escapeHtml(rule.valid_from)} → ${escapeHtml(rule.valid_to)} · priority ${Number(rule.priority||0)}${!1===rule.is_active?" · inactive":""}</small></button></li>`}).join("")}</ul>`:"<p>Select a product, then create a reviewed seasonal or weekday rule.</p>"}\n      </section>\n      <section class="hotel-workspace-card">\n        <div class="hotel-calendar-rules__heading"><div><span class="hotel-workspace-eyebrow">Occupancy / LOS tiers</span><h4>${tiers.length} tier${1===tiers.length?"":"s"}</h4></div><button class="btn-secondary" type="button" data-add-occupancy-tier ${selected.size?"":"disabled"}>+ Tier</button></div>\n        ${tiers.length?`<ul>${tiers.slice(0,30).map(tier=>{const product=productById.get(tier.room_rate_id);return`<li><button type="button" data-edit-occupancy-tier="${escapeAttr(tier.id)}"><strong>${Number(tier.guest_count)} guests · ${Number(tier.threshold_nights)}+ nights</strong><small>${escapeHtml(product?.room_name||"Rate product")} · ${escapeHtml(formatMoney(tier.nightly_rate,product?.currency))}${!1===tier.is_active?" · inactive":""}</small></button></li>`}).join("")}</ul>`:"<p>Optional tiers let one product reproduce reviewed guest-count and length-of-stay pricing without duplicating rooms.</p>"}\n        <button class="btn-secondary" type="button" data-preview-authoritative-rate ${1===selected.size?"":"disabled"}>Preview authoritative stay</button>\n      </section>\n    </aside>`}(products)}</div>\n      <section class="hotel-calendar-legend"><span><i class="is-base"></i> Base</span><span><i class="is-manual"></i> Manual / resolved override</span><span><i class="is-closed"></i> Closed</span><small>Displayed cells use server-resolved effective values and provenance whenever supplied. Raw rows remain in the reviewed concurrency snapshot.</small></section>`,function(panel,products){panel.querySelectorAll("[data-calendar-cell]").forEach(button=>button.addEventListener("click",event=>{!function(productId,date,extend=!1){state.calendar.selected_product_ids.includes(productId)||state.calendar.selected_product_ids.push(productId);const existingAnchor=state.calendar.selection_anchor||(extend?state.calendar.selection_start:null);existingAnchor?(state.calendar.selection_start=existingAnchor<date?existingAnchor:date,state.calendar.selection_end=existingAnchor>date?existingAnchor:date,state.calendar.selection_anchor=null):(state.calendar.selection_start=date,state.calendar.selection_end=date,state.calendar.selection_anchor=date),renderActivePanel()}(button.dataset.productId,button.dataset.date,event.shiftKey)})),panel.querySelectorAll("[data-calendar-product-check]").forEach(checkbox=>checkbox.addEventListener("change",()=>{const id=checkbox.value;state.calendar.selected_product_ids=checkbox.checked?Array.from(new Set([...state.calendar.selected_product_ids,id])):state.calendar.selected_product_ids.filter(value=>value!==id),renderActivePanel()})),panel.querySelector("[data-calendar-mobile-product]")?.addEventListener("change",event=>{state.calendar.mobile_product_id=event.currentTarget.value,renderActivePanel()}),panel.querySelectorAll("[data-calendar-shift]").forEach(button=>button.addEventListener("click",()=>function(periods){const anchor=parseIsoDate(state.calendar.anchor_date||todayIsoDate());anchor&&("week"===state.calendar.view?anchor.setUTCDate(anchor.getUTCDate()+7*Number(periods)):anchor.setUTCMonth(anchor.getUTCMonth()+Number(periods)*("two_months"===state.calendar.view?2:1),1),state.calendar.anchor_date=isoDateFromUtc(anchor),state.calendar.data=null,state.calendar.error=null,state.calendar.selection_start=null,state.calendar.selection_end=null,state.calendar.selection_anchor=null,renderActivePanel())}(Number(button.dataset.calendarShift)))),panel.querySelector("[data-calendar-today]")?.addEventListener("click",()=>{state.calendar.anchor_date=todayIsoDate(),state.calendar.data=null,state.calendar.error=null,renderActivePanel()}),panel.querySelector("[data-calendar-view]")?.addEventListener("change",event=>{state.calendar.view=["week","two_months"].includes(event.currentTarget.value)?event.currentTarget.value:"month",state.calendar.data=null,state.calendar.error=null,renderActivePanel()}),panel.querySelector("[data-calendar-select-all]")?.addEventListener("click",()=>{state.calendar.selected_product_ids=state.calendar.selected_product_ids.length===products.length?[]:products.map(product=>product.id),renderActivePanel()}),panel.querySelector("[data-calendar-clear-selection]")?.addEventListener("click",()=>{state.calendar.selection_start=null,state.calendar.selection_end=null,state.calendar.selection_anchor=null,renderActivePanel()}),panel.querySelector("[data-calendar-edit-range]")?.addEventListener("click",openCalendarRangeEditor),panel.querySelector("[data-add-calendar-rule]")?.addEventListener("click",()=>openCalendarRuleEditor()),panel.querySelectorAll("[data-edit-calendar-rule]").forEach(button=>button.addEventListener("click",()=>openCalendarRuleEditor(button.dataset.editCalendarRule))),panel.querySelector("[data-add-occupancy-tier]")?.addEventListener("click",()=>openOccupancyTierEditor()),panel.querySelectorAll("[data-edit-occupancy-tier]").forEach(button=>button.addEventListener("click",()=>openOccupancyTierEditor(button.dataset.editOccupancyTier))),panel.querySelector("[data-preview-authoritative-rate]")?.addEventListener("click",openAuthoritativeRatePreview)}(panel,products)}async function loadCalendarRange(){if(!state.workspace||state.calendar.loading)return;const propertyId=state.workspace.property.id,range=activeCalendarRange();state.calendar.loading=!0,state.calendar.error=null,renderActivePanel();try{const data=await Repository.getCalendar(propertyId,range.start,range.end);if(!state.workspace||state.workspace.property.id!==propertyId)return;state.calendar.data=data,state.calendar.selected_product_ids=state.calendar.selected_product_ids.filter(id=>data.room_rates.some(rate=>rate.id===id))}catch(error){state.calendar.error=error}finally{state.calendar.loading=!1,state.workspace?.property.id===propertyId&&"calendar"===state.activeTab&&renderActivePanel()}}function renderCalendarPanel(panel){const range=activeCalendarRange(),data=state.calendar.data,rangeMatches=data?.hotel_id===state.workspace.property.id&&data?.start_date===range.start&&data?.end_date===range.end;if(!state.calendar.loading)return state.calendar.error?(panel.innerHTML=`${workspacePanelHeader("Calendar & Rates","The calendar failed closed; no raw-table fallback is used.")}<div class="hotel-property-empty hotel-property-empty--error"><p>${escapeHtml(state.calendar.error.message||"Calendar could not be loaded.")}</p><button class="btn-secondary" type="button" data-calendar-retry>Retry exact range</button></div>`,void panel.querySelector("[data-calendar-retry]")?.addEventListener("click",()=>{state.calendar.error=null,loadCalendarRange()})):rangeMatches?void renderCalendarLoaded(panel,range,data):(panel.innerHTML=`${workspacePanelHeader("Calendar & Rates","Loading authoritative room rates, inventory and restrictions…")}<div class="hotel-property-empty"><span class="hotel-workspace-spinner" aria-hidden="true"></span> Loading ${escapeHtml(calendarMonthTitle(range))}…</div>`,void loadCalendarRange());panel.innerHTML=`${workspacePanelHeader("Calendar & Rates","Loading authoritative room rates, inventory and restrictions…")}<div class="hotel-property-empty"><span class="hotel-workspace-spinner" aria-hidden="true"></span> Loading ${escapeHtml(calendarMonthTitle(range))}…</div>`}function selectedCalendarProducts(){const selected=new Set(state.calendar.selected_product_ids);return calendarProducts().filter(product=>selected.has(product.id))}function buildCalendarPlan(operations){const range=activeCalendarRange(),snapshotStart=state.calendar.data?.start_date,snapshotEnd=state.calendar.data?.end_date,plan={hotel_id:state.workspace.property.id,from:snapshotStart||range.start,to:snapshotEnd||range.end,reviewed_at:(new Date).toISOString(),operations:Core.clone(operations)};return state.calendar.data?.snapshot_token&&(plan.snapshot_token=state.calendar.data.snapshot_token),plan}function calendarReviewRows(rows){return Core.asArray(rows).map(row=>`<tr><th>${escapeHtml(row.field)}</th><td><pre>${escapeHtml(displayReviewValue(row.before))}</pre></td><td><pre>${escapeHtml(displayReviewValue(row.after))}</pre></td></tr>`).join("")}function calendarReviewState(entity,row){const source=Core.asObject(row);return"daily_inventory"===entity?{sellable_units:source.sellable_units??null,sellable_units_mode:source.sellable_units_mode??null,closed:source.closed??null,closed_mode:source.closed_mode??null,expires_at:source.expires_at??null}:{nightly_rate:source.nightly_rate??null,nightly_rate_mode:source.nightly_rate_mode??null,minimum_stay:source.minimum_stay??null,minimum_stay_mode:source.minimum_stay_mode??null,maximum_stay:source.maximum_stay??null,maximum_stay_mode:source.maximum_stay_mode??null,closed_to_arrival:source.closed_to_arrival??null,closed_to_arrival_mode:source.closed_to_arrival_mode??null,closed_to_departure:source.closed_to_departure??null,closed_to_departure_mode:source.closed_to_departure_mode??null,expires_at:source.expires_at??null}}function calendarExactReviewRows(operations){const overrideById=new Map(Core.asArray(state.calendar.data?.calendar_overrides).map(row=>[row.id,row])),inventoryByKey=new Map(Core.asArray(state.calendar.data?.daily_inventory).map(row=>[`${row.room_type_id}:${row.stay_date}`,row])),productById=new Map(calendarProducts().map(product=>[product.id,product])),roomById=new Map(state.workspace.room_types.map(room=>[room.id,room])),rows=operations.slice(0,200).map(operation=>{const payload=Core.asObject(operation.payload),existing="daily_inventory"===operation.entity?inventoryByKey.get(`${payload.room_type_id}:${payload.stay_date}`):overrideById.get(operation.id),after="delete"===operation.type?null:{...Core.asObject(existing),...payload},product=productById.get(payload.room_rate_id||existing?.room_rate_id),room=roomById.get(payload.room_type_id||existing?.room_type_id||product?.room_type_id);return{field:"daily_inventory"===operation.entity?`${Core.i18nText(room?.name_i18n,"en",room?.code||"Room Type")} inventory · ${payload.stay_date}`:`${product?.room_name||"Rate product"} · ${product?.rate_plan_name||""} · ${payload.stay_date||existing?.stay_date}`,before:existing?calendarReviewState(operation.entity,existing):"Inherited / base value",after:after?calendarReviewState(operation.entity,after):"Inherited / base value"}});return operations.length>rows.length&&rows.push({field:"Additional exact rows",before:0,after:operations.length-rows.length}),rows}async function openCalendarReview({title:title,plan:plan,rows:rows,successMessage:successMessage}){plan?.operations?.length?(state.pendingReview={calendarPlan:Core.clone(plan)},openModal({title:title,className:"hotel-workspace-modal--review hotel-workspace-modal--wide",body:`<div class="hotel-review-summary"><p>One exact-property Calendar transaction will apply only if every reviewed version still matches.</p><dl><div><dt>Property ID</dt><dd><code>${escapeHtml(plan.hotel_id)}</code></dd></div><div><dt>Operations</dt><dd>${plan.operations.length}</dd></div><div><dt>Range</dt><dd>${escapeHtml(plan.from)} → ${escapeHtml(plan.to)}</dd></div><div><dt>Concurrency</dt><dd>Exact row versions</dd></div></dl></div>\n        <div class="hotel-review-table-wrap"><table class="hotel-review-table"><thead><tr><th>Change</th><th>Before</th><th>After</th></tr></thead><tbody>${calendarReviewRows(rows)}</tbody></table></div>\n        <p class="hotel-workspace-safety-note">This remains inert V2 configuration. It does not publish the property, alter legacy prices, migrate bookings or enable a Hotels feature flag.</p>`,footer:'<button class="btn-secondary" type="button" data-hotel-modal-close>Back</button><button class="btn-primary" type="button" data-calendar-review-confirm>Save reviewed Calendar changes</button>',onReady(overlay){overlay.querySelector("[data-calendar-review-confirm]")?.addEventListener("click",async event=>{const button=event.currentTarget;button.disabled=!0,button.textContent="Saving…",setModalSaving(overlay,!0);try{const result=await Repository.applyCalendarPlan(plan);closeModal({restoreFocus:!1,skipCleanup:!0,force:!0});const activeRange=activeCalendarRange();result.calendar?.hotel_id===state.workspace.property.id&&result.calendar.start_date===activeRange.start&&result.calendar.end_date===activeRange.end?(state.calendar.data=result.calendar,Array.isArray(result.calendar.activity)&&(state.workspace.activity=Core.clone(result.calendar.activity)),renderActivePanel()):(state.calendar.data=null,await loadCalendarRange()),toast(successMessage||"Reviewed Calendar changes saved.","success")}catch(error){setModalSaving(overlay,!1),button.disabled=!1,button.textContent="Save reviewed Calendar changes",toast(error?.isStale?"Save stopped: Calendar data changed after Review. Reload the exact range and review again.":error?.isAmbiguousOutcome?"The Calendar save result could not be confirmed. Reload before retrying; never submit the reviewed plan blindly.":error?.message||"Calendar save failed. No partial change was kept.",error?.isAmbiguousOutcome?"warning":"error")}})}})):toast("There are no Calendar changes to review.","info")}function calendarPatchField(fd,field,options={}){const mode=String(fd.get(`${field}_mode`)||"no_change");if("no_change"===mode)return null;if("clear"===mode)return{mode:"clear"};if(options.boolean)return{mode:"set",value:"true"===mode};const raw=fd.get(field),value=Number(raw);if(!Number.isFinite(value)||value<0||options.integer&&!Number.isInteger(value)||options.positive&&value<=0)throw new Error(`${options.label||field} is invalid.`);return{mode:"set",value:value}}function rangePatchControl(name,label,type,options={}){const setLabel=options.setLabel||"Set value",clearLabel=options.clearLabel||"Clear override";return`<label class="admin-form-field hotel-calendar-patch-field"><span>${escapeHtml(label)}</span><select name="${escapeAttr(name)}_mode" data-calendar-patch-mode><option value="no_change">No change</option><option value="set">${escapeHtml(setLabel)}</option><option value="clear">${escapeHtml(clearLabel)}</option></select><input name="${escapeAttr(name)}" type="${type}" min="${options.min??0}" step="${options.step||1}" placeholder="${escapeAttr(options.placeholder||"")}" disabled /></label>`}function applyCalendarFieldPatch(source,patch){const next={...source};return Object.entries(patch).forEach(([field,change])=>{next[`${field}_mode`]=change.mode,"set"===change.mode?next[field]=change.value:"clear"===change.mode&&(next[field]=null)}),next}function calendarOverrideOperations(products,dates,patch,provenance){if(!Object.keys(patch).length)return[];const existingByKey=new Map(Core.asArray(state.calendar.data?.calendar_overrides).map(row=>[`${row.room_rate_id}:${row.stay_date}`,row])),valueFields=["nightly_rate","minimum_stay","maximum_stay","closed","closed_to_arrival","closed_to_departure"],operations=[];return products.forEach(product=>dates.forEach(stayDate=>{const existing=existingByKey.get(`${product.id}:${stayDate}`)||null,next=applyCalendarFieldPatch(existing||{},patch);if(!valueFields.some(field=>null!=next[`${field}_mode`]))return;const payload={};Object.entries(patch).forEach(([field,change])=>{payload[field]="clear"===change.mode?null:change.value,payload[`${field}_mode`]=change.mode}),payload.provenance={...Core.asObject(existing?.provenance),...provenance,source:"manual"},payload.reason=provenance.reason,payload.expires_at=provenance.expires_at,payload.source="manual",payload.is_active=!0,existing||(payload.room_rate_id=product.id,payload.stay_date=stayDate),operations.push({entity:"calendar_override",type:existing?"update":"create",id:existing?.id||Core.newUuid(),expected_version:existing?Number(existing.version):0,payload:payload})})),operations}function dailyInventoryOperations(products,dates,patch,provenance){if(!Object.keys(patch).length)return[];const roomById=new Map;products.forEach(product=>{roomById.has(product.room_type_id)||roomById.set(product.room_type_id,product)});const existingByKey=new Map(Core.asArray(state.calendar.data?.daily_inventory).map(row=>[`${row.room_type_id}:${row.stay_date}`,row])),operations=[];return roomById.forEach((product,roomTypeId)=>dates.forEach(stayDate=>{const existing=existingByKey.get(`${roomTypeId}:${stayDate}`)||null,next=applyCalendarFieldPatch(existing||{},patch),sellableMode=next.sellable_units_mode??null,closedMode=next.closed_mode??null,noRemainingOverride=!(sellableMode&&"clear"!==sellableMode||closedMode&&"clear"!==closedMode);if(existing&&noRemainingOverride)return void operations.push({entity:"daily_inventory",type:"delete",expected_version:Number(existing.version),payload:{room_type_id:roomTypeId,stay_date:stayDate}});if(!existing&&noRemainingOverride)return;const payload={room_type_id:roomTypeId,stay_date:stayDate,source:"manual",reason:provenance.reason,expires_at:provenance.expires_at,provenance:{...Core.asObject(existing?.provenance),...provenance,source:"manual"}};Object.entries(patch).forEach(([field,change])=>{payload[field]="clear"===change.mode?null:change.value,payload[`${field}_mode`]=change.mode}),existing||Object.hasOwn(payload,"sellable_units")||(payload.sellable_units=Number(product.base_inventory_count??0),payload.sellable_units_mode="clear"),existing||Object.hasOwn(payload,"closed")||(payload.closed=!1,payload.closed_mode="clear"),operations.push({entity:"daily_inventory",type:"upsert",expected_version:existing?Number(existing.version):0,payload:payload})})),operations}function openCalendarRangeEditor(){const products=selectedCalendarProducts(),startDate=state.calendar.selection_start,endDate=state.calendar.selection_end;products.length&&startDate&&endDate?openModal({title:"Edit selected Calendar range",className:"hotel-workspace-modal--wide",body:`<form id="hotelCalendarRangeForm" class="hotel-workspace-form">\n        <div class="hotel-calendar-edit-context"><div><span>Products</span><strong>${products.length}</strong><small>${products.map(product=>`${product.room_name} · ${product.rate_plan_name}`).join(", ")}</small></div><div><span>Date range</span><strong>${escapeHtml(startDate)} → ${escapeHtml(endDate)}</strong><small>${enumerateCalendarDates(startDate,endDate).length} calendar days</small></div></div>\n        <fieldset><legend>Apply on weekdays</legend><div class="hotel-calendar-weekdays">${["Mon","Tue","Wed","Thu","Fri","Sat","Sun"].map((label,index)=>`<label><input type="checkbox" name="weekday" value="${index+1}" checked /> ${label}</label>`).join("")}</div></fieldset>\n        <fieldset><legend>Rates & inventory</legend><div class="hotel-workspace-form-grid">${rangePatchControl("nightly_rate","Nightly rate","number",{step:"0.01",placeholder:"EUR",clearLabel:"Use lower-precedence price"})}${rangePatchControl("sellable_units","Sellable rooms","number",{integer:!0,clearLabel:"Use base inventory"})}</div></fieldset>\n        <fieldset><legend>Availability & restrictions</legend><div class="hotel-workspace-form-grid">\n          <label class="admin-form-field"><span>Room safety closure</span><select name="closed_mode"><option value="no_change">No change</option><option value="false">Set open</option><option value="true">Set closed</option><option value="clear">Clear override / inherit</option></select></label>\n          ${rangePatchControl("minimum_stay","Minimum stay","number",{min:1,positive:!0,clearLabel:"No minimum override"})}\n          ${rangePatchControl("maximum_stay","Maximum stay","number",{min:1,positive:!0,clearLabel:"No maximum override"})}\n          <label class="admin-form-field"><span>Arrival</span><select name="closed_to_arrival_mode"><option value="no_change">No change</option><option value="false">Allow arrival</option><option value="true">Close to arrival</option><option value="clear">Clear override / inherit</option></select></label>\n          <label class="admin-form-field"><span>Departure</span><select name="closed_to_departure_mode"><option value="no_change">No change</option><option value="false">Allow departure</option><option value="true">Close to departure</option><option value="clear">Clear override / inherit</option></select></label>\n        </div></fieldset>\n        <fieldset><legend>Audit context</legend><div class="hotel-workspace-form-grid"><label class="admin-form-field"><span>Reason</span><input name="reason" maxlength="500" required placeholder="Why is this override needed?" /></label><label class="admin-form-field"><span>Temporary override expires</span><input name="expires_at" type="datetime-local" /><small>Optional. Leave empty for a reviewed persistent override.</small></label></div></fieldset>\n        <p class="hotel-workspace-safety-note">No change preserves every selected product's current value. Clear removes only the selected override field and reveals the next lower precedence.</p>\n      </form>`,footer:'<button class="btn-secondary" type="button" data-hotel-modal-close>Cancel</button><button class="btn-primary" type="submit" form="hotelCalendarRangeForm">Review range changes</button>',onReady(overlay){const form=overlay.querySelector("#hotelCalendarRangeForm");form.querySelectorAll("[data-calendar-patch-mode]").forEach(select=>select.addEventListener("change",()=>{const input=select.parentElement.querySelector("input");input.disabled="set"!==select.value,input.disabled||input.focus()})),form.addEventListener("submit",event=>{event.preventDefault();const fd=new FormData(form);let ratePatch,inventoryPatch;try{ratePatch={nightly_rate:calendarPatchField(fd,"nightly_rate",{label:"Nightly rate"}),minimum_stay:calendarPatchField(fd,"minimum_stay",{label:"Minimum stay",integer:!0,positive:!0}),maximum_stay:calendarPatchField(fd,"maximum_stay",{label:"Maximum stay",integer:!0,positive:!0}),closed_to_arrival:calendarPatchField(fd,"closed_to_arrival",{boolean:!0}),closed_to_departure:calendarPatchField(fd,"closed_to_departure",{boolean:!0})},inventoryPatch={sellable_units:calendarPatchField(fd,"sellable_units",{label:"Sellable rooms",integer:!0}),closed:calendarPatchField(fd,"closed",{boolean:!0})}}catch(error){return void toast(error.message,"error")}if(ratePatch=Object.fromEntries(Object.entries(ratePatch).filter(([_key,value])=>value)),inventoryPatch=Object.fromEntries(Object.entries(inventoryPatch).filter(([_key,value])=>value)),!Object.keys(ratePatch).length&&!Object.keys(inventoryPatch).length)return void toast("Choose at least one explicit Calendar change.","error");const weekdays=fd.getAll("weekday").map(Number).sort((a,b)=>a-b);if(!weekdays.length)return void toast("Select at least one weekday.","error");const reason=String(fd.get("reason")||"").trim(),expiresAtRaw=String(fd.get("expires_at")||"").trim(),expiresDate=expiresAtRaw?new Date(expiresAtRaw):null,expiresAt=expiresDate&&!Number.isNaN(expiresDate.getTime())?expiresDate.toISOString():null;if(!reason)return void toast("A concise audit reason is required.","error");if(expiresAtRaw&&!expiresAt)return void toast("Temporary override expiry is invalid.","error");const dates=enumerateCalendarDates(startDate,endDate).filter(date=>weekdays.includes(parseIsoDate(date).getUTCDay()||7)),provenance={reason:reason,expires_at:expiresAt,reviewed_range:{from:startDate,to:endDate,weekdays:weekdays}},operations=[...calendarOverrideOperations(products,dates,ratePatch,provenance),...dailyInventoryOperations(products,dates,inventoryPatch,provenance)];if(!operations.length)return void toast("The reviewed range produces no database changes.","info");const plan=buildCalendarPlan(operations,products.map(product=>product.id));closeModal({restoreFocus:!1}),openCalendarReview({title:"Review Calendar range update",plan:plan,rows:[{field:"Room × Rate Plan products",before:"No change",after:products.map(product=>`${product.room_name} · ${product.rate_plan_name}`)},{field:"Dates / weekdays",before:"Current exact rows",after:{start_date:startDate,end_date:endDate,weekdays:weekdays}},{field:"Audit context",before:"Not supplied",after:{reason:reason,expires_at:expiresAt}},...calendarExactReviewRows(operations)],successMessage:"Calendar range updated atomically."})})}}):toast("Select at least one Room × Rate Plan product and a date range.","error")}function openCalendarRuleEditor(ruleId=null){const existing=ruleId?Core.asArray(state.calendar.data?.rate_rules).find(rule=>rule.id===ruleId):null,products=existing?calendarProducts().filter(product=>product.id===existing.room_rate_id):selectedCalendarProducts();if(!products.length)return void toast("Select at least one Room × Rate Plan product.","error");const range=activeCalendarRange();openModal({title:existing?"Edit seasonal / weekday rule":"Add seasonal / weekday rule",className:"hotel-workspace-modal--wide",body:`<form id="hotelCalendarRuleForm" class="hotel-workspace-form">\n        <p>${existing?"This edits one exact reviewed rule.":`One draft rule will be created for each of ${products.length} selected products.`}</p>\n        <div class="hotel-workspace-form-grid"><label class="admin-form-field"><span>Valid from</span><input type="date" name="valid_from" value="${escapeAttr(existing?.valid_from||range.start)}" required /></label><label class="admin-form-field"><span>Valid to</span><input type="date" name="valid_to" value="${escapeAttr(existing?.valid_to||range.end)}" required /></label><label class="admin-form-field"><span>Nightly rate</span><input type="number" name="nightly_rate" min="0" step="0.01" value="${escapeAttr(existing?.nightly_rate??"")}" required /></label><label class="admin-form-field"><span>Priority</span><input type="number" name="priority" min="-32768" max="32767" step="1" value="${escapeAttr(existing?.priority??0)}" required /></label><label class="admin-form-field"><span>Minimum stay</span><input type="number" name="minimum_stay" min="1" step="1" value="${escapeAttr(existing?.minimum_stay??"")}" /></label><label class="admin-form-field"><span>Maximum stay</span><input type="number" name="maximum_stay" min="1" step="1" value="${escapeAttr(existing?.maximum_stay??"")}" /></label></div>\n        <fieldset><legend>Weekdays</legend><div class="hotel-calendar-weekdays">${["Mon","Tue","Wed","Thu","Fri","Sat","Sun"].map((label,index)=>`<label><input type="checkbox" name="weekday" value="${index+1}" ${!existing||Core.asArray(existing.weekdays).includes(index+1)?"checked":""} /> ${label}</label>`).join("")}</div></fieldset>\n        <div class="hotel-workspace-form-grid"><label class="admin-checkbox-field"><input type="checkbox" name="closed_to_arrival" ${existing?.closed_to_arrival?"checked":""} /> Closed to arrival</label><label class="admin-checkbox-field"><input type="checkbox" name="closed_to_departure" ${existing?.closed_to_departure?"checked":""} /> Closed to departure</label><label class="admin-checkbox-field"><input type="checkbox" name="is_active" ${!1===existing?.is_active?"":"checked"} /> Active</label></div>\n        <p class="hotel-workspace-safety-note">Equal-priority overlapping rules are rejected by the server. Use exact priorities deliberately.</p>\n      </form>`,footer:'<button class="btn-secondary" type="button" data-hotel-modal-close>Cancel</button><button class="btn-primary" type="submit" form="hotelCalendarRuleForm">Review rule</button>',onReady(overlay){overlay.querySelector("#hotelCalendarRuleForm")?.addEventListener("submit",event=>{event.preventDefault();const fd=new FormData(event.currentTarget),payload={valid_from:String(fd.get("valid_from")),valid_to:String(fd.get("valid_to")),weekdays:fd.getAll("weekday").map(Number).sort((a,b)=>a-b),nightly_rate:Number(fd.get("nightly_rate")),minimum_stay:""===fd.get("minimum_stay")?null:Number(fd.get("minimum_stay")),maximum_stay:""===fd.get("maximum_stay")?null:Number(fd.get("maximum_stay")),closed_to_arrival:fd.has("closed_to_arrival"),closed_to_departure:fd.has("closed_to_departure"),priority:Number(fd.get("priority")),is_active:fd.has("is_active")};if(!payload.valid_from||!payload.valid_to||payload.valid_to<payload.valid_from||!payload.weekdays.length||!Number.isFinite(payload.nightly_rate)||payload.nightly_rate<0||payload.minimum_stay&&payload.maximum_stay&&payload.maximum_stay<payload.minimum_stay)return void toast("Review dates, weekdays, rate and stay limits.","error");const plan=buildCalendarPlan(products.map(product=>({entity:"rate_rule",type:existing?"update":"create",id:existing?.id||Core.newUuid(),expected_version:existing?Number(existing.version):0,payload:{...payload,room_rate_id:product.id}})),products.map(product=>product.id));closeModal({restoreFocus:!1}),openCalendarReview({title:"Review seasonal / weekday rule",plan:plan,rows:[{field:"Products",before:existing?existing.room_rate_id:"No rules",after:products.map(product=>`${product.room_name} · ${product.rate_plan_name}`)},{field:"Rule",before:existing||"Not configured",after:payload}],successMessage:existing?"Rate rule updated.":"Rate rules created atomically."})})}})}function openOccupancyTierEditor(tierId=null){const existing=tierId?Core.asArray(state.calendar.data?.occupancy_tiers).find(tier=>tier.id===tierId):null,products=existing?calendarProducts().filter(product=>product.id===existing.room_rate_id):selectedCalendarProducts();products.length?openModal({title:existing?"Edit occupancy / stay tier":"Add occupancy / stay tier",body:`<form id="hotelOccupancyTierForm" class="hotel-workspace-form"><p>${existing?"Edit this exact tier.":`Create one reviewed tier for each of ${products.length} selected products.`}</p><div class="hotel-workspace-form-grid"><label class="admin-form-field"><span>Guest count</span><input name="guest_count" type="number" min="1" step="1" value="${escapeAttr(existing?.guest_count??"")}" required /></label><label class="admin-form-field"><span>From stay length</span><input name="threshold_nights" type="number" min="1" step="1" value="${escapeAttr(existing?.threshold_nights??"")}" required /><small>Selected nightly rate applies to the complete stay.</small></label><label class="admin-form-field"><span>Nightly rate</span><input name="nightly_rate" type="number" min="0" step="0.01" value="${escapeAttr(existing?.nightly_rate??"")}" required /></label><label class="admin-checkbox-field"><input name="is_active" type="checkbox" ${!1===existing?.is_active?"":"checked"} /> Active</label></div><p class="hotel-workspace-safety-note">A duplicate exact product + guest count + threshold is rejected. Pricing conversion remains shadow-only until its complete legacy oracle passes.</p></form>`,footer:'<button class="btn-secondary" type="button" data-hotel-modal-close>Cancel</button><button class="btn-primary" type="submit" form="hotelOccupancyTierForm">Review tier</button>',onReady(overlay){overlay.querySelector("#hotelOccupancyTierForm")?.addEventListener("submit",event=>{event.preventDefault();const fd=new FormData(event.currentTarget),payload={guest_count:Number(fd.get("guest_count")),threshold_nights:Number(fd.get("threshold_nights")),nightly_rate:Number(fd.get("nightly_rate")),is_active:fd.has("is_active")};if(!Number.isInteger(payload.guest_count)||payload.guest_count<=0||!Number.isInteger(payload.threshold_nights)||payload.threshold_nights<=0||!Number.isFinite(payload.nightly_rate)||payload.nightly_rate<0)return void toast("Guest count, stay threshold and nightly rate are required.","error");const plan=buildCalendarPlan(products.map(product=>({entity:"occupancy_tier",type:existing?"update":"create",id:existing?.id||Core.newUuid(),expected_version:existing?Number(existing.version):0,payload:{...payload,room_rate_id:product.id}})),products.map(product=>product.id));closeModal({restoreFocus:!1}),openCalendarReview({title:"Review occupancy / stay tier",plan:plan,rows:[{field:"Products",before:existing?existing.room_rate_id:"No tiers",after:products.map(product=>`${product.room_name} · ${product.rate_plan_name}`)},{field:"Tier",before:existing||"Not configured",after:payload}],successMessage:existing?"Occupancy tier updated.":"Occupancy tiers created atomically."})})}}):toast("Select at least one Room × Rate Plan product.","error")}function openAuthoritativeRatePreview(){const[product]=selectedCalendarProducts();if(!product||1!==state.calendar.selected_product_ids.length)return void toast("Select exactly one product to preview.","error");const range=activeCalendarRange(),arrival=state.calendar.selection_start||range.start,departure=addCalendarDays(state.calendar.selection_end||arrival,1);openModal({title:"Preview authoritative stay rate",body:`<form id="hotelRatePreviewForm" class="hotel-workspace-form"><p><strong>${escapeHtml(product.room_name)} · ${escapeHtml(product.rate_plan_name)}</strong></p><div class="hotel-workspace-form-grid"><label class="admin-form-field"><span>Check-in</span><input name="check_in" type="date" value="${escapeAttr(arrival)}" required /></label><label class="admin-form-field"><span>Check-out</span><input name="check_out" type="date" value="${escapeAttr(departure)}" required /></label><label class="admin-form-field"><span>Guests</span><input name="guest_count" type="number" min="1" step="1" value="2" required /></label></div><div data-rate-preview-result aria-live="polite"></div></form>`,footer:'<button class="btn-secondary" type="button" data-hotel-modal-close>Close</button><button class="btn-primary" type="submit" form="hotelRatePreviewForm">Resolve preview</button>',onReady(overlay){overlay.querySelector("#hotelRatePreviewForm")?.addEventListener("submit",async event=>{event.preventDefault();const form=event.currentTarget,fd=new FormData(form),resultBox=form.querySelector("[data-rate-preview-result]");resultBox.innerHTML='<span class="hotel-workspace-spinner" aria-hidden="true"></span> Resolving on server…';try{const result=await Repository.resolveRate(product.id,fd.get("check_in"),fd.get("check_out"),Number(fd.get("guest_count"))),total=result.total??result.total_price??result.quote_total,nights=result.nights??Core.asArray(result.nightly_rates).length;resultBox.innerHTML=`<section class="hotel-rate-preview-result"><span class="hotel-workspace-eyebrow">Authoritative read-only preview</span><h4>${null==total?"See nightly breakdown":escapeHtml(formatMoney(total,product.currency))}</h4><p>${Number(nights||0)} night${1===Number(nights)?"":"s"} · ${escapeHtml(result.status||(!1===result.ok?"Blocked":"Resolved"))}</p><pre>${escapeHtml(JSON.stringify(result,null,2))}</pre></section>`}catch(error){resultBox.innerHTML=`<p class="hotel-property-card__blocker">${escapeHtml(error.message||"Authoritative preview failed closed.")}</p>`}})}})}function renderBookingsPanel(panel){const upcoming=Number(state.workspace.counts?.upcoming_bookings||0);panel.innerHTML=`${workspacePanelHeader("Bookings","Existing Hotel booking and partner-confirmation behavior remains unchanged.")}\n      <section class="hotel-workspace-card hotel-placeholder-card"><span class="hotel-workspace-eyebrow">Current legacy booking system</span><h4>${upcoming} upcoming booking${1===upcoming?"":"s"}</h4><p>H2A does not introduce a V2 booking engine. Open the existing Hotels Bookings view to manage current request-confirmation bookings.</p><button class="btn-primary" type="button" data-open-current-hotel-bookings>Open current Hotel bookings</button></section>`,panel.querySelector("[data-open-current-hotel-bookings]")?.addEventListener("click",()=>{closeWorkspace(),document.querySelector('.hotels-tab-button[data-tab="bookings"]')?.click?.()})}function openCentralHotelDepositSettings(){const propertyId=state.workspace?.property?.id;closeWorkspace(),"function"!=typeof window.openHotelDepositSettings?toast("Deposit Settings handoff is unavailable. Reload Admin and try again.","error"):window.openHotelDepositSettings(propertyId)}function renderPaymentsPanel(panel){const payment=Core.asObject(state.workspace.payment_due),exact=Core.asObject(payment.exact_override),fallback=Core.asObject(payment.default_rule),effective=Object.keys(exact).length?exact:fallback;panel.innerHTML=`${workspacePanelHeader("Payments","A read-only summary of the existing central Hotel payment-due-at-booking system.")}\n      <div class="hotel-workspace-summary-grid"><section class="hotel-workspace-card"><span class="hotel-workspace-eyebrow">Payment due at booking</span><h4>${escapeHtml(function(rule){const source=Core.asObject(rule);if(!source.enabled)return"No online payment required by this rule";const amount=Number(source.amount||0),currency=String(source.currency||state.workspace.property.currency||"EUR");return{percent_total:`${amount}% of booking total`,per_day:`${formatMoney(amount,currency)} per day`,per_hour:`${formatMoney(amount,currency)} per hour`,per_person:`${formatMoney(amount,currency)} per person`,flat:`${formatMoney(amount,currency)} flat`}[source.mode]||"Configured central payment rule"}(effective))}</h4><p>${Object.keys(exact).length?"Exact property override":"Hotels default rule"} from the existing central Deposit Settings tables.</p><button class="btn-primary" type="button" data-open-hotel-deposit>Manage in Deposit Settings</button></section>\n      <section class="hotel-workspace-card hotel-placeholder-card"><span class="hotel-workspace-eyebrow">Platform commission</span><h4>Not configured in H2A</h4><p>Commission is distinct from customer prepayment and remains future work.</p></section>\n      <section class="hotel-workspace-card hotel-placeholder-card"><span class="hotel-workspace-eyebrow">Partner payout / Stripe Connect</span><h4>Capability disabled</h4><p>No connected-account or payout behavior is exposed in this stage.</p></section></div>`,panel.querySelector("[data-open-hotel-deposit]")?.addEventListener("click",openCentralHotelDepositSettings)}function openPropertyMediaEditor(){const property=state.workspace.property,photos=Core.normalizeGallery(property.photos);openModal({title:"Edit property gallery",body:`<form id="hotelPropertyMediaForm" class="hotel-workspace-form">\n        <p class="hotel-workspace-intro">Property photos stay shared at property level. Room-specific photos belong in each Room Type.</p>\n        ${photos.length?`<div class="hotel-gallery-editor__grid">${photos.map(url=>`<label><img src="${escapeAttr(url)}" alt="" loading="lazy" /><span><input type="checkbox" name="remove_property_gallery_url" value="${escapeAttr(url)}" /> Remove</span><span><input type="radio" name="property_cover_url" value="${escapeAttr(url)}" ${url===property.cover_image_url?"checked":""} /> Cover</span></label>`).join("")}</div>`:"<p>No property gallery images yet.</p>"}\n        <label class="admin-form-field"><span>Add property photos</span><input type="file" name="property_gallery_files" accept="image/*" multiple /></label>\n        <label class="admin-form-field"><span>Cover image URL</span><input name="cover_image_url" type="url" value="${escapeAttr(property.cover_image_url||"")}" placeholder="Selected gallery photo or existing trusted URL" /></label>\n      </form>`,footer:'<button class="btn-secondary" type="button" data-hotel-modal-close>Cancel</button><button class="btn-primary" type="submit" form="hotelPropertyMediaForm">Review media changes</button>',onReady(overlay){const form=overlay.querySelector("#hotelPropertyMediaForm");form?.querySelectorAll('[name="property_cover_url"]').forEach(radio=>radio.addEventListener("change",()=>{form.elements.cover_image_url.value=radio.value})),form?.addEventListener("submit",async event=>{event.preventDefault();const fd=new FormData(form),removed=fd.getAll("remove_property_gallery_url").map(String),retained=photos.filter(url=>!removed.includes(url)),files=Array.from(form.elements.property_gallery_files?.files||[]);let uploadedUrls=[];if(closeModal({restoreFocus:!1}),files.length){const uploader=window.HotelsV2AdminMedia?.uploadPropertyGallery;if("function"!=typeof uploader)return void toast("Optimized property-image uploader is unavailable.","error");try{uploadedUrls=await uploader(property.slug,files)}catch(error){return void toast(error?.message||"Property gallery upload failed before Review.","error")}}const nextPhotos=Core.normalizeGallery([...retained,...uploadedUrls]);let coverImageUrl=String(fd.get("cover_image_url")||"").trim()||null;coverImageUrl&&removed.includes(coverImageUrl)&&(coverImageUrl=nextPhotos[0]||null),!coverImageUrl&&nextPhotos.length&&(coverImageUrl=nextPhotos[0]);const next={...property,photos:nextPhotos,cover_image_url:coverImageUrl},cleanupUploaded=async()=>{uploadedUrls.length&&(await(window.HotelsV2AdminMedia?.removePropertyGalleryUploads?.(uploadedUrls)),uploadedUrls=[])};await openReview({title:"Review property media changes",entity:"property",before:property,after:next,operation:Core.operationForEntity("property",next,property),onCancel:files.length?cleanupUploaded:null,onApplyError:files.length?async error=>{error?.isDefinitiveFailure&&await cleanupUploaded()}:null,closeOnApplyError:files.length>0,successMessage:"Property gallery updated without changing publication."})})}})}function renderContentPanel(panel){const property=state.workspace.property,photos=Core.normalizeGallery(property.photos),roomPhotoCount=state.workspace.room_types.reduce((count,room)=>count+room.gallery.length,0);panel.innerHTML=`${workspacePanelHeader("Content & Media","Property media stays shared; Room Type galleries contain only room-specific images.")}\n      <div class="hotel-workspace-summary-grid"><section class="hotel-workspace-card"><span class="hotel-workspace-eyebrow">Property gallery</span><h4>${photos.length} image${1===photos.length?"":"s"}</h4>${photos.length?`<div class="hotel-content-preview">${photos.slice(0,6).map(url=>`<img src="${escapeAttr(url)}" alt="" loading="lazy" />`).join("")}</div>`:"<p>No property gallery images.</p>"}<button class="btn-primary" type="button" data-edit-property-media>Edit property gallery</button></section>\n      <section class="hotel-workspace-card"><span class="hotel-workspace-eyebrow">Room galleries</span><h4>${roomPhotoCount} room image${1===roomPhotoCount?"":"s"}</h4><p>Edit a Room Type to upload images to its exact normalized gallery. Property photos are not duplicated automatically.</p><button class="btn-secondary" type="button" data-open-rooms-media>Open Rooms & Rates</button></section></div>\n      ${"legacy"===property.architecture_version?`<section class="hotel-workspace-card hotel-legacy-advanced"><details><summary>Advanced / legacy content</summary><p>The current editor remains available for legacy JSON room information and public-page compatibility. It never writes normalized Room Types.</p><button class="btn-secondary" type="button" data-edit-legacy-content>Open legacy editor</button><code>${escapeHtml(property.id)}</code></details></section>`:""}`,panel.querySelector("[data-edit-property-media]")?.addEventListener("click",openPropertyMediaEditor),panel.querySelector("[data-edit-legacy-content]")?.addEventListener("click",()=>window.editHotel?.(property.id)),panel.querySelector("[data-open-rooms-media]")?.addEventListener("click",()=>{state.activeTab="rooms",renderWorkspace()})}function renderPartnerPanel(panel){const property=state.workspace.property,owner=Core.asObject(property.owner_partner),assignments=state.workspace.operational_partners;panel.innerHTML=`${workspacePanelHeader("Partner & Access","Commercial ownership and existing operational partner routing are shown separately.")}\n      <div class="hotel-workspace-summary-grid"><section class="hotel-workspace-card"><span class="hotel-workspace-eyebrow">Commercial owner</span><h4>${escapeHtml(owner.name||"Not assigned")}</h4><p>${owner.id?`Status: ${escapeHtml(owner.status||"unknown")}`:"Assign an active commercial owner from Overview if required."}</p>${owner.id?`<code>${escapeHtml(owner.id)}</code>`:""}</section>\n      <section class="hotel-workspace-card"><span class="hotel-workspace-eyebrow">Operational assignments</span><h4>${assignments.length} assignment${1===assignments.length?"":"s"}</h4>${assignments.length?`<ul class="hotel-simple-list">${assignments.map(entry=>`<li><span>${escapeHtml(entry.name||entry.partner_id)}</span><small>${entry.is_active?"Active":"Inactive"}</small></li>`).join("")}</ul>`:"<p>No operational partner assignment.</p>"}<small>H2A is read-only for operational assignments so it cannot backfill or reroute historical fulfillments.</small></section>\n      <section class="hotel-workspace-card hotel-placeholder-card"><span class="hotel-workspace-eyebrow">Partner permissions</span><h4>Not broadened</h4><p>Partners retain the exact H1A booking/fulfillment access contract. Raw normalized tables remain unavailable.</p></section></div>`}function renderDistributionPanel(panel){const flags=state.workspace.flags;panel.innerHTML=`${workspacePanelHeader("Distribution & Sync","External sources and distribution adapters are intentionally deferred.")}\n      <section class="hotel-workspace-card hotel-placeholder-card"><span class="hotel-workspace-eyebrow">Capability status</span><h4>All Hotels V2 capabilities must remain off</h4><ul class="hotel-simple-list">${["hotel_rooms_v2_enabled","hotel_external_sync_enabled","hotel_instant_booking_enabled","hotel_stripe_connect_enabled"].map(key=>`<li><span>${escapeHtml(key.replaceAll("_"," "))}</span><strong>${!0===flags[key]?"ON — unexpected":"OFF"}</strong></li>`).join("")}</ul><p>No Booking.com, iCal, external API, instant-booking or Stripe Connect behavior is implemented here.</p></section>`}function renderActivityPanel(panel){const activity=state.workspace.activity;panel.innerHTML=`${workspacePanelHeader("Activity","Immutable reviewed changes written through the H2A Admin transaction.")}\n      <section class="hotel-workspace-card">${activity.length?`<ol class="hotel-activity-list">${activity.map(entry=>`<li><span><strong>${escapeHtml(String(entry.action||"").replaceAll("_"," "))}</strong><small>${escapeHtml(entry.entity_type||"entity")} · ${escapeHtml(entry.source||"admin")}</small></span><time datetime="${escapeAttr(entry.created_at)}">${escapeHtml(new Date(entry.created_at).toLocaleString())}</time></li>`).join("")}</ol>`:renderEmptyState("No H2A activity yet","Reviewed normalized saves will appear here.")}</section>`}return Object.freeze({state:state,init:init,loadPropertyList:loadPropertyList,renderPropertyList:renderPropertyList,openWorkspace:openWorkspace,closeWorkspace:closeWorkspace,renderWorkspace:renderWorkspace})}(root.HotelsV2WorkspaceCore,root.HotelsV2WorkspaceRepository);"object"==typeof module&&module.exports&&(module.exports=api),root.HotelsV2Workspace=api}("undefined"!=typeof globalThis?globalThis:window);